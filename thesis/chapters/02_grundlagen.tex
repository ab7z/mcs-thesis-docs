% =============================================================================
% Kapitel 2: Grundlagen
% =============================================================================

\chapter{Grundlagen}
\label{chap:grundlagen}

Dieses Kapitel stellt die Grundlagen bereit, die zum Verständnis von RFC~9868 und zur Beurteilung der
späteren Implementierung und Messungen nötig sind: IPv4 und UDP als die beiden beteiligten Protokolle
(\secref{sec:ipv4}, \secref{sec:udp}), die Erweiterungsmuster im Vergleich (\secref{sec:erweiterungsmuster}),
die Surplus Area samt Entwurfsprinzipien (\secref{sec:surplus-area}) sowie Betriebssystem und Netzpfad der
Messungen (\secref{sec:netzpfad}).

\section{IPv4}
\label{sec:ipv4}

Das Internet Protocol (IP) vermittelt Datagramme über die Grenzen einzelner Netze hinweg zwischen Endsystemen
und bildet damit die Vermittlungsschicht, auf der UDP aufsetzt \cite{rfc791}. Dass diese Arbeit eine Schicht
unterhalb ihres eigentlichen Gegenstands beginnt, hat einen konkreten Grund: Die \textit{Surplus Area}, in der
RFC~9868 seine Optionen ablegt, ist allein durch das Zusammenspiel beider Schichten definiert. Das
UDP-Length-Feld markiert das Ende der UDP-Nutzdaten; wo das umgebende IP-Datagramm endet, ergibt sich dagegen
ausschließlich aus dem IP-Header. Ohne die Längenangaben von IPv4 lässt sich daher weder feststellen, ob eine
Surplus Area vorhanden ist, noch wie groß sie ausfällt \cite[Sec.~7]{rfc9868}.

Dabei genügt der Blick auf die Version~4 des Protokolls. RFC~9868 definiert die Surplus Area für IPv4 und IPv6
gleichermaßen, und sämtliche Optionsmechanismen sind in beiden Fällen identisch; die Versionen unterscheiden
sich nur darin, wie das Ende der Nutzlast bestimmt wird \cite[Sec.~7]{rfc9868}. Bei IPv4 reichen dafür
zwei Felder des Headers, während bei IPv6 die Kette der Extension Header durchlaufen werden muss \cite{rfc8200}.
Da somit kein Mechanismus des Standards von der
IP-Version abhängt, lässt er sich vollständig an IPv4 zeigen. Implementierung und Evaluation dieser Arbeit sind
entsprechend auf IPv4 begrenzt (\secref{sec:abgrenzung}).

Auch IPv4 selbst wird in diesem Unterkapitel nicht vollständig erklärt und dargestellt.
Dieser Abschnitt behandelt ausschließlich die Themen,
Header-Elemente und Eigenschaften, die RFC~9868 und die spätere Implementierung tatsächlich berühren: die
beiden Längenangaben des Headers, das Protokollfeld, die Adressen, die Header-Prüfsumme, das
Fragmentierungsverhalten sowie das Feld \texttt{Time to Live} als Messgröße der Evaluation.

Den Ausgangspunkt bildet der Header; seinen Aufbau zeigt \figref{fig:ipv4-header} \cite{rfc791}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=0.365cm, y=1cm]
    \node[bitindex, anchor=south west] at (0,0.06) {0};
    \node[bitindex, anchor=south] at (8,0.06) {8};
    \node[bitindex, anchor=south] at (16,0.06) {16};
    \node[bitindex, anchor=south] at (24,0.06) {24};
    \node[bitindex, anchor=south east] at (32,0.06) {31};
    \draw[feld] (0,-0.72) rectangle (4,0);
    \node[feldtext] at (2,-0.36) {Version};
    \draw[feldakzent] (4,-0.72) rectangle (8,0);
    \node[feldtext] at (6,-0.36) {IHL};
    \draw[feld] (8,-0.72) rectangle (16,0);
    \node[feldtext] at (12,-0.36) {Type of Service};
    \draw[feldakzent] (16,-0.72) rectangle (32,0);
    \node[feldtext] at (24,-0.36) {Total Length};
    \draw[feld] (0,-1.54) rectangle (16,-0.82);
    \node[feldtext] at (8,-1.18) {Identification};
    \draw[feld] (16,-1.54) rectangle (19,-0.82);
    \node[feldtext] at (17.5,-1.18) {Flags};
    \draw[feld] (19,-1.54) rectangle (32,-0.82);
    \node[feldtext] at (25.5,-1.18) {Fragment Offset};
    \draw[feld] (0,-2.36) rectangle (8,-1.64);
    \node[feldtext] at (4,-2.00) {Time to Live};
    \draw[feld] (8,-2.36) rectangle (16,-1.64);
    \node[feldtext] at (12,-2.00) {Protocol};
    \draw[feld] (16,-2.36) rectangle (32,-1.64);
    \node[feldtext] at (24,-2.00) {Header Checksum};
    \draw[feld] (0,-3.18) rectangle (32,-2.46);
    \node[feldtext] at (16,-2.82) {Source Address};
    \draw[feld] (0,-4.00) rectangle (32,-3.28);
    \node[feldtext] at (16,-3.64) {Destination Address};
    \draw[feldoptional] (0,-4.82) rectangle (24,-4.10);
    \node[feldtext] at (12,-4.46) {IP-Optionen (falls vorhanden)};
    \draw[feldoptional] (24,-4.82) rectangle (32,-4.10);
    \node[feldtext] at (28,-4.46) {Padding};
    \draw[klammer] (32.35,0) -- (32.35,-4.00)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {fester Header\\20~Byte};
    \draw[klammer] (32.35,-4.10) -- (32.35,-4.82)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {$(\textit{IHL}-5) \times 4$~Byte};
  \end{tikzpicture}
  \caption{Der IPv4-Header nach RFC~791 \cite{rfc791}. Hervorgehoben sind die beiden Längenangaben, auf denen
    die Bestimmung der Surplus Area beruht; gestrichelte Felder sind nur vorhanden, wenn \texttt{IHL} über dem
    Minimalwert~5 liegt.}
  \label{fig:ipv4-header}
\end{figure}

Im Zentrum stehen die beiden Längenangaben der ersten Zeile. \texttt{Total Length} gibt die Gesamtlänge des
Datagramms in Byte an, Header und Nutzlast zusammen; als 16-Bit-Feld erlaubt es höchstens 65\,535~Byte.

Die zweite Längenangabe, die \texttt{IHL} (Internet Header Length), beziffert die Länge des Headers. Sie zählt
dabei nicht in Byte, sondern in 32-Bit-Wörtern zu je 4~Byte. Der Grund liegt in der Breite des Feldes: Mit
seinen 4~Bit kann es höchstens den Wert~15 darstellen. In Byte gezählt würde das nicht einmal für den festen
Headeranteil von 20~Byte reichen; in 32-Bit-Wörtern gezählt beschreibt derselbe Wertebereich dagegen Header bis
60~Byte \cite{rfc791}. Die Headerlänge ergibt sich daher als
\begin{equation}
  \textit{Headerlänge} = \textit{IHL} \times 4~\mbox{Byte}.
  \label{eq:ihl-headerlaenge}
\end{equation}
Der Minimalwert~5 entspricht dem optionslosen 20-Byte-Header, der Maximalwert~15 einem Header von 60~Byte.
Eine IPv4-Headerlänge ist damit stets ein Vielfaches von~4; diese unscheinbare Eigenschaft kehrt bei der
Ausrichtung der UDP Options wieder.

Zwischen festem Header und Nutzlast können zudem IP-Optionen liegen. Sie stellen Steuerfunktionen für
Sonderfälle bereit, die die gewöhnliche Kommunikation nicht braucht \cite{rfc791} und dienen als zusätzliche
Felder zur Erweiterung der IP-Header: \texttt{Record Route}
etwa sammelt die Adressen der durchlaufenen Router ein, \texttt{Internet Timestamp} deren Zeitstempel. In
der Praxis werden solche Optionen kaum genutzt: Optionstragende Pakete fallen aus dem schnellen
Weiterleitungspfad der Router heraus, gelten deshalb als Überlastrisiko und werden verbreitet gefiltert
\cite[Sec.~1 und 3.1]{rfc7126}; zudem fasst der 40-Byte-Optionsraum höchstens neun Adressen und damit viele
reale Pfade nicht. Abschließend gilt, dass erst die \texttt{IHL} und keine Konstante den Beginn der
Nutzlast festlegt.

Aus den beiden Längenangaben \texttt{Total Length} und \texttt{IHL} ergibt sich die Größe, auf die sich
RFC~9868 durchgängig bezieht: Die Nutzlast eines IPv4-Datagramms beginnt nach \texttt{IHL} $\times$ 4 Byte
und endet bei \texttt{Total Length}. Für ein
UDP-Datagramm fordert RFC~9868 deshalb \cite[Sec.~7]{rfc9868}\footnote{In Ausnahmefällen liegen zwischen
IPv4- und UDP-Header noch Shim-Header, etwa bei IPsec oder IPComp; das Feld \texttt{Protocol} zeigt dann
nicht UDP an, und die Obergrenze verringert sich zusätzlich um die Gesamtlänge dieser Shim-Header
\cite[Sec.~7]{rfc9868}. Diese Arbeit betrachtet solche Ketten nicht weiter.}
\begin{equation}
  \textit{UDP Length} \leq \textit{Total Length} - \textit{Headerlänge}.
  \label{eq:udp-length-obergrenze}
\end{equation}
Die rechte Seite der Ungleichung ist die Länge dieser Nutzlast, also der Platz hinter dem IP-Header.
Die linke Seite ist die Länge, die das UDP-Datagramm für sich beansprucht. Die Bedingung verlangt damit, dass
das UDP-Datagramm vollständig in die Nutzlast des IP-Datagramms passt; UDP darf nie mehr Platz beanspruchen, als
das IP-Datagramm hergibt. Bei Gleichheit füllt das UDP-Datagramm die Nutzlast exakt aus; so arbeiten
klassische Sender. Bleibt \texttt{UDP Length} dagegen zurück, ist die Differenz genau der Raum, den
\secref{sec:surplus-area} als Surplus Area einführt.

Von den übrigen Feldern braucht die Arbeit nur wenige. \texttt{Protocol} benennt das Transportprotokoll der
Nutzlast; der Wert~17 steht für UDP. Die \texttt{Header Checksum} sichert ausschließlich den Header, nicht die
Nutzlast; deren Fehlererkennung bleibt der UDP-Prüfsumme überlassen (\secref{subsec:udp-struktur}). Quell- und
Zieladresse geben Absender und Empfänger des Datagramms an. Sie werden später noch einmal wichtig: Die
UDP-Prüfsumme bezieht beide Adressen über einen sogenannten Pseudo-Header in ihre Berechnung ein.
\texttt{Version} und \texttt{Type of Service} berühren den Optionsmechanismus nicht und werden nicht weiter
vertieft.

Auch \texttt{Time to Live} spielt für die Optionen keine Rolle, kehrt in der Evaluation aber als
Messgröße wieder: Jeder Router verringert den Wert beim Weiterleiten um eins und verwirft das Datagramm, wenn
null erreicht ist \cite{rfc791}. Aus der Differenz zwischen gesendetem und empfangenem Wert liest
\chapref{chap:evaluation} daher die Zahl der durchlaufenen IPv4-Router ab und prüft damit die Stabilität der
Messpfade. Die zweite Zeile des Headers schließlich (\texttt{Identification}, \texttt{Flags},
\texttt{Fragment Offset}) gehört vollständig zur Fragmentierung, der sich der Rest dieses Abschnitts widmet.

Wie groß ein Datagramm praktisch werden kann, entscheidet allerdings nicht das 16-Bit-Längenfeld, sondern der
Übertragungsweg. Jede Technik der Sicherungsschicht begrenzt die Nutzlast eines einzelnen Rahmens; diese
Obergrenze heißt Maximum Transmission Unit (MTU) und liegt für Ethernet üblicherweise bei 1500~Byte. Streng
genommen ist die MTU also eine Eigenschaft der Sicherungsschicht. Ihre Folgen trägt jedoch die
Vermittlungsschicht, denn ein Datagramm durchquert auf dem Weg zum Ziel mehrere Netze mit möglicherweise
unterschiedlichen MTUs, und IP muss festlegen, was mit einem Datagramm geschieht, das nicht in den nächsten
Rahmen passt \cite{rfc791}.

IPv4 beantwortet das mit Fragmentierung. Ist ein Datagramm größer als die MTU und das Flag \texttt{DF} (Don't
Fragment) nicht gesetzt, zerlegt die IP-Schicht es in mehrere Fragmente. Das übernimmt nicht nur der
Absender: Bei IPv4 darf auch jeder Router unterwegs ein Datagramm zerlegen, das nicht in sein nächstes Netz
passt \cite{rfc791}. Jedes Fragment erhält einen eigenen IP-Header, übernimmt die \texttt{Identification} des
Originals, trägt in \texttt{Fragment Offset} seine Position in Einheiten von 8~Byte und setzt das Flag
\texttt{MF} (More Fragments), das nur beim letzten Fragment fehlt. Der Empfänger sammelt zusammengehörige
Fragmente anhand von Quelladresse, Zieladresse, Protokoll und
\texttt{Identification} und setzt das Datagramm vor der Übergabe an die Transportschicht wieder zusammen
\cite{rfc791}. Ist \texttt{DF} dagegen gesetzt, verwirft ein Router das zu große Datagramm und meldet dies dem
Absender per ICMP; auf dieser Rückmeldung baut die Path MTU Discovery auf, mit der ein Sender die kleinste MTU
seines Pfades ermittelt \cite[Sec.~3.2]{rfc8085}.

Für diese Arbeit ist an der Fragmentierung vor allem eine Konsequenz bedeutsam: Der UDP-Header gehört aus Sicht
von IP zur Nutzlast und landet deshalb vollständig im ersten Fragment. Was das für die übrigen Fragmente
bedeutet, zeigt \figref{fig:ip-fragmentierung} am Beispiel eines in drei Teile zerlegten Datagramms.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \node[font=\tiny, text=feldlinie, anchor=south west] at (0,0.06) {Original-Datagramm};
    \draw[feld] (0,-0.72) rectangle (1.7,0);
    \node[feldtext] at (0.85,-0.36) {IP-Header};
    \draw[feldakzent] (1.7,-0.72) rectangle (3.4,0);
    \node[feldtext] at (2.55,-0.36) {UDP-Header};
    \draw[feld] (3.4,-0.72) rectangle (10.4,0);
    \node[feldtext] at (6.9,-0.36) {UDP-Nutzdaten};
    \draw[feldlinie, thin, dashed] (5.4,-0.72) -- (5.4,-2.70);
    \draw[feldlinie, thin, dashed] (8.0,-0.72) -- (8.0,-3.85);
    \node[font=\tiny, text=feldlinie, anchor=south west] at (0,-1.53) {Fragment 1};
    \draw[feld] (0,-2.27) rectangle (1.7,-1.55);
    \node[feldtext] at (0.85,-1.91) {IP-Header};
    \draw[feldakzent] (1.7,-2.27) rectangle (3.4,-1.55);
    \node[feldtext] at (2.55,-1.91) {UDP-Header};
    \draw[feld] (3.4,-2.27) rectangle (5.4,-1.55);
    \node[feldtext] at (4.4,-1.91) {Teil 1};
    \node[font=\tiny, text=feldlinie, anchor=south west] at (3.7,-2.68) {Fragment 2};
    \draw[feld] (3.7,-3.42) rectangle (5.4,-2.70);
    \node[feldtext] at (4.55,-3.06) {IP-Header};
    \draw[feld] (5.4,-3.42) rectangle (8.0,-2.70);
    \node[feldtext] at (6.7,-3.06) {Teil 2};
    \node[font=\scriptsize, text=warnton, anchor=west, align=left] at (8.25,-3.06) {keine\\Portnummern};
    \node[font=\tiny, text=feldlinie, anchor=south west] at (6.3,-3.83) {Fragment 3};
    \draw[feld] (6.3,-4.57) rectangle (8.0,-3.85);
    \node[feldtext] at (7.15,-4.21) {IP-Header};
    \draw[feld] (8.0,-4.57) rectangle (10.4,-3.85);
    \node[feldtext] at (9.2,-4.21) {Teil 3};
    \node[font=\scriptsize, text=warnton, anchor=west, align=left] at (10.65,-4.21) {keine\\Portnummern};
  \end{tikzpicture}
  \caption{Schematische Zerlegung eines UDP-Datagramms in drei IPv4-Fragmente}
  \label{fig:ip-fragmentierung}
\end{figure}

Aus \figref{fig:ip-fragmentierung} geht hervor, dass nur das erste Fragment den UDP-Header enthält: Alle
Folgefragmente tragen keine Portnummern. Zwischensysteme wie NATs und Firewalls, die Pakete anhand
vollständiger Header zuordnen oder prüfen, können solche Fragmente nicht einordnen und verwerfen sie teils
pauschal; zudem macht der Verlust eines einzelnen Fragments das gesamte Datagramm unbrauchbar. RFC~8085 wertet
beides als grundsätzliches Problem der IP-Fragmentierung, das jedes Transportprotokoll trifft, und rät
UDP-Anwendungen deshalb, IP-Fragmentierung zu vermeiden \cite[Sec.~3.2]{rfc8085}. Dass der Rat gerade an UDP
geht, hat einen Grund: Während TCP seinen Datenstrom selbst in passende Segmente zerlegt und der
IP-Fragmentierung so meist entgeht \cite{rfc9293}, erzeugt UDP je Nachricht genau ein Datagramm
(\secref{subsec:udp-eigenschaften}); ein zu großes Datagramm kann bislang nur die IP-Schicht zerteilen. Genau
diese Lücke schließt die FRAG-Option von RFC~9868: Sie verlagert die Fragmentierung auf die Transportschicht
und wiederholt die Portnummern in jedem Fragment \cite[Sec.~11.4]{rfc9868}; den Verlust einzelner Fragmente
behebt allerdings auch FRAG nicht. Darauf kommt \chapref{chap:analyse} zurück.

Der folgende Abschnitt wendet sich dem Protokoll zu, das RFC~9868 tatsächlich erweitert: UDP.

\section{Das User Datagram Protocol (UDP)}
\label{sec:udp}

Das User Datagram Protocol (UDP) ist neben dem Transmission Control Protocol (TCP) eines der beiden zentralen
Transportprotokolle der Transportschicht. Im Gegensatz zu TCP verfolgt UDP ein bewusst minimalistisches Design:
Es sendet Datagramme ohne vorherigen Handshake oder Zustandsabgleich mit dem Empfänger, bietet keine Garantien
für Zustellung, Reihenfolge oder Duplikaterkennung und verzichtet
auf jeglichen Flusskontroll- oder Staukontrollmechanismus \cite{rfc768}. Mit der Protokollnummer~17 im IP-Header
registriert, wurde UDP in RFC~768 spezifiziert und trägt die Bezeichnung STD~6 als Internet-Standard. Bemerkenswert
ist, dass RFC~768 seit seiner Veröffentlichung im August 1980 über 45~Jahre hinweg keine formelle Aktualisierung
erfahren hat. Erst RFC~9868 trägt den Header-Eintrag \texttt{Updates:~768} und erweitert damit den ursprünglichen
UDP-Standard erstmalig direkt \cite{rfc9868}.

Historisch geht UDP auf die Aufspaltung des ursprünglichen \emph{Transmission Control Program} zurück, das
Cerf, Dalal und Sunshine Ende 1974 noch als monolithische Einheit aus Netzwerk- und Transportfunktionen
beschrieben \cite{rfc675}. Mit deren Trennung in IP und TCP entstand der Bedarf nach einer schlanken,
transaktionsorientierten Alternative ohne den Overhead von TCP.
Während der UDP-Kern über die folgenden Jahrzehnte unverändert blieb und nur
Begleitdokumente Randfragen klärten, ließ erst die wachsende Bedeutung UDP-basierter Protokolle wie QUIC
\cite{rfc9000} und HTTP/3 \cite{rfc9114} mit RFC~9868 die erste direkte Erweiterung des Standards entstehen.

\subsection{Protokollstruktur}
\label{subsec:udp-struktur}

Ein UDP-Datagramm besteht aus einem Header mit fester Länge von 8~Byte und den darauf folgenden Nutzdaten;
den Aufbau des Headers zeigt \figref{fig:udp-header} \cite{rfc768}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=0.365cm, y=1cm]
    \node[bitindex, anchor=south west] at (0,0.06) {0};
    \node[bitindex, anchor=south] at (8,0.06) {8};
    \node[bitindex, anchor=south] at (16,0.06) {16};
    \node[bitindex, anchor=south] at (24,0.06) {24};
    \node[bitindex, anchor=south east] at (32,0.06) {31};
    \draw[feld] (0,-0.72) rectangle (16,0);
    \node[feldtext] at (8,-0.36) {Source Port};
    \draw[feld] (16,-0.72) rectangle (32,0);
    \node[feldtext] at (24,-0.36) {Destination Port};
    \draw[feld] (0,-1.54) rectangle (16,-0.82);
    \node[feldtext] at (8,-1.18) {Length};
    \draw[feld] (16,-1.54) rectangle (32,-0.82);
    \node[feldtext] at (24,-1.18) {Checksum};
    \draw[klammer] (32.35,0) -- (32.35,-1.54)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {fester Header\\8~Byte};
  \end{tikzpicture}
  \caption{Der UDP-Header nach RFC~768 \cite{rfc768}}
  \label{fig:udp-header}
\end{figure}

Aus \figref{fig:udp-header} geht hervor, dass der gesamte Header aus genau vier Feldern zu je 16~Bit
besteht; unter ihnen das Längenfeld, für das \secref{sec:ipv4} mit \eqnref{eq:udp-length-obergrenze} bereits
die Obergrenze aufgestellt hat. Die vier Felder im Einzelnen:

\begin{itemize}
    \item \texttt{Source Port}: Die Portnummer des sendenden Prozesses. Dieses Feld ist optional; wird es nicht
    benötigt, wird es auf null gesetzt \cite{rfc768}. In der Praxis nutzen Anwendungen den \textit{Source Port}
    zur Identifikation des Absenders, damit der Empfänger Antworten an den korrekten Port zurücksenden kann.

    \item \texttt{Destination Port}: Die Portnummer des Zielprozesses auf dem empfangenden Host. Zusammen mit der
    IP-Zieladresse ermöglicht dieses Feld die \textit{Demultiplexierung} eingehender Datagramme an die korrekte
    Anwendung.

    \item \texttt{Length}: Die Gesamtlänge des UDP-Datagramms in Byte, bestehend aus Header \emph{und} Nutzdaten.
    Der Minimalwert beträgt~8, was einem Datagramm ohne Nutzdaten entspricht \cite{rfc768}. Da das Feld 16~Bit
    breit ist, ergibt sich eine theoretische Maximalgröße von 65\,535~Byte.

    \item \texttt{Checksum}: Eine Prüfsumme zur Fehlererkennung über einen Pseudo-Header, den UDP-Header
    und die Nutzdaten; ihr Verfahren und ihre Sonderwerte erläutert der folgende Absatz.
\end{itemize}

\paragraph{Prüfsumme.}
Die Prüfsumme dient der Fehlererkennung: Mit ihr erkennt der Empfänger, ob einzelne Bits des Datagramms auf
dem Weg verändert wurden. Diese Prüfung auf der Transportschicht ist nötig, weil nicht jede Teilstrecke
eines Pfades eigene Fehlererkennung bietet und Bits auch im Speicher eines Routers kippen können
\cite{kurose2021topdown}. Dennoch ist die Prüfsumme optional.
Für die Berechnung summiert der Sender Pseudo-Header, UDP-Header und Nutzdaten
als 16-Bit-Wörter im Einerkomplement auf (ein Übertrag wird am niederwertigen Ende wieder hinzugezählt);
das invertierte Ergebnis bildet den Feldwert \cite{rfc768,rfc1071}. Der Empfänger summiert dieselben Wörter
einschließlich des Prüfsummenfelds und erwartet lauter Einsen, also \texttt{0xFFFF}; jedes andere Ergebnis
zeigt einen Übertragungsfehler an \cite{rfc1071}.

Dabei hilft eine Eigenheit des Einerkomplements: Es kennt zwei Darstellungen der Null, \texttt{0x0000} und
\texttt{0xFFFF}. Diese Doppelrolle nutzt UDP für die Sonderwerte des Feldes:
eine übertragene \texttt{0x0000} bedeutet, dass der Sender keine berechnet hat. Ergibt die
Berechnung selbst den Wert null, wird stattdessen \texttt{0xFFFF} übertragen.
Dadurch bleibt im Einerkomplement die Zahl Null erhalten, und der Sonderwert bleibt eindeutig \cite{rfc768}.
Dabei ist zu beachten, dass UDP Fehler nur erkennen und nicht beheben kann;
ein beschädigtes Datagramm wird typischerweise verworfen \cite{kurose2021topdown}.

\paragraph{Pseudo-Header.}
Die UDP-Prüfsumme schützt nicht nur die Transportschichtdaten, sondern bezieht über einen \textit{Pseudo-Header}
auch Informationen der Vermittlungsschicht ein. Dieser Pseudo-Header ist kein Teil des Datagramms und steht an
keiner Stelle im Paket: Sender und Empfänger bauen ihn jeweils nur für die Berechnung im Speicher auf und
stellen ihn dem UDP-Header dabei gedanklich voran \cite{rfc768}. Dieses Konzept stellt sicher, dass ein
Datagramm, das durch einen IP-Fehler an den falschen Host oder das falsche Protokoll zugestellt wird, erkannt
und verworfen werden kann. Seinen Aufbau zeigt \figref{fig:udp-pseudo-header} \cite{rfc768}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=0.365cm, y=1cm]
    \node[bitindex, anchor=south west] at (0,0.06) {0};
    \node[bitindex, anchor=south] at (8,0.06) {8};
    \node[bitindex, anchor=south] at (16,0.06) {16};
    \node[bitindex, anchor=south] at (24,0.06) {24};
    \node[bitindex, anchor=south east] at (32,0.06) {31};
    \draw[feldoptional] (0,-0.72) rectangle (32,0);
    \node[feldtext] at (16,-0.36) {Source Address};
    \draw[feldoptional] (0,-1.54) rectangle (32,-0.82);
    \node[feldtext] at (16,-1.18) {Destination Address};
    \draw[feldoptional] (0,-2.36) rectangle (8,-1.64);
    \node[feldtext] at (4,-2.00) {Zero};
    \draw[feldoptional] (8,-2.36) rectangle (16,-1.64);
    \node[feldtext] at (12,-2.00) {Protocol};
    \draw[feldoptional] (16,-2.36) rectangle (32,-1.64);
    \node[feldtext] at (24,-2.00) {UDP Length};
    \draw[klammer] (32.35,0) -- (32.35,-2.36)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {12~Byte};
  \end{tikzpicture}
  \caption{Der Pseudo-Header der UDP-Prüfsumme für IPv4 \cite{rfc768}}
  \label{fig:udp-pseudo-header}
\end{figure}

Aus \figref{fig:udp-pseudo-header} geht hervor, dass der Pseudo-Header ausschließlich aus Werten besteht,
die beiden Seiten ohnehin vorliegen: den beiden Adressen und dem Feld \texttt{Protocol} aus dem IP-Header
(\secref{sec:ipv4}), einem Nullbyte als Füllung sowie einer Wiederholung des UDP-Längenfelds. Nur deshalb
können Sender und Empfänger unabhängig voneinander dieselben zwölf Byte zusammensetzen; die gestrichelte
Darstellung kennzeichnet, dass keines dieser Felder je übertragen wird \cite{rfc768}.

Ein letzter Blick gilt dem Feld \texttt{Zero}: Es trägt keine Information, sondern stellt die Ausrichtung
her. Die Prüfsummenarithmetik arbeitet ausschließlich auf 16-Bit-Wörtern \cite{rfc1071}, das Protokollfeld
aus dem IP-Header ist aber nur 8~Bit breit; das vorangestellte Nullbyte ergänzt es zu einem vollen Wort.
Nach demselben Prinzip werden auch Nutzdaten ungerader Länge für die Berechnung am Ende mit einem Nulloktett
aufgefüllt \cite{rfc768}. Durch ein anderes Feld aus dem IP-Header, etwa \texttt{Time to Live}, lässt sich
das Nullbyte dagegen nicht ersetzen: Jeder Router verringert diesen Wert beim Weiterleiten
(\secref{sec:ipv4}); der Empfänger würde also mit einem anderen Wert rechnen als der Sender, und die
Prüfung ginge selbst bei fehlerfreier Übertragung nie auf. In den Pseudo-Header gehören nur Werte, die
unterwegs unverändert bleiben; ein festes Nullbyte erfüllt das von selbst.

\subsection{Eigenschaften}
\label{subsec:udp-eigenschaften}

Die in \secref{subsec:udp-struktur} beschriebene Header-Struktur verdeutlicht bereits das minimalistische Design
von UDP. Aus diesem Designziel leiten sich eine Reihe funktionaler Eigenschaften ab, die maßgeblich beeinflussen,
für welche Anwendungsfälle das Protokoll geeignet ist.

\paragraph{Nachrichtenorientierung.}
UDP ist ein \textit{nachrichtenorientiertes} Protokoll: Jede Sendeoperation erzeugt genau ein Datagramm, und der
Empfänger erhält jede Nachricht als atomare Einheit. TCP arbeitet dagegen \textit{stromorientiert}: Es bietet der
Anwendung einen fortlaufenden Bytestrom, den es für die Übertragung selbst in Segmente zerlegt
\cite[Sec.~2.2]{rfc9293}. Sendet eine Anwendung etwa nacheinander \texttt{\dq hello\dq}, \texttt{\dq world\dq}
und \texttt{\dq !\dq}, stellt UDP drei Datagramme mit genau diesen Nachrichten zu; ein TCP-Empfänger kann
denselben Strom auch als \texttt{\dq hellowo\dq} und \texttt{\dq rld!\dq} erhalten \cite[Sec.~3.7]{rfc9293}.
Eine Anwendung, die über TCP einzelne Nachrichten austauschen will, muss deren Grenzen deshalb selbst
kennzeichnen, etwa durch Längenfelder; bei UDP entfällt das, weil jedes Datagramm genau eine Nachricht trägt.

\paragraph{Zustandslosigkeit.}
UDP verwaltet keinen protokollspezifischen Zustand für einzelne Kommunikationsbeziehungen und hält keinerlei
Datenstrukturen wie Sequenznummern, Fenstergrößen oder Retransmission-Timer vor \cite[Sec.~6]{rfc9868}.

\paragraph{Minimale Latenz.}
Da UDP verbindungslos arbeitet, entfällt jeglicher Handshake vor der Datenübertragung. Die erste Nachricht kann
unmittelbar gesendet werden, ohne dass ein vorheriger Verbindungsaufbau abgewartet werden muss
\cite[Sec.~1]{rfc8085}.

\paragraph{Unabhängigkeit der Datagramme.}
Jedes UDP-Datagramm ist eine eigenständige, von allen anderen Datagrammen unabhängige Einheit. Der Verlust eines
Datagramms hat daher keine Auswirkung auf die Verarbeitung nachfolgender Datagramme. Diese Eigenschaft vermeidet
das sogenannte \textit{Head-of-Line-Blocking} (HoL-Blocking), bei dem verlorene Pakete die Auslieferung
nachfolgender Daten verzögern. Diese Eigenschaft von UDP war eine der Motivationen für die Entwicklung von QUIC:
QUIC nutzt UDP als Transportschicht und realisiert auf Anwendungsebene ein Stream-Multiplexing, bei dem der Verlust
eines Pakets nur den betroffenen Stream blockiert, nicht jedoch andere parallele Streams \cite[Sec.~1]{rfc9000}.

Diese Eigenschaften machen UDP zu einem attraktiven Transportprotokoll für latenzempfindliche oder
verlusttolerante Anwendungen. Allerdings fehlt UDP jede Form der protokolleigenen Erweiterbarkeit. Genau
diese Lücke adressiert RFC~9868 durch die Nutzung der
\textit{Surplus Area} (siehe \secref{sec:surplus-area}), um Optionen zu transportieren, ohne den bestehenden
UDP-Header zu verändern.

\section{Erweiterungsmuster im Vergleich}
\label{sec:erweiterungsmuster}

Nun stellt sich die Frage, wie ein Protokoll nachträglich erweitert werden kann, dessen Header weder freie Bits
noch ein Versionsfeld bietet. Das klassische Muster ist ein Optionsbereich zwischen festem Header und Nutzdaten:
Ein Offsetfeld gibt an, wo die Nutzdaten beginnen, und der Raum dazwischen steht für Optionen zur Verfügung. TCP
arbeitet genau so. Sein Feld \texttt{Data Offset} zählt wie die \texttt{IHL} in 32-Bit-Wörtern und ist 4~Bit
breit; vom 60-Byte-Maximum bleiben nach dem festen 20-Byte-Header höchstens 40~Byte für Optionen wie Maximum
Segment Size, Window Scale oder Timestamps \cite{rfc9293}. Dasselbe Muster kennt auch IPv4: Die IP-Optionen
aus \secref{sec:ipv4} liegen genauso zwischen festem Header und Nutzlast. TCP und IPv4 teilen dabei sogar das
Füllmuster aus den Ein-Byte-Codes \texttt{NOP} (No Operation) und \texttt{EOL} (End of Option List).

Für UDP steht dieser Weg nicht offen. Sein Header besteht aus genau vier 16-Bit-Feldern
(\secref{subsec:udp-struktur}); es gibt kein Offsetfeld, kein Versionsfeld und kein reserviertes Bit, das einen
Optionsbereich ankündigen könnte. Jede Änderung am Headerformat würde sämtliche bestehenden Implementierungen
brechen und scheidet damit aus.

Ein erster Ausweg verzichtet auf jede Änderung am Protokoll und legt die Optionen in die Nutzdaten selbst.
Diesen Weg gehen die Erweiterungen von Teredo, einem Tunnelprotokoll, das IPv6-Pakete in UDP-Datagramme
verpackt und so Hosts hinter NATs den Zugang zum IPv6-Internet öffnet \cite{rfc4380}. Ihre Zusatzfelder
stehen als \textit{Trailer} hinter dem getunnelten Paket, noch innerhalb der UDP-Nutzdaten: Was ein Header
vor den Daten ist, ist ein Trailer hinter ihnen. Weil das getunnelte Paket sein Ende selbst markiert, liest
der Empfänger alles dahinter als Trailer \cite{rfc6081}. Die Stärke dieses Ansatzes liegt auf der Hand: UDP
und IP bleiben unangetastet, das Datagramm ist nach außen ein gewöhnliches Datagramm, und ein Empfänger,
der nur das innere Paket auswertet, stoppt an dessen Ende und stört sich nicht an den Trailern. Die
Schwäche liegt in der Voraussetzung: Das Verfahren funktioniert nur, wenn die Nutzdaten ein eigenes
Längenfeld mitbringen, das ihr Ende markiert. Beliebige UDP-Anwendungen erfüllen das nicht; ihnen bliebe
nur, Zusatzfelder per Konvention innerhalb der Nutzdaten von den Anwendungsdaten zu unterscheiden. Solche
In-Band-Kodierung verwirft RFC~9868 jedoch, weil Altempfänger die Konvention nicht kennen und die
Zusatzfelder als Nutzdaten lesen \cite[Sec.~4]{rfc9868}.

Den Weg zur Surplus Area öffnet schließlich eine Eigenheit, die UDP von TCP unterscheidet: UDP trägt eine
eigene Längenangabe. TCP leitet die Länge seiner Nutzlast aus der IP-Datagrammlänge ab; UDP wiederholt sie
im eigenen Header \cite[Sec.~4]{rfc9868}. Damit beschreiben
zwei unabhängige Angaben redundant dasselbe Ende der Nutzdaten: das UDP-Length-Feld und die IP-Längenangaben aus
\secref{sec:ipv4}. Genau diese Redundanz überträgt den Trailer-Gedanken auf beliebige UDP-Anwendungen, denn
das Längenfeld, das das Ende der Daten markiert, muss nun nicht mehr in den Nutzdaten stecken: UDP selbst
bringt es mit. Bleibt \texttt{UDP Length} hinter der Nutzlast des IP-Datagramms zurück, entsteht hinter den
Nutzdaten Platz, den ein Altempfänger nie zu Gesicht bekommt, weil er nur bis \texttt{UDP Length} liest.
Mit den Teredo-Trailern hat dieser Bereich nichts zu tun; beide Mechanismen bleiben deshalb kompatibel
\cite[Sec.~4]{rfc9868}. Warum RFC~768 das Längenfeld überhaupt vorsieht, ist bemerkenswerterweise
ungeklärt: Die kursierenden Erklärungen (mehrere UDP-Pakete je IP-Datagramm; Abgrenzung der Nutzdaten von
erzwungenem Null-Padding) hält RFC~9868 für unvereinbar mit früheren UDP-Entwürfen \cite[Sec.~4]{rfc9868}. Ein Feld,
dessen Zweck sich nie klärte, wird damit 45~Jahre nach seiner Definition zum Erweiterungspunkt des
Protokolls.

Von den drei Mustern ist für UDP damit allein der Trailer-Weg gangbar: Header-Optionen scheitern am
unveränderlichen Headerformat, die In-Band-Kodierung an den Altempfängern, denen die Konvention unbekannt
ist. Der Trailer bleibt für Altempfänger unsichtbar und erbt zugleich keine feste Obergrenze für den
Optionsraum, denn seine Grenze ist das Ende des IP-Datagramms. Wie dieser Bereich im Einzelnen aufgebaut
ist, zeigt der folgende Abschnitt.

\section{Die Surplus Area}
\label{sec:surplus-area}

Da das UDP-Length-Feld die Länge des UDP-Datagramms (Header und Nutzdaten) angibt und diese kleiner sein kann
als die Nutzlast, die das IP-Datagramm laut seinen Längenangaben transportiert (\secref{sec:ipv4}), entsteht
zwischen dem Ende der UDP-Nutzdaten und dem Ende des IP-Datagramms ein bislang ungenutzter Bereich. RFC~9868
nennt die gesamte Nutzlast hinter dem IP-Header \textit{IP Transport Payload} und ihren ungenutzten Teil
hinter den UDP-Nutzdaten \textit{Surplus Area}; dort liegen die UDP Options. Die Lage beider Bereiche
zeigt \figref{fig:surplus-area} an einem Beispieldatagramm mit \texttt{Total Length} 60, \texttt{IHL} 5
und \texttt{UDP Length} 32 \cite[Sec.~7]{rfc9868}:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \draw[feld] (0,-0.92) rectangle (2.6,0);
    \node[feldtext] at (1.3,-0.34) {IP-Header};
    \node[font=\tiny, text=feldlinie] at (1.3,-0.66) {20~Byte (\texttt{IHL} 5)};
    \draw[feld] (2.6,-0.92) rectangle (4.6,0);
    \node[feldtext] at (3.6,-0.34) {UDP-Header};
    \node[font=\tiny, text=feldlinie] at (3.6,-0.66) {8~Byte};
    \draw[feld] (4.6,-0.92) rectangle (9.4,0);
    \node[feldtext] at (7.0,-0.34) {UDP-Nutzdaten};
    \node[font=\tiny, text=feldlinie] at (7.0,-0.66) {24~Byte};
    \draw[feldakzent] (9.4,-0.92) rectangle (12.4,0);
    \node[feldtext] at (10.9,-0.34) {Surplus Area};
    \node[font=\tiny, text=feldlinie] at (10.9,-0.66) {8~Byte};
    \node[bitindex, anchor=north] at (0,-1.04) {0};
    \node[bitindex, anchor=north] at (2.6,-1.04) {20};
    \node[bitindex, anchor=north] at (4.6,-1.04) {28};
    \node[bitindex, anchor=north] at (9.4,-1.04) {52};
    \node[bitindex, anchor=north] at (12.4,-1.04) {60};
    \draw[klammer] (2.6,0.12) -- (12.4,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {IP Transport Payload (40~Byte)};
    \draw[klammer] (9.4,-1.56) -- (2.6,-1.56)
      node[midway, below=7pt, font=\scriptsize, text=feldlinie] {\texttt{UDP Length} = 32};
  \end{tikzpicture}
  \caption{IP Transport Payload und Surplus Area eines Datagramms mit \texttt{Total Length} 60}
  \label{fig:surplus-area}
\end{figure}

Aus \figref{fig:surplus-area} geht hervor, dass beide Größen unmittelbar hinter dem IP-Header beginnen:
\texttt{UDP Length} deckt von der IP Transport Payload nur UDP-Header und Nutzdaten ab, und die Surplus
Area ist exakt der Rest bis zum Ende des IP-Datagramms. Im Beispiel umfasst die IP Transport Payload
$60 - 20 = 40$~Byte, von denen \texttt{UDP Length} nur 32 beansprucht; die übrigen 8~Byte bilden die
Surplus Area. Allgemein gilt
\begin{equation}
  \textit{Surplus-Länge} = \textit{Total Length} - \textit{Headerlänge} - \textit{UDP Length},
  \label{eq:surplus-laenge}
\end{equation}
wobei die Obergrenze aus \eqnref{eq:udp-length-obergrenze} sicherstellt, dass diese Differenz nie
negativ wird. Im Regelfall des heutigen Internets ist sie schlicht null: \texttt{UDP Length} und
IP-Längenangaben enden am selben Byte, und es gibt keine Surplus Area. RFC~768 verlangt diese Gleichheit
allerdings nicht; die Freiheit besteht seit seiner Veröffentlichung, RFC~9868 nutzt sie lediglich
\cite[Sec.~7]{rfc9868}.

Für den Beginn der Surplus Area schreibt RFC~9868 dabei bewusst keine Ausrichtung vor: Sie darf an jedem
gültigen Byte-Offset anfangen, auch an einem ungeraden. Die Nutzdaten bleiben dadurch unangetastet, und
das Ausrichtungsproblem wird, wie gleich zu sehen ist, innerhalb der Surplus Area gelöst. Im Ergebnis
deutet RFC~9868 das Feld \texttt{UDP Length} in einen \textit{trailer options offset} um: einen Zeiger
auf das erste Byte hinter den Nutzdaten, an dem die Optionen beginnen \cite[Sec.~7]{rfc9868}. Genau
darin liegt der inhaltliche Kern der Aktualisierung von RFC~768: Kein Feld ändert sein Format, ein Feld
ändert seine Bedeutung \cite{rfc9868}.

Innerhalb dieses Bereichs legt RFC~9868 eine feste Struktur fest: Die Optionen nutzen stets die gesamte
Surplus Area und beginnen mit der \textit{Option Checksum} (OCS), einem 2~Byte großen Prüfsummenfeld an
der ersten 2-Byte-Grenze der Area, gemessen am Beginn des IP-Datagramms. Beginnt die Surplus Area an
einem ungeraden Offset, stellt genau ein Nullbyte vor dem OCS diese Ausrichtung her (das \textit{Pad}).
Hinter dem OCS folgen die eigentlichen Optionen im TLV-Format (Type-Length-Value); die Liste läuft bis
zum Ende der Surplus Area oder endet vorzeitig mit \texttt{EOL}, worauf nur noch Nullbytes folgen
\cite[Sec.~8]{rfc9868}. Diese Anatomie zeigt \figref{fig:surplus-struktur} an einem Datagramm mit
5~Byte Nutzdaten und einer einzelnen Option:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \draw[feld] (0,-0.92) rectangle (2.0,0);
    \node[feldtext] at (1.0,-0.34) {IP-Header};
    \node[font=\tiny, text=feldlinie] at (1.0,-0.66) {20~Byte};
    \draw[feld] (2.0,-0.92) rectangle (3.7,0);
    \node[feldtext] at (2.85,-0.34) {UDP-Header};
    \node[font=\tiny, text=feldlinie] at (2.85,-0.66) {8~Byte};
    \draw[feld] (3.7,-0.92) rectangle (5.3,0);
    \node[feldtext] at (4.5,-0.34) {Nutzdaten};
    \node[font=\tiny, text=feldlinie] at (4.5,-0.66) {5~Byte};
    \draw[feldoptional] (5.3,-0.92) rectangle (5.85,0);
    \node[feldtext, font=\scriptsize\ttfamily] at (5.575,-0.46) {00};
    \draw[feldakzent] (5.85,-0.92) rectangle (6.95,0);
    \node[feldtext] at (6.4,-0.34) {OCS};
    \node[font=\tiny, text=feldlinie] at (6.4,-0.66) {2~Byte};
    \draw[feldakzent] (6.95,-0.92) rectangle (7.85,0);
    \node[feldtext] at (7.4,-0.34) {Kind};
    \node[font=\tiny, text=feldlinie] at (7.4,-0.66) {1~Byte};
    \draw[feldakzent] (7.85,-0.92) rectangle (8.95,0);
    \node[feldtext] at (8.4,-0.34) {Length};
    \node[font=\tiny, text=feldlinie] at (8.4,-0.66) {1~Byte};
    \draw[feldakzent] (8.95,-0.92) rectangle (10.55,0);
    \node[feldtext] at (9.75,-0.46) {Value \dots};
    \draw[feldakzent] (10.55,-0.92) rectangle (11.45,0);
    \node[feldtext] at (11.0,-0.34) {EOL};
    \node[font=\tiny, text=feldlinie] at (11.0,-0.66) {\texttt{0x00}};
    \draw[feldakzent] (11.45,-0.92) rectangle (13.05,0);
    \node[feldtext] at (12.25,-0.34) {Nullfüllung};
    \node[font=\tiny, text=feldlinie] at (12.25,-0.66) {\texttt{0x00} \dots};
    \node[bitindex, anchor=north] at (0,-1.04) {0};
    \node[bitindex, anchor=north] at (2.0,-1.04) {20};
    \node[bitindex, anchor=north] at (3.7,-1.04) {28};
    \node[bitindex, anchor=north east] at (5.42,-1.04) {33};
    \node[bitindex, anchor=north west] at (5.73,-1.04) {34};
    \node[bitindex, anchor=north] at (6.95,-1.04) {36};
    \draw[klammer] (2.0,0.12) -- (5.3,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {\texttt{UDP Length} = 13};
    \draw[klammer] (6.95,0.12) -- (10.55,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {eine Option (TLV)};
    \draw[klammer] (13.05,-1.56) -- (5.3,-1.56)
      node[midway, below=7pt, font=\scriptsize, text=feldlinie] {Surplus Area};
    \draw[draw=feldlinie, thin] (2.0,-2.30) -- (5.3,-2.30);
    \draw[draw=feldlinie, thin] (2.0,-2.22) -- (2.0,-2.38);
    \draw[draw=feldlinie, thin] (5.3,-2.22) -- (5.3,-2.38);
    \node[font=\tiny, text=feldlinie] at (3.65,-2.58) {Deckungsbereich der UDP-Prüfsumme};
    \draw[draw=akzent, semithick] (5.38,-2.30) -- (13.05,-2.30);
    \draw[draw=akzent, semithick] (5.38,-2.22) -- (5.38,-2.38);
    \draw[draw=akzent, semithick] (13.05,-2.22) -- (13.05,-2.38);
    \node[font=\tiny, text=akzent] at (9.2,-2.58) {Deckungsbereich des OCS};
  \end{tikzpicture}
  \caption{Anatomie eines Datagramms mit UDP Options}
  \label{fig:surplus-struktur}
\end{figure}

Aus \figref{fig:surplus-struktur} geht hervor, wie die Bausteine ineinandergreifen: \texttt{UDP Length}
$= 8 + 5 = 13$ lässt die Surplus Area bei Byte $20 + 13 = 33$ beginnen. Dieser Offset ist ungerade, also
rückt ein Pad-Byte das OCS auf die 2-Byte-Grenze bei Byte 34, und die erste Option beginnt bei Byte 36.
Die unterste Ebene der Abbildung zeigt zugleich, dass sich die beiden Prüfbereiche lückenlos ergänzen:
Die UDP-Prüfsumme endet an \texttt{UDP Length}, das OCS deckt genau den Teil des Datagramms, den die
UDP-Prüfsumme nie sieht \cite[Sec.~8]{rfc9868}.

Mit der Ausrichtung beginnt zugleich der normative Teil des RFC: Seine ersten verbindlichen Regeln
überhaupt betreffen genau diese Stelle \cite[Sec.~8]{rfc9868}. Der Sender darf Optionen an jedem
gültigen \texttt{UDP-Length}-Offset beginnen lassen; Ausrichtungsbytes vor dem OCS müssen null sein; und
findet der Empfänger dort einen anderen Wert, muss er alle Optionen ignorieren und die Surplus Area
stillschweigend verwerfen. Verworfen wird dabei nur die Surplus Area, nicht das Datagramm: Nutzdaten mit
gültiger UDP-Prüfsumme werden weiterhin zugestellt \cite[Sec.~8 und 9]{rfc9868}. Die 2-Byte-Ausrichtung
selbst begründet der RFC pragmatisch: Sie erspart der Prüfsummenberechnung das Vertauschen von Bytes
\cite[Sec.~8]{rfc9868}.

Ob ein Pad-Byte nötig ist, entscheidet allein die Parität des Startoffsets; es gibt nur zwei Fälle, kein
Pad oder genau eines. Den Unterschied zeigt \figref{fig:surplus-pad} an zwei sonst gleichen Datagrammen,
deren Nutzdaten sich um ein einziges Byte unterscheiden:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \node[feldtext, anchor=west] at (0,-0.30) {5~Byte Nutzdaten};
    \node[font=\tiny, text=feldlinie, anchor=west] at (0,-0.62) {Start bei $20 + 13 = 33$};
    \draw[feld] (3.2,-0.92) rectangle (5.0,0);
    \node[feldtext] at (4.1,-0.46) {UDP-Header};
    \draw[feld] (5.0,-0.92) rectangle (6.4,0);
    \node[feldtext] at (5.7,-0.46) {Nutzdaten};
    \draw[feldoptional] (6.4,-0.92) rectangle (6.95,0);
    \node[feldtext, font=\scriptsize\ttfamily] at (6.675,-0.46) {00};
    \draw[feldakzent] (6.95,-0.92) rectangle (8.05,0);
    \node[feldtext] at (7.5,-0.46) {OCS};
    \draw[feldakzent] (8.05,-0.92) rectangle (10.0,0);
    \node[feldtext] at (9.025,-0.46) {Optionen \dots};
    \node[font=\tiny, text=feldlinie, anchor=west] at (10.3,-0.46) {ungerade: ein Pad, OCS ab 34};
    \node[feldtext, anchor=west] at (0,-1.70) {6~Byte Nutzdaten};
    \node[font=\tiny, text=feldlinie, anchor=west] at (0,-2.02) {Start bei $20 + 14 = 34$};
    \draw[feld] (3.2,-2.32) rectangle (5.0,-1.40);
    \node[feldtext] at (4.1,-1.86) {UDP-Header};
    \draw[feld] (5.0,-2.32) rectangle (6.68,-1.40);
    \node[feldtext] at (5.84,-1.86) {Nutzdaten};
    \draw[feldakzent] (6.68,-2.32) rectangle (7.78,-1.40);
    \node[feldtext] at (7.23,-1.86) {OCS};
    \draw[feldakzent] (7.78,-2.32) rectangle (9.73,-1.40);
    \node[feldtext] at (8.755,-1.86) {Optionen \dots};
    \node[font=\tiny, text=feldlinie, anchor=west] at (10.3,-1.86) {gerade: kein Pad, OCS ab 34};
  \end{tikzpicture}
  \caption{Paritätsentscheid für das Pad-Byte vor dem OCS}
  \label{fig:surplus-pad}
\end{figure}

Aus \figref{fig:surplus-pad} geht hervor, dass bereits ein einziges Nutzdatenbyte über das Pad
entscheidet: Bei einem IP-Header ohne Optionen liegt der Startoffset bei $20 + \textit{UDP Length}$,
sodass allein die Parität von \texttt{UDP Length} den Ausschlag gibt. Zwei Randfälle gehören zu diesem
Bild. Fehlt die Surplus Area ganz, weil \texttt{UDP Length} die IP-Nutzlast vollständig ausfüllt, liegt
schlicht ein gewöhnliches UDP-Datagramm vor: Es gibt nichts zu deuten, auch ein optionsfähiger Empfänger
stellt die Nutzdaten wie ein Altempfänger zu, und die Optionsverarbeitung entfällt ersatzlos
\cite[Sec.~7]{rfc9868}. Gebunden ist die Deutung als Optionen außerdem an eine Mindestgröße: Die Surplus
Area trägt nur dann Optionen, wenn das ausgerichtete OCS vollständig hineinpasst. Ein Überschuss von
einem einzelnen Byte (bei ungeradem Start: von zwei Byte) enthält daher schlicht keine Optionen und ist
auch kein Fehlerfall \cite[Sec.~8]{rfc9868}.
Nutzdaten braucht es dagegen keine: Auch ein Datagramm mit \texttt{UDP Length} 8, also leerem
Nutzdatenteil, kann Optionen tragen \cite[Sec.~8]{rfc9868}; davon macht später die FRAG-Option Gebrauch,
deren Fragmente ihre Daten vollständig in der Surplus Area transportieren.

Das OCS selbst ist eine gewöhnliche Internet-Prüfsumme mit derselben Einerkomplementarithmetik, die
\secref{subsec:udp-struktur} für die UDP-Prüfsumme beschreibt \cite{rfc1071}. Zwei Eigenheiten
unterscheiden die beiden. Erstens deckt das OCS genau den Bereich, den die UDP-Prüfsumme ausspart: die
gesamte Surplus Area einschließlich des Pads, wobei das OCS-Feld für die Berechnung als null gilt
\cite[Sec.~9 und 10]{rfc9868}. Zweitens geht zusätzlich die Länge der Surplus Area als vorzeichenloser
16-Bit-Summand in die Rechnung ein, obwohl dieser Wert an keiner Stelle des Pakets steht
\cite[Sec.~9]{rfc9868}. Das folgt demselben Gedanken wie der Pseudo-Header aus
\secref{subsec:udp-struktur}: Wie die UDP-Prüfsumme dort die \texttt{UDP Length} einbezieht, bezieht das
OCS die Surplus-Länge ein, und beide Seiten können diesen Summanden unabhängig voneinander aus den
Längenfeldern ableiten. Der Sender trägt das invertierte Ergebnis in das Feld ein; der Empfänger
summiert die Wörter der Surplus Area einschließlich des OCS-Felds sowie den Längensummanden und erwartet
wie in \secref{subsec:udp-struktur} lauter Einsen, also \texttt{0xFFFF} \cite[Sec.~9]{rfc9868}.

Diese Bauart hat einen praktischen Hintergrund: Manche fehlerhafte Middleboxes berechnen die
UDP-Prüfsumme über die gesamte IP-Nutzlast statt nur bis \texttt{UDP Length}. Weil das OCS über die
Surplus Area und deren Länge gebildet und anschließend invertiert wird, neutralisiert es den Beitrag der
fälschlich einbezogenen Surplus Area: Mit gesetztem OCS fällt die UDP-Prüfsumme in beiden Rechnungen
gleich aus, und solche Geräte stören sich nicht an dem Zusatzbereich \cite[Sec.~9]{rfc9868}. In erster
Linie dient das OCS ohnehin dem Erkennen zufälliger Fehler und anderweitiger, nicht standardkonformer
Nutzungen der Surplus Area; ein Schutz gegen gezielte Angriffe ist es ausdrücklich nicht
\cite[Sec.~9]{rfc9868}. Wie die UDP-Prüfsumme ist das OCS zudem bedingt abschaltbar: Der Wert
\texttt{0x0000} bedeutet ungenutzt, zulässig ist das nur, wenn auch die UDP-Prüfsumme null ist, und
Implementierungen müssen standardmäßig ein echtes OCS setzen \cite[Sec.~9]{rfc9868}. Schlägt die
Prüfung beim Empfänger fehl, gilt dieselbe Disposition wie beim Ausrichtungsfehler: Optionen ignorieren,
Surplus Area still verwerfen, gültig geprüfte Nutzdaten dennoch zustellen \cite[Sec.~9]{rfc9868}.

Die Optionen hinter dem OCS folgen schließlich dem TLV-Muster, dessen Syntax RFC~9868 bewusst an TCP
anlehnt (\secref{sec:erweiterungsmuster}): Ein 1~Byte großes Feld \texttt{Kind}, benannt nach dem
TCP-Vorbild, identifiziert die Option; das 1~Byte große Feld \texttt{Length} zählt die Gesamtlänge der
Option einschließlich \texttt{Kind} und \texttt{Length} selbst; dahinter steht der eigentliche Wert
\cite[Sec.~8 und 10]{rfc9868}. Für die Größe des Werts ist das nur scheinbar eine Grenze: Im
Standardformat fasst eine Option höchstens 254~Byte einschließlich \texttt{Kind} und \texttt{Length};
größere Optionen weichen auf ein erweitertes Format aus, in dem der Wert 255 im \texttt{Length}-Feld ein
zusätzliches 16-Bit-Längenfeld ankündigt \cite[Sec.~10]{rfc9868}. Eine eigene Obergrenze für den Wert
einer Option kennt RFC~9868 darüber hinaus bewusst nicht: Wie das Entwurfsprinzip der fehlenden
Längengrenze am Ende dieses Abschnitts festhält, begrenzt allein das UDP-Paket selbst den Optionsraum.
Auch die beiden Ein-Byte-Codes aus
\secref{sec:erweiterungsmuster} kehren als einzige Ausnahmen ohne Längenfeld wieder: \texttt{NOP}
(Kind 1) dient mit impliziter Länge eins der Ausrichtung zwischen Optionen und darf sich dafür
wiederholen, \texttt{EOL} (Kind 0) beendet die Liste vorzeitig; hinter \texttt{EOL} folgen bis zum Ende
der Surplus Area nur noch Nullbytes \cite[Sec.~8 und 10]{rfc9868}. Für die Anzahl der Optionen gilt
dasselbe Bild wie für ihre Länge: Eine feste Obergrenze gibt es nicht, die Liste endet erst am Ende der
Surplus Area. Begrenzend wirken stattdessen die Wiederholungsregeln: Außer FRAG, \texttt{NOP} und den
Experimentaloptionen soll jede Option höchstens einmal je Datagramm vorkommen, ein Empfänger wertet
ohnehin nur die erste Instanz, und ein mehrfaches FRAG macht die gesamte Optionsliste ungültig
\cite[Sec.~10]{rfc9868}. \texttt{NOP} wiederum soll höchstens siebenmal in Folge stehen, gerade genug
für eine Ausrichtung an 8-Byte-Grenzen, und nicht als Ersatz für \texttt{EOL} samt Nullfüllung dienen
\cite[Sec.~11.2]{rfc9868}. Variable Optionslängen und künftige Erweiterungen sind damit im Format selbst
angelegt.

Ein bewusst kleines Rechenbeispiel führt beide Bausteine zusammen: Eine Surplus Area von 6~Byte beginne
an gerader Grenze und enthalte hinter dem OCS zwei \texttt{NOP}, ein \texttt{EOL} und ein Nullbyte, als
16-Bit-Wörter gelesen also \texttt{0x0101} und \texttt{0x0000}. Der Sender addiert dazu den
Längensummanden \texttt{0x0006}, erhält \texttt{0x0107} und trägt das invertierte Ergebnis
\texttt{0xFEF8} als OCS ein. Der Empfänger summiert \texttt{0xFEF8}, \texttt{0x0101}, \texttt{0x0000}
und \texttt{0x0006} zu \texttt{0xFFFF}: Die Prüfung geht auf, obwohl die Länge selbst nie übertragen
wurde.

Da Empfänger, die UDP Options nicht unterstützen, ausschließlich die durch das UDP-Length-Feld begrenzten Nutzdaten
verarbeiten, bleibt die Erweiterung für Endsysteme typischerweise abwärtskompatibel; Ausnahmen bei Bestandssystemen
und das Verhalten von Zwischensystemen behandelt die Evaluation (\chapref{chap:evaluation}).

Wesentlich für die Einordnung ist, dass RFC~9868 kein neues Verhalten der Vermittlungsschicht einführt: Es
definiert weder neue IP-Header-Felder noch verändert es die Verarbeitung von IPv4. Für die IP-Schicht
sind die Bytes der \textit{Surplus Area} reine IP Transport Payload innerhalb der durch \texttt{IP Total Length}
begrenzten Datagramm-Größe; sie liegen lediglich hinter dem durch \texttt{UDP Length} markierten Ende der
UDP-Nutzdaten. RFC~9868 ist damit ein reines Transport-Feature, das ausschließlich die zuvor ungenutzte Differenz
zwischen \texttt{UDP Length} und der IP Transport Payload belegt \cite{rfc9868}.

Wie dieser Bereich genutzt werden darf, bindet RFC~9868 an sechs Entwurfsprinzipien, die ein gemeinsames Ziel
haben: UDP soll durch die Optionen seinen Charakter nicht verlieren \cite[Sec.~6]{rfc9868}:

\begin{itemize}
    \item \textbf{Zustandslos:} Nötiger Zustand gehört in die Anwendung oder in eine Bibliothek, die in ihrem
    Auftrag arbeitet. Die einzige, ausdrücklich begrenzte Ausnahme ist die Reassemblierung von Fragmenten.

    \item \textbf{Unidirektional:} Die UDP-Schicht beantwortet keine Option selbstständig; Antworten löst die
    Anwendung aus. Andernfalls ließe sich die Schicht mit gefälschter Absenderadresse als Reflektor
    missbrauchen, denn UDP prüft die Herkunft eines Datagramms nicht. Verfahren, die Bidirektionalität
    benötigen, gehören in ein eigenes Dokument.

    \item \textbf{Keine eigene Längengrenze:} Für Optionen gilt allein die Grenze des UDP-Pakets. Feste
    Grenzen werden erfahrungsgemäß überschritten; der RFC verweist auf die 40~Byte des TCP-Optionsraums und
    die 32-Bit-Adressen von IPv4. Grenzen darf eine Implementierung setzen, nicht die Spezifikation.

    \item \textbf{Kein Protokollersatz:} Optionen liefern Funktionen für den eigenen Verkehr einer Anwendung;
    sie sollen NTP, ICMP (insbesondere Echo) und Netzwerk-Testgeräte weder ersetzen noch nachbauen.

    \item \textbf{Rahmenwerk statt Protokoll:} RFC~9868 definiert Optionsformate auch dann, wenn sie noch
    kein vollständiges Verfahren ergeben; die Nutzung regeln andere Dokumente. Für REQ/RES übernimmt das
    RFC~9869 \cite{rfc9869}, für TIME bleibt sie bewusst offen. Das entspricht dem Muster von TCP, dessen
    Basisnorm den Optionsmechanismus festlegt, während einzelne Optionen in eigenen Dokumenten folgten.

    \item \textbf{Voreinstellung wie beim Altempfänger:} Empfangene Pakete werden standardmäßig auch dann
    zugestellt, wenn Prüfsummen-, Authentifizierungs- oder Entschlüsselungsprüfungen der Optionen scheitern;
    einzige Ausnahme sind UDP-Fragmente. Strengeres Verhalten muss die Anwendung ausdrücklich anfordern.
    Optionen sind ein Angebot, kein Filter.
\end{itemize}

Der gemeinsame Nenner der sechs Prinzipien: RFC~9868 legt nur die Optionsformate fest. UDP bleibt ein
minimales Transportprotokoll, und jede zusätzliche Fähigkeit wird von der Anwendung ausdrücklich aktiviert
\cite[Sec.~6]{rfc9868}.

An das letzte Prinzip knüpft die zentrale Zweiteilung der Optionen an, und sie folgt unmittelbar aus der
Abwärtskompatibilität: Weil Altempfänger jede Surplus Area stumm überlesen, muss jede Option damit
rechnen, ignoriert zu werden. \textit{SAFE}-Optionen (Kind 0 bis 191) sind genau dafür entworfen: Sie
lassen die Bedeutung der Nutzdaten unangetastet, ein Empfänger, der sie nicht versteht, ignoriert sie
folgenlos; die bisher erwähnten Optionen einschließlich FRAG gehören in diese Klasse
\cite[Sec.~10 und 11]{rfc9868}. \textit{UNSAFE}-Optionen (Kind 192 bis 255) verändern dagegen die
Nutzdaten selbst, etwa durch Kompression oder Verschlüsselung, und wären für einen Empfänger, der sie
nicht versteht, unbrauchbar. RFC~9868 löst das mit einer Transportbeschränkung: UNSAFE-Optionen dürfen
nur in UDP-Fragmenten auftreten, deren Nutzdatenteil leer ist und deren Inhalt in der FRAG-Option der
Surplus Area steckt. Ein Altempfänger sieht dann nur ein leeres Datagramm und verwirft die Daten still,
statt sie falsch zu deuten; und auch ein optionsfähiger Empfänger, der eine UNSAFE-Option nicht
unterstützt, muss die reassemblierten Nutzdaten still verwerfen \cite[Sec.~10 und 12]{rfc9868}. Die
einzelnen Optionen und ihre Pflichten behandelt \chapref{chap:analyse}.

Zwei Folgen dieser Prinzipien verdienen eine eigene Erwähnung. Erstens umspannt ein user datagram künftig
gegebenenfalls mehrere IP-Datagramme, wenn es per FRAG-Option fragmentiert wird. Zweitens opfert der RFC an
genau einer Stelle bewusst Kompatibilität: Ein Altempfänger sieht jedes UDP-Fragment als leeres Datagramm,
ein optionsfähiger Empfänger das reassemblierte Datagramm mit Inhalt. Diese Zählungen gleicht der RFC nicht
an, weil leere Datagramme als Lebenszeichen ohnehin unzuverlässig sind \cite[Sec.~6]{rfc9868}.

\section{Betriebssystem und Netzpfad}
\label{sec:netzpfad}

Ob ein Datagramm mit Surplus Area sein Ziel erreicht, entscheidet sich an zwei Stellen außerhalb des
Protokolls: im Betriebssystem der beiden Endpunkte und auf dem Netzpfad dazwischen. Dieser Abschnitt stellt
zunächst die zwei Socket-Sichten vor, mit denen ein Betriebssystem UDP-Verkehr an Anwendungen übergibt, und
anschließend die Akteure des Pfades; am Ende fügt \figref{fig:akteurskette} beides zu einem Gesamtbild
zusammen. Allgemeine Netzwerkgrundlagen bleiben bewusst ausgespart: Es geht genau um die Kette, die die
Messpfade dieser Arbeit tatsächlich durchlaufen.

\paragraph{Zwei Socket-Sichten.}
Der gewöhnliche Zugang zu UDP ist der \textit{UDP-Socket}. Bei ihm übernimmt der Kernel die gesamte
Protokollarbeit: Beim Empfang prüft er Länge und Prüfsumme, ordnet das Datagramm über den Zielport der
richtigen Anwendung zu und übergibt ihr genau die Nutzdaten bis zur Grenze von \texttt{UDP Length}
\cite{stephan2024packetpath}. Eine Surplus Area bekommt die Anwendung nie zu sehen; ein Empfänger, der nur
diese Sicht nutzt, ist damit genau der Altempfänger aus \secref{sec:surplus-area}. Beim Senden gilt das
Gegenstück: Der Kernel bemisst das IP-Datagramm stets exakt auf das UDP-Datagramm, hinter den Nutzdaten
bleibt kein Platz. Ein UDP-Socket kann eine Surplus Area also weder erzeugen noch lesen. Wie dieser
Normalweg im Kernel aussieht, zeigt \figref{fig:udp-kernelpfad} mit den wichtigsten Funktionen je Schicht
für beide Richtungen:

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{02_udp_kernelpfad.pdf}
  \caption{Sende- und Empfangsweg eines UDP-Datagramms durch den Linux-Kernel (aus
    \cite{stephan2024packetpath})}
  \label{fig:udp-kernelpfad}
\end{figure}

Aus \figref{fig:udp-kernelpfad} geht hervor, dass jede Schicht des Sendewegs ihr Gegenstück im
Empfangsweg hat: \texttt{udp\_sendmsg()} baut den UDP-Header und bemisst die Längen,
\texttt{udp\_recvmsg()} liefert erst aus, nachdem der Kernel die UDP-Prüfsumme validiert hat
\cite{stephan2024packetpath}. Für diese Arbeit entscheidend ist, wo die zweite Sicht ansetzt: Der
Raw-Sender speist sein fertig gebautes Datagramm eine Schicht tiefer ein, auf Höhe der IP-Schicht, und
der Raw-Empfänger erhält seine Kopie, bevor der UDP-Pfad beginnt. Beide Abzweige zeichnen die folgenden
Abbildungen nach.

Wer beides will, braucht die zweite Sicht: den \textit{Raw Socket}. Er arbeitet unterhalb von UDP direkt
mit IP-Datagrammen, und beim Senden erlaubt er mit der Socket-Option \texttt{IP\_HDRINCL}, das IP-Datagramm
mitsamt IP-Header selbst zu bauen \cite{man7raw}; erst damit lässt sich eine \texttt{Total Length} wählen,
die über das Ende des UDP-Datagramms hinausreicht. Die vertauschten Rollen beider Sendewege zeigt
\figref{fig:sendepfade}:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      station/.style={feld, minimum width=4.7cm, minimum height=1.05cm, align=center, font=\scriptsize},
      akzentbox/.style={station, feldakzent},
      fluss/.style={->, draw=feldlinie, thin, rounded corners=3pt},
    ]
    \node[station] (ua) at (2.6,0) {Anwendung\\{\tiny übergibt nur die Nutzdaten}};
    \node[akzentbox] (ra) at (9.0,0)
      {Anwendung\\{\tiny baut IP-Header, UDP-Header,}\\{\tiny Nutzdaten und Surplus Area}};
    \node[akzentbox] (us) at (2.6,-1.85) {UDP-Socket\\{\tiny Kernel baut beide Header,}\\{\tiny Längen bündig}};
    \node[station] (rs) at (9.0,-1.85) {Raw Socket mit \texttt{IP\_HDRINCL}\\{\tiny Kernel übernimmt das Datagramm}};
    \node[station] (ip) at (5.8,-3.55) {IP-Schicht und Netzwerkkarte\\{\tiny Routing, netfilter, Draht}};
    \draw[fluss] (ua) -- (us);
    \draw[fluss] (ra) -- (rs);
    \draw[fluss] (us.south) |- (ip.west);
    \draw[fluss] (rs.south) |- (ip.east);
  \end{tikzpicture}
  \caption{Die zwei Sendepfade durch den Linux-Kernel \cite{stephan2024packetpath,man7raw}}
  \label{fig:sendepfade}
\end{figure}

Aus \figref{fig:sendepfade} geht hervor, dass die Arbeitsteilung kippt: Am UDP-Socket baut der Kernel beide
Header und bemisst die Längen bündig, am Raw Socket liefert die Anwendung das fertige Datagramm ab, und nur
auf diesem Weg entsteht hinter den Nutzdaten Platz für eine Surplus Area. Der Unterbau bleibt derselbe:
Beide Wege münden in die IP-Schicht mit ihren netfilter-Stationen und enden an der Netzwerkkarte
\cite{stephan2024packetpath}.

Ganz entlässt der Kernel den Raw-Sender allerdings nicht aus seiner Obhut, und diese Restarbeit prägt den
Sendepfad der Implementierung. Routing, ARP und Link-Schicht bleiben Kernelsache, und im übergebenen
IP-Header trägt der Kernel \texttt{Total Length} und die IP-Header-Prüfsumme stets selbst ein; UDP-Header
und Surplus Area lässt er dagegen unangetastet \cite{man7raw}. Zugleich fragmentiert dieser Pfad nicht:
Mit \texttt{IP\_HDRINCL} ist ein Datagramm auf die MTU des Interfaces begrenzt, ein größerer Sendeaufruf
schlägt fehl \cite{man7raw}. Für RFC~9868 ist das kein Verlust: IP-Fragmentierung sollen UDP-Anwendungen
ohnehin vermeiden (\secref{sec:ipv4}) \cite[Sec.~3.2]{rfc8085}, und große Nutzlasten zerlegt stattdessen
die FRAG-Option auf Transportebene.

Beim Empfang entscheidet sich, an welcher Stelle ein Datagramm geprüft wird; den Weg vom Draht zu den
beiden Sichten zeichnet \figref{fig:empfangspfad} nach:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      station/.style={feld, minimum width=3.05cm, minimum height=1.05cm, align=center, font=\scriptsize},
      akzentbox/.style={station, feldakzent},
      fluss/.style={->, draw=feldlinie, thin, rounded corners=3pt},
    ]
    \node[station] (nic) at (0,0) {Netzwerkkarte\\{\tiny Empfang vom Draht}};
    \node[station] (ips) at (3.55,0) {IP-Schicht\\{\tiny prüft den IP-Header}};
    \node[station] (nf) at (7.1,0) {netfilter\\{\tiny lokaler Paketfilter}};
    \node[station] (udp) at (10.65,0) {UDP-Pfad\\{\tiny prüft Länge, Prüfsumme, Port}};
    \node[akzentbox] (raw) at (7.1,-2.2)
      {Raw Socket\\{\tiny Kopie des ganzen Datagramms}\\{\tiny samt Surplus Area, ungeprüft}};
    \node[station] (usock) at (10.65,-2.2) {UDP-Socket\\{\tiny Nutzdaten bis \texttt{UDP Length}}};
    \draw[fluss] (nic) -- (ips);
    \draw[fluss] (ips) -- (nf);
    \draw[fluss] (nf) -- (udp);
    \draw[fluss] (nf.south) -- node[left, font=\tiny, text=feldlinie] {Kopie} (raw.north);
    \draw[fluss] (udp) -- (usock);
  \end{tikzpicture}
  \caption{Der Empfangspfad durch den Linux-Kernel \cite{stephan2024packetpath,man7raw}}
  \label{fig:empfangspfad}
\end{figure}

Aus \figref{fig:empfangspfad} geht hervor, dass sich der Weg hinter dem Paketfilter gabelt: Der Raw Socket
erhält eine Kopie des gesamten IP-Datagramms samt Surplus Area, und zwar bevor der UDP-Pfad des Kernels
Länge, Prüfsumme und Portzuordnung geprüft hat \cite{man7raw,stephan2024packetpath}. Ungeprüft heißt dabei
vollständig ungeprüft: Auch ein Datagramm mit falscher UDP-Prüfsumme oder einer \texttt{UDP Length}, die
über die IP-Nutzlast hinausweist, erreicht den Raw Socket unverändert. Die Prüfungen, die der Kernel dem
UDP-Socket abnimmt, muss eine Raw-Empfangsschicht deshalb vollständig selbst ausführen, und zwar in der
Reihenfolge, die RFC~9868 vorgibt: erst Mindestlänge und Längenkonsistenz des UDP-Headers, dann die
UDP-Prüfsumme, erst danach Surplus-Ortung und OCS \cite[Sec.~10 und 14]{rfc9868}; wie die Implementierung
das umsetzt, zeigt \chapref{chap:entwurf}. Zwei Arbeiten behält der Kernel dagegen auch hier. Die
netfilter-Hooks bleiben beiden Sichten vorgeschaltet: Ein lokaler Paketfilter kann ein Datagramm
verwerfen, bevor irgendeine Anwendung es sieht \cite{stephan2024packetpath}. Und ankommende IP-Fragmente
setzt die IP-Schicht bereits vor der Zustellung wieder zusammen, auch für Raw Sockets; eine
Raw-Empfangsschicht muss die IPv4-Reassemblierung deshalb nicht nachbauen
\cite{man7raw,stephan2024packetpath}.

\paragraph{Akteure auf dem Netzpfad.}
Zwischen den Endpunkten begegnet das Datagramm einer zweiten Gruppe von Akteuren. Der Sammelbegriff dafür
lautet \textit{Middlebox}: ein Zwischensystem, das Pakete nicht nur weiterleitet, sondern liest, verändert
oder verwirft \cite[Sec.~3.5]{rfc8085}. Für die Interpretation der späteren Messungen genügt der
Sammelbegriff nicht, denn die Akteure setzen an verschiedenen Stellen an und hinterlassen verschiedene
Spuren. Im Zugangsnetz ersetzt die NAT eines Heimrouters oder Hotspots Adressen und Ports und bindet
jeden Fluss an einen Zustandseintrag mit Zeitschranke; ihre Provider-Variante \textit{Carrier-Grade NAT}
(CGNAT) teilt dieselben öffentlichen Adressen zusätzlich unter vielen Kunden auf. Firewalls verwerfen an
Netzgrenzen und Endsystemen nach Regelwerk, etwa anhand von Ports und Protokollen. Leicht zu übersehen
sind die letzten beiden Akteure: die Virtualisierungsschicht eines Testrechners, deren eigener Netz- und
NAT-Stack Kopffelder auf erwartete Normwerte umschreiben kann, und das Transitnetz selbst, die AS-Kette
zwischen den Zugangsnetzen, in der einzelne Netze filtern.

Keiner dieser Akteure kennt UDP Options: Was mit einer Surplus Area geschieht, ist stets Nebenwirkung von
Regeln, die für gewöhnlichen UDP-Verkehr geschrieben wurden. Wie verschieden diese Nebenwirkungen
ausfallen, zeigen drei Befunde, die \chapref{chap:evaluation} belegt: Ein Mobilfunkzugang verwirft
Datagramme mit Surplus Area klassenbasiert, ein VMware-NAT normalisiert die Kopffelder und entfernt die
Surplus Area dabei, und der Transitpfad zu Hetzner transportiert sie unverändert. Zwei Eigenschaften
prägen darüber hinaus die Messungen. Erstens arbeiten mehrere Akteure mit Zustand und Zeitschranken;
derselbe Pfad kann sich je nach Vorgeschichte unterschiedlich verhalten \cite[Sec.~3.5]{rfc8085}.
Zweitens lässt sich die Kette gezielt umgehen: Eine Kapselung, etwa in einen WireGuard-Tunnel, entzieht
das innere Datagramm allen genannten Akteuren; \chapref{chap:evaluation} nutzt genau das als Gegenprobe.

Diese Kette erklärt zugleich, warum RFC~9868 seine Optionen ausgerechnet in einem Bereich ablegt, den
Altempfänger nie lesen. Weil Middleboxes bevorzugt durchlassen, was sie kennen, kann sich ein Protokoll
praktisch nur dort weiterentwickeln, wo seine Neuerungen für sie unsichtbar bleiben; dieses Erstarren des
Netzes unter seinen Zwischensystemen wird als \textit{Ossifikation} bezeichnet. RFC~9868 rechnet
ausdrücklich mit solchen Eingriffen: Fehlerhaft rechnende Middleboxes begründen Details der OCS-Berechnung
(\secref{sec:surplus-area}) \cite[Sec.~9]{rfc9868}, und UDP-Relays können eine Surplus Area beim Weiterleiten vollständig abstreifen
\cite[Sec.~25.6]{rfc9868}.

Alle Stationen zusammen zeigt \figref{fig:akteurskette}: der Lebenslauf eines Datagramms von der sendenden
Anwendung bis zu dem Raw-Empfänger, der die Surplus Area auswertet.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      station/.style={feld, minimum width=3.05cm, minimum height=1.05cm, align=center, font=\scriptsize},
      fremd/.style={station, feldwarn},
      fluss/.style={->, draw=feldlinie, thin, rounded corners=3pt},
    ]
    \node[station] (anw)   at (0,0)        {Anwendung\\{\tiny übergibt Nutzdaten}};
    \node[station] (sock)  at (3.55,0)     {Socket-Schicht\\{\tiny UDP- oder Raw-Pfad, netfilter}};
    \node[fremd]   (virt)  at (7.1,0)      {Virtualisierung\\{\tiny VM-NAT, venet}};
    \node[fremd]   (zug)   at (10.65,0)    {Zugangsnetz\\{\tiny NAT, CGNAT}};
    \node[fremd]   (trans) at (0,-2.2)     {Transitnetz\\{\tiny AS-Kette}};
    \node[fremd]   (ingr)  at (3.55,-2.2)  {Ingress-Filter\\{\tiny Firewall des Ziels}};
    \node[station] (kern)  at (7.1,-2.2)   {Empfänger-Kernel\\{\tiny netfilter vor Zustellung}};
    \node[station] (raw)   at (10.65,-2.2) {Raw-Empfänger\\{\tiny liest die Surplus Area}};
    \draw[fluss] (anw) -- (sock);
    \draw[fluss] (sock) -- (virt);
    \draw[fluss] (virt) -- (zug);
    \draw[fluss] (zug.south) -- (10.65,-1.1) -- (0,-1.1) -- (trans.north);
    \draw[fluss] (trans) -- (ingr);
    \draw[fluss] (ingr) -- (kern);
    \draw[fluss] (kern) -- (raw);
  \end{tikzpicture}
  \caption{Der Lebenslauf eines Datagramms}
  \label{fig:akteurskette}
\end{figure}

Aus \figref{fig:akteurskette} geht hervor, dass zwischen Socket-Schicht und Empfänger-Kernel vier
Stationen liegen, die das Datagramm außerhalb der Endpunkte berühren: Sie greifen teils mit Zustand ein und
sind von außen nicht direkt beobachtbar. Für die Feldmessungen folgt daraus die Interpretationsregel dieser
Arbeit: Geht ein optionsbehaftetes Datagramm verloren oder kommt es verändert an, ist der Befund zuerst
einer Station dieser Kette zuzuordnen, bevor er der Implementierung oder dem Standard angelastet wird;
diese Zuordnung leisten in \chapref{chap:evaluation} Messpunkte an beiden Enden des Pfades.

Bezogen auf diese Arbeit hat der Blick auf das Werkzeugumfeld eine letzte praktische Folge. Die
verbreiteten Mitschnittwerkzeuge Wireshark und tshark kennen RFC~9868 in Version~4.6 nicht: Ein Mitschnitt
zeigt eine Surplus Area kommentarlos als unbenannte Bytes hinter den Nutzdaten, ohne eigenes Feld und ohne
Warnung. Für die Messkampagnen ist deshalb ein eigener Prüfer für aufgezeichnete Pakete unverzichtbar
(\chapref{chap:evaluation}); zugleich zeigt der fehlende Dissektor, wie wenig verbreitet UDP Options im
Ökosystem bislang sind.

Damit sind die Grundlagen beisammen: die beteiligten Protokolle, die Erweiterungsmuster, die Surplus Area
mit ihren Entwurfsprinzipien sowie Betriebssystem und Netzpfad, auf denen jede Messung aufsetzt. Bevor die
Arbeit mit ihnen Implementierung und Messungen bewertet, legt \chapref{chap:methodik} die Arbeitsweise
offen, mit der diese Ergebnisse entstanden sind. Denn für alle folgenden Kapitel gilt derselbe Grundsatz:
Eine Aussage eines Sprachmodells ist zunächst nur ein Kandidat; ob sie gilt, entscheiden RFC, Code und
Messung.
