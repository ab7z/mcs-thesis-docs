% =============================================================================
% Kapitel 2: Grundlagen
% =============================================================================

\chapter{Grundlagen}
\label{chap:grundlagen}

IPv4 und UDP sind die beteiligten Protokolle (\secref{sec:ipv4}, \secref{sec:udp}), gefolgt von den
Erweiterungsmustern (\secref{sec:erweiterungsmuster}), der Surplus Area (\secref{sec:surplus-area}) sowie
Betriebssystem und Netzpfad (\secref{sec:netzpfad}).

\section{IPv4}
\label{sec:ipv4}

Das Internet Protocol (IP) vermittelt Datagramme zwischen Endsystemen und bildet die Vermittlungsschicht
für UDP \cite{rfc791}. Für RFC~9868 ist es nötig, weil erst \texttt{UDP Length} und das durch den IP-Header
bestimmte Datagrammende Lage und Größe der Surplus Area festlegen \cite[Sec.~7]{rfc9868}.

Diese Arbeit beschränkt sich auf IPv4. Bei IPv4 bestimmen \texttt{IHL} und \texttt{Total Length} die
IP-Nutzlast; bei IPv6 ergeben sich ihre Grenzen aus dem Basis-Header und der Kette der Extension Header
\cite[Sec.~7]{rfc9868}\cite{rfc8200}.
Auch Randwerte unterscheiden sich: Endpunkte müssen mindestens zwei Fragmente zerlegen und wieder
zusammensetzen können, von denen jedes in die Ethernet-MTU von 1\,500~Byte passt
\cite[Sec.~11.4]{rfc9868}. Die zugehörige lokale MRDS-size beträgt mindestens 2\,926~Byte bei IPv4 und
2\,886~Byte bei IPv6 \cite[Sec.~11.6]{rfc9868}; bei IPv6-Jumbograms mit \texttt{UDP Length} null sind UDP
Options nicht nutzbar \cite[Sec.~22]{rfc9868}. Die Grundstruktur des Mechanismus bleibt gleich,
Implementierung und Evaluation betrachten jedoch nur IPv4 (\secref{sec:abgrenzung}).

Der Abschnitt behandelt deshalb nur die für RFC~9868 und die Messung nötigen IPv4-Felder: \texttt{IHL},
\texttt{Total Length}, \texttt{Protocol}, Adressen, Header-Prüfsumme, Fragmentierungsfelder und
\texttt{Time to Live}.

Den Ausgangspunkt bildet der Header; seinen Aufbau zeigt \figref{fig:ipv4-header} \cite{rfc791}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=0.365cm, y=1cm]
    \node[bitindex, anchor=south west] at (0,0.06) {0};
    \node[bitindex, anchor=south] at (8,0.06) {8};
    \node[bitindex, anchor=south] at (16,0.06) {16};
    \node[bitindex, anchor=south] at (24,0.06) {24};
    \node[bitindex, anchor=south east] at (32,0.06) {31};
    \draw[feld] (0,-0.72) rectangle (4,0);
    \node[feldtext] at (2,-0.36) {Version};
    \draw[feldakzent] (4,-0.72) rectangle (8,0);
    \node[feldtext] at (6,-0.36) {IHL};
    \draw[feld] (8,-0.72) rectangle (16,0);
    \node[feldtext] at (12,-0.36) {Type of Service};
    \draw[feldakzent] (16,-0.72) rectangle (32,0);
    \node[feldtext] at (24,-0.36) {Total Length};
    \draw[feld] (0,-1.54) rectangle (16,-0.82);
    \node[feldtext] at (8,-1.18) {Identification};
    \draw[feld] (16,-1.54) rectangle (19,-0.82);
    \node[feldtext] at (17.5,-1.18) {Flags};
    \draw[feld] (19,-1.54) rectangle (32,-0.82);
    \node[feldtext] at (25.5,-1.18) {Fragment Offset};
    \draw[feld] (0,-2.36) rectangle (8,-1.64);
    \node[feldtext] at (4,-2.00) {Time to Live};
    \draw[feld] (8,-2.36) rectangle (16,-1.64);
    \node[feldtext] at (12,-2.00) {Protocol};
    \draw[feld] (16,-2.36) rectangle (32,-1.64);
    \node[feldtext] at (24,-2.00) {Header Checksum};
    \draw[feld] (0,-3.18) rectangle (32,-2.46);
    \node[feldtext] at (16,-2.82) {Source Address};
    \draw[feld] (0,-4.00) rectangle (32,-3.28);
    \node[feldtext] at (16,-3.64) {Destination Address};
    \draw[feldoptional] (0,-4.82) rectangle (24,-4.10);
    \node[feldtext] at (12,-4.46) {IP-Optionen (falls vorhanden)};
    \draw[feldoptional] (24,-4.82) rectangle (32,-4.10);
    \node[feldtext] at (28,-4.46) {Padding};
    \draw[klammer] (32.35,0) -- (32.35,-4.00)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {fester Header\\20~Byte};
    \draw[klammer] (32.35,-4.10) -- (32.35,-4.82)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {$(\textit{IHL}-5) \times 4$~Byte};
  \end{tikzpicture}
  \caption{Der IPv4-Header nach RFC~791 \cite{rfc791}. Hervorgehoben sind die beiden Längenangaben, auf denen
    die Bestimmung der Surplus Area beruht; gestrichelte Felder sind nur vorhanden, wenn \texttt{IHL} über dem
    Minimalwert~5 liegt.}
  \label{fig:ipv4-header}
\end{figure}

Im Zentrum stehen die beiden Längenangaben der ersten Zeile. \texttt{Total Length} gibt die Gesamtlänge des
Datagramms in Byte an, Header und Nutzlast zusammen; als 16-Bit-Feld erlaubt es höchstens 65\,535~Byte.

Die zweite Längenangabe, die \texttt{IHL} (Internet Header Length), beziffert die Länge des Headers. Sie zählt
dabei nicht in Byte, sondern in 32-Bit-Wörtern zu je 4~Byte. Der Grund liegt in der Breite des Feldes: Mit
seinen 4~Bit kann es höchstens den Wert~15 darstellen. In Byte gezählt würde das nicht einmal für den festen
Headeranteil von 20~Byte reichen; in 32-Bit-Wörtern gezählt beschreibt derselbe Wertebereich dagegen Header bis
60~Byte \cite{rfc791}. Die Headerlänge ergibt sich daher als
\begin{equation}
  \textit{Headerlänge} = \textit{IHL} \times 4~\mbox{Byte}.
  \label{eq:ihl-headerlaenge}
\end{equation}
Der Minimalwert~5 entspricht dem optionslosen 20-Byte-Header, der Maximalwert~15 einem Header von 60~Byte.
Eine IPv4-Headerlänge ist damit stets ein Vielfaches von~4; diese unscheinbare Eigenschaft kehrt bei der
Ausrichtung der UDP Options wieder.

Zwischen festem Header und Nutzlast können zudem IP-Optionen liegen. Sie stellen Steuerfunktionen für
Sonderfälle bereit, die die gewöhnliche Kommunikation nicht braucht, und dienen als zusätzliche Felder zur
Erweiterung der IP-Header \cite{rfc791}: \texttt{Record Route} etwa sammelt die Adressen der durchlaufenen
Router ein, \texttt{Internet Timestamp} deren Zeitstempel. In der Praxis werden solche Optionen kaum genutzt.
Wie ein Router optionstragende Pakete behandelt, hängt von seiner Architektur und Konfiguration ab: Er kann
sie mit Leitungsgeschwindigkeit weiterleiten oder filtern oder die Optionen im allgemeinen Prozessor oder in
Spezialhardware verarbeiten; vor allem die Verarbeitung im allgemeinen Prozessor birgt bei großen
Paketmengen ein Überlastrisiko. Zugleich ist das Verwerfen optionstragender Pakete verbreitete Praxis
\cite[Sec.~1 und 3.1]{rfc7126}. Zudem fasst der 40-Byte-Optionsraum bei \texttt{Record Route} höchstens neun
Adressen und damit viele reale Pfade nicht \cite{rfc791}. Abschließend gilt, dass erst die \texttt{IHL} und
keine Konstante den Beginn der Nutzlast festlegt.

Aus den beiden Längenangaben \texttt{Total Length} und \texttt{IHL} ergibt sich die Größe, auf die sich
RFC~9868 durchgängig bezieht: Die Nutzlast eines IPv4-Datagramms beginnt nach \texttt{IHL} $\times$ 4 Byte
und endet bei \texttt{Total Length}. Für ein
UDP-Datagramm fordert RFC~9868 deshalb \cite[Sec.~7]{rfc9868}\footnote{In Ausnahmefällen liegen zwischen
IPv4- und UDP-Header noch Shim-Header, etwa bei IPsec oder IPComp; das Feld \texttt{Protocol} zeigt dann
nicht UDP an, und die Obergrenze verringert sich zusätzlich um die Gesamtlänge dieser Shim-Header
\cite[Sec.~7]{rfc9868}. Diese Arbeit betrachtet solche Ketten nicht weiter.}
\begin{equation}
  \textit{UDP Length} \leq \textit{Total Length} - \textit{Headerlänge}.
  \label{eq:udp-length-obergrenze}
\end{equation}
Die rechte Seite der Ungleichung ist die Länge dieser Nutzlast, also der Platz hinter dem IP-Header.
Die linke Seite ist die Länge, die das UDP-Datagramm für sich beansprucht. Die Bedingung verlangt damit, dass
das UDP-Datagramm vollständig in die Nutzlast des IP-Datagramms passt; UDP darf nie mehr Platz beanspruchen, als
das IP-Datagramm hergibt. Bei Gleichheit füllt das UDP-Datagramm die Nutzlast exakt aus; so arbeiten
klassische Sender. Bleibt \texttt{UDP Length} dagegen zurück, ist die Differenz genau der Raum, den
\secref{sec:surplus-area} als Surplus Area einführt.

Von den übrigen Feldern braucht die Arbeit nur wenige. \texttt{Protocol} benennt das Transportprotokoll der
Nutzlast; der Wert~17 steht für UDP. Die \texttt{Header Checksum} sichert ausschließlich den Header, nicht die
Nutzlast; deren Fehlererkennung bleibt der UDP-Prüfsumme überlassen (\secref{subsec:udp-struktur}). Quell- und
Zieladresse geben Absender und Empfänger des Datagramms an. Sie werden später noch einmal wichtig: Die
UDP-Prüfsumme bezieht beide Adressen über einen sogenannten Pseudo-Header in ihre Berechnung ein.
\texttt{Version} und \texttt{Type of Service} berühren den Optionsmechanismus nicht und werden nicht weiter
vertieft.

Auch \texttt{Time to Live} spielt für die Optionen keine Rolle, kehrt in der Evaluation aber als Messgröße
wieder. Jede weiterleitende Instanz verringert den Wert um mindestens eins und verwirft das Datagramm bei
null \cite{rfc791}. Die Differenz zwischen gesendetem und empfangenem Wert entspricht deshalb nur unter der
Annahme eines Dekrements von genau eins je Weiterleitung der Zahl der durchlaufenen IPv4-Router;
\chapref{chap:evaluation} nutzt sie unter dieser Annahme, um die Stabilität der Messpfade zu prüfen. Die
zweite Zeile des Headers schließlich (\texttt{Identification}, \texttt{Flags}, \texttt{Fragment Offset})
gehört vollständig zur Fragmentierung, der sich der Rest dieses Abschnitts widmet.

Wie groß ein Datagramm praktisch werden kann, entscheidet allerdings nicht das 16-Bit-Längenfeld, sondern der
Übertragungsweg. Jede Technik der Sicherungsschicht begrenzt die Nutzlast eines einzelnen Rahmens; diese
Obergrenze heißt Maximum Transmission Unit (MTU) und liegt für Ethernet üblicherweise bei 1500~Byte. Streng
genommen ist die MTU also eine Eigenschaft der Sicherungsschicht. Ihre Folgen trägt jedoch die
Vermittlungsschicht, denn ein Datagramm durchquert auf dem Weg zum Ziel mehrere Netze mit möglicherweise
unterschiedlichen MTUs, und IP muss festlegen, was mit einem Datagramm geschieht, das nicht in den nächsten
Rahmen passt \cite{rfc791}.

IPv4 beantwortet das mit Fragmentierung. Ist ein Datagramm größer als die MTU und das Flag \texttt{DF} (Don't
Fragment) nicht gesetzt, zerlegt die IP-Schicht es in mehrere Fragmente. Das übernimmt nicht nur der
Absender: Bei IPv4 darf auch jeder Router unterwegs ein Datagramm zerlegen, das nicht in sein nächstes Netz
passt \cite{rfc791}. Jedes Fragment erhält einen eigenen IP-Header, übernimmt die \texttt{Identification} des
Originals, trägt in \texttt{Fragment Offset} seine Position in Einheiten von 8~Byte und setzt das Flag
\texttt{MF} (More Fragments), das nur beim letzten Fragment fehlt. Der Empfänger sammelt zusammengehörige
Fragmente anhand von Quelladresse, Zieladresse, Protokoll und
\texttt{Identification} und setzt das Datagramm vor der Übergabe an die Transportschicht wieder zusammen
\cite{rfc791}. Ist \texttt{DF} dagegen gesetzt, verwirft ein Router das zu große Datagramm und meldet dies dem
Absender per ICMP; auf dieser Rückmeldung baut die Path MTU Discovery auf, mit der ein Sender die kleinste MTU
seines Pfades ermittelt \cite[Sec.~3.2]{rfc8085}.

Für diese Arbeit ist an der Fragmentierung vor allem eine Konsequenz bedeutsam: Der UDP-Header gehört aus Sicht
von IP zur Nutzlast und landet deshalb vollständig im ersten Fragment. Was das für die übrigen Fragmente
bedeutet, zeigt \figref{fig:ip-fragmentierung} am Beispiel eines in drei Teile zerlegten Datagramms.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \node[font=\tiny, text=feldlinie, anchor=south west] at (0,0.06) {Original-Datagramm};
    \draw[feld] (0,-0.72) rectangle (1.7,0);
    \node[feldtext] at (0.85,-0.36) {IP-Header};
    \draw[feldakzent] (1.7,-0.72) rectangle (3.4,0);
    \node[feldtext] at (2.55,-0.36) {UDP-Header};
    \draw[feld] (3.4,-0.72) rectangle (10.4,0);
    \node[feldtext] at (6.9,-0.36) {UDP-Nutzdaten};
    \draw[feldlinie, thin, dashed] (5.4,-0.72) -- (5.4,-2.70);
    \draw[feldlinie, thin, dashed] (8.0,-0.72) -- (8.0,-3.85);
    \node[font=\tiny, text=feldlinie, anchor=south west] at (0,-1.53) {Fragment 1};
    \draw[feld] (0,-2.27) rectangle (1.7,-1.55);
    \node[feldtext] at (0.85,-1.91) {IP-Header};
    \draw[feldakzent] (1.7,-2.27) rectangle (3.4,-1.55);
    \node[feldtext] at (2.55,-1.91) {UDP-Header};
    \draw[feld] (3.4,-2.27) rectangle (5.4,-1.55);
    \node[feldtext] at (4.4,-1.91) {Teil 1};
    \node[font=\tiny, text=feldlinie, anchor=south west] at (3.7,-2.68) {Fragment 2};
    \draw[feld] (3.7,-3.42) rectangle (5.4,-2.70);
    \node[feldtext] at (4.55,-3.06) {IP-Header};
    \draw[feld] (5.4,-3.42) rectangle (8.0,-2.70);
    \node[feldtext] at (6.7,-3.06) {Teil 2};
    \node[font=\scriptsize, text=warnton, anchor=west, align=left] at (8.25,-3.06) {keine\\Portnummern};
    \node[font=\tiny, text=feldlinie, anchor=south west] at (6.3,-3.83) {Fragment 3};
    \draw[feld] (6.3,-4.57) rectangle (8.0,-3.85);
    \node[feldtext] at (7.15,-4.21) {IP-Header};
    \draw[feld] (8.0,-4.57) rectangle (10.4,-3.85);
    \node[feldtext] at (9.2,-4.21) {Teil 3};
    \node[font=\scriptsize, text=warnton, anchor=west, align=left] at (10.65,-4.21) {keine\\Portnummern};
  \end{tikzpicture}
  \caption{Schematische Zerlegung eines UDP-Datagramms in drei IPv4-Fragmente}
  \label{fig:ip-fragmentierung}
\end{figure}

Aus \figref{fig:ip-fragmentierung} geht hervor, dass nur das erste Fragment den UDP-Header enthält: Alle
Folgefragmente tragen keine Portnummern. Zwischensysteme wie NATs und Firewalls, die Pakete anhand
vollständiger Header zuordnen oder prüfen, können solche Fragmente nicht einordnen und verwerfen sie teils
pauschal; zudem macht der Verlust eines einzelnen Fragments das gesamte Datagramm unbrauchbar. RFC~8085 wertet
beides als grundsätzliches Problem der IP-Fragmentierung, das jedes Transportprotokoll trifft, und rät
UDP-Anwendungen deshalb, IP-Fragmentierung zu vermeiden \cite[Sec.~3.2]{rfc8085}. Dass der Rat gerade an UDP
geht, hat einen Grund: Während TCP seinen Datenstrom selbst in passende Segmente zerlegt und der
IP-Fragmentierung so meist entgeht \cite{rfc9293}, erzeugt UDP je Nachricht genau ein Datagramm
(\secref{subsec:udp-eigenschaften}); ein zu großes Datagramm kann bislang nur die IP-Schicht zerteilen. Genau
diese Lücke schließt die FRAG-Option von RFC~9868: Sie verlagert die Fragmentierung auf die Transportschicht
und wiederholt die Portnummern in jedem Fragment \cite[Sec.~11.4]{rfc9868}; den Verlust einzelner Fragmente
behebt allerdings auch FRAG nicht. Darauf kommt \chapref{chap:evaluation} zurück.

Der folgende Abschnitt wendet sich dem Protokoll zu, das RFC~9868 tatsächlich erweitert: UDP.

\section{Das User Datagram Protocol (UDP)}
\label{sec:udp}

Das User Datagram Protocol (UDP) ist ein verbindungsloses, nachrichtenorientiertes Transportprotokoll. Es
arbeitet ohne vorherigen Handshake und bietet selbst keine Garantie für Zustellung, Reihenfolge oder
Duplikaterkennung \cite{rfc768}. Zuverlässigkeit und Staukontrolle muss ein nutzendes Protokoll bei Bedarf
oberhalb von UDP bereitstellen \cite[Sec.~3.1 bis 3.3]{rfc8085}. UDP verwendet die IP-Protokollnummer~17 und
ist als STD~6 standardisiert. Bemerkenswert ist, dass RFC~768 seit seiner Veröffentlichung im August 1980
über 45~Jahre hinweg keine formelle Aktualisierung erfahren hat. Erst RFC~9868 trägt den Header-Eintrag
\texttt{Updates:~768} und erweitert damit den ursprünglichen UDP-Standard erstmalig direkt \cite{rfc9868}.

Historisch geht UDP auf die Aufspaltung des ursprünglichen \emph{Transmission Control Program} zurück, das
Cerf, Dalal und Sunshine Ende 1974 noch als monolithische Einheit aus Netzwerk- und Transportfunktionen
beschrieben \cite{rfc675}. Mit deren Trennung in IP und TCP entstand Raum für eine schlanke,
transaktionsorientierte Alternative ohne den Overhead von TCP \cite{rfc768}. Der UDP-Kern blieb über die
folgenden Jahrzehnte unverändert, und Begleitdokumente wie die Einsatzrichtlinien von RFC~8085 klärten
Randfragen, ohne den Standard formell zu ändern \cite{rfc8085}. Die Motivation, ihn nun doch direkt zu
erweitern, benennt RFC~9868 selbst: UDP zählt zu den meistgenutzten Protokollen ohne Raum für
Header-Optionen, und weil inzwischen viele Protokolle UDP direkt nutzen, wäre eine Zwischenschicht oberhalb
von UDP für Bestandsanwendungen kaum noch auszurollen \cite[Sec.~4 und 5]{rfc9868}.

\subsection{Protokollstruktur}
\label{subsec:udp-struktur}

Ein UDP-Datagramm besteht aus einem Header mit fester Länge von 8~Byte und den darauf folgenden Nutzdaten;
den Aufbau des Headers zeigt \figref{fig:udp-header} \cite{rfc768}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=0.365cm, y=1cm]
    \node[bitindex, anchor=south west] at (0,0.06) {0};
    \node[bitindex, anchor=south] at (8,0.06) {8};
    \node[bitindex, anchor=south] at (16,0.06) {16};
    \node[bitindex, anchor=south] at (24,0.06) {24};
    \node[bitindex, anchor=south east] at (32,0.06) {31};
    \draw[feld] (0,-0.72) rectangle (16,0);
    \node[feldtext] at (8,-0.36) {Source Port};
    \draw[feld] (16,-0.72) rectangle (32,0);
    \node[feldtext] at (24,-0.36) {Destination Port};
    \draw[feld] (0,-1.54) rectangle (16,-0.82);
    \node[feldtext] at (8,-1.18) {Length};
    \draw[feld] (16,-1.54) rectangle (32,-0.82);
    \node[feldtext] at (24,-1.18) {Checksum};
    \draw[klammer] (32.35,0) -- (32.35,-1.54)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {fester Header\\8~Byte};
  \end{tikzpicture}
  \caption{Der UDP-Header nach RFC~768 \cite{rfc768}}
  \label{fig:udp-header}
\end{figure}

Aus \figref{fig:udp-header} geht hervor, dass der gesamte Header aus genau vier Feldern zu je 16~Bit
besteht; unter ihnen das Längenfeld, für das \secref{sec:ipv4} mit \eqnref{eq:udp-length-obergrenze} bereits
die Obergrenze aufgestellt hat. Die vier Felder im Einzelnen:

\begin{itemize}
    \item \texttt{Source Port}: Die Portnummer des sendenden Prozesses. Dieses Feld ist optional; wird es nicht
    benötigt, wird es auf null gesetzt \cite{rfc768}. In der Praxis nutzen Anwendungen den \textit{Source Port}
    zur Identifikation des Absenders, damit der Empfänger Antworten an den korrekten Port zurücksenden kann.

    \item \texttt{Destination Port}: Die Portnummer des Zielprozesses auf dem empfangenden Host. Zusammen mit der
    IP-Zieladresse ermöglicht dieses Feld die \textit{Demultiplexierung} eingehender Datagramme an die korrekte
    Anwendung.

    \item \texttt{Length}: Die Gesamtlänge des UDP-Datagramms in Byte, bestehend aus Header \emph{und} Nutzdaten.
    Der Minimalwert beträgt~8, was einem Datagramm ohne Nutzdaten entspricht \cite{rfc768}. Da das Feld 16~Bit
    breit ist, ergibt sich eine theoretische Maximalgröße von 65\,535~Byte.

    \item \texttt{Checksum}: Eine Prüfsumme zur Fehlererkennung über einen Pseudo-Header, den UDP-Header
    und die Nutzdaten; ihr Verfahren und ihre Sonderwerte erläutert der folgende Absatz.
\end{itemize}

\paragraph{Prüfsumme.}
Die Prüfsumme dient der Fehlererkennung: Mit ihr erkennt der Empfänger, ob einzelne Bits des Datagramms auf
dem Weg verändert wurden. Diese Prüfung auf der Transportschicht ist nötig, weil nicht jede Teilstrecke
eines Pfades eigene Fehlererkennung bietet und Bits auch im Speicher eines Routers kippen können
\cite{kurose2021topdown}. Für IPv4 darf der Sender die Prüfsumme durch den Feldwert null ungenutzt lassen;
bei IPv6 ist sie grundsätzlich verpflichtend, abgesehen von eigens geregelten Tunnel-Ausnahmen
\cite[Sec.~3.4 und 3.4.1]{rfc8085}.
Für die Berechnung summiert der Sender Pseudo-Header, UDP-Header und Nutzdaten
als 16-Bit-Wörter im Einerkomplement auf (ein Übertrag wird am niederwertigen Ende wieder hinzugezählt);
das invertierte Ergebnis bildet den Feldwert \cite{rfc768,rfc1071}. Der Empfänger summiert dieselben Wörter
einschließlich des Prüfsummenfelds und erwartet lauter Einsen, also \texttt{0xFFFF}; jedes andere Ergebnis
zeigt einen Übertragungsfehler an \cite{rfc1071}.

Dabei hilft eine Eigenheit des Einerkomplements: Es kennt zwei Darstellungen der Null, \texttt{0x0000} und
\texttt{0xFFFF}. Diese Doppelrolle nutzt UDP für die Sonderwerte des Feldes:
eine übertragene \texttt{0x0000} bedeutet, dass der Sender keine berechnet hat. Ergibt die
Berechnung selbst den Wert null, wird stattdessen \texttt{0xFFFF} übertragen.
Dadurch bleibt im Einerkomplement die Zahl Null erhalten, und der Sonderwert bleibt eindeutig \cite{rfc768}.
Dabei ist zu beachten, dass UDP Fehler nur erkennen und nicht beheben kann;
ein beschädigtes Datagramm wird typischerweise verworfen \cite{kurose2021topdown}.

\paragraph{Pseudo-Header.}
Die UDP-Prüfsumme schützt nicht nur die Transportschichtdaten, sondern bezieht über einen \textit{Pseudo-Header}
auch Informationen der Vermittlungsschicht ein. Dieser Pseudo-Header ist kein Teil des Datagramms und steht an
keiner Stelle im Paket: Sender und Empfänger bauen ihn jeweils nur für die Berechnung im Speicher auf und
stellen ihn dem UDP-Header dabei gedanklich voran \cite{rfc768}. Dieses Konzept stellt sicher, dass ein
Datagramm, das durch einen IP-Fehler an den falschen Host oder das falsche Protokoll zugestellt wird, erkannt
und verworfen werden kann. Seinen Aufbau zeigt \figref{fig:udp-pseudo-header} \cite{rfc768}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=0.365cm, y=1cm]
    \node[bitindex, anchor=south west] at (0,0.06) {0};
    \node[bitindex, anchor=south] at (8,0.06) {8};
    \node[bitindex, anchor=south] at (16,0.06) {16};
    \node[bitindex, anchor=south] at (24,0.06) {24};
    \node[bitindex, anchor=south east] at (32,0.06) {31};
    \draw[feldoptional] (0,-0.72) rectangle (32,0);
    \node[feldtext] at (16,-0.36) {Source Address};
    \draw[feldoptional] (0,-1.54) rectangle (32,-0.82);
    \node[feldtext] at (16,-1.18) {Destination Address};
    \draw[feldoptional] (0,-2.36) rectangle (8,-1.64);
    \node[feldtext] at (4,-2.00) {Zero};
    \draw[feldoptional] (8,-2.36) rectangle (16,-1.64);
    \node[feldtext] at (12,-2.00) {Protocol};
    \draw[feldoptional] (16,-2.36) rectangle (32,-1.64);
    \node[feldtext] at (24,-2.00) {UDP Length};
    \draw[klammer] (32.35,0) -- (32.35,-2.36)
      node[midway, right=8pt, font=\scriptsize, text=feldlinie, align=left] {12~Byte};
  \end{tikzpicture}
  \caption{Der Pseudo-Header der UDP-Prüfsumme für IPv4 \cite{rfc768}}
  \label{fig:udp-pseudo-header}
\end{figure}

Der Pseudo-Header besteht aus beiden IP-Adressen, einem Nullbyte, \texttt{Protocol} und der wiederholten
\texttt{UDP Length}. Sender und Empfänger bilden diese nicht übertragenen zwölf Byte aus den jeweils
vorliegenden Werten \cite{rfc768}. Ohne Umschreibung sind die Werte an beiden Enden gleich; eine NAT kann
Adressen oder Ports samt UDP-Prüfsumme anpassen \cite[Sec.~11.3]{rfc9868}. Das Nullbyte ergänzt das
8-Bit-Protokollfeld zu einem 16-Bit-Wort, wie ein Nulloktett Nutzdaten ungerader Länge für die Rechnung
auffüllt \cite{rfc768,rfc1071}. Ein veränderliches Feld wie \texttt{Time to Live} wäre ungeeignet, weil
Sender und Empfänger dann verschiedene Summen bildeten (\secref{sec:ipv4}).

\subsection{Eigenschaften}
\label{subsec:udp-eigenschaften}

UDP ist nachrichtenorientiert: Jede Sendeoperation erzeugt ein Datagramm, das der Empfänger als Einheit
entgegennimmt. TCP stellt dagegen einen Bytestrom bereit, segmentiert ihn selbst und überlässt der Anwendung
die Kennzeichnung von Nachrichtengrenzen \cite[Sec.~2.2 und 3.7]{rfc9293}. Klassisches UDP hält zudem keine
Sequenznummern, Fenstergrößen oder Zeitgeber je Kommunikationsbeziehung vor. Nur die Reassemblierung von
FRAG-Datagrammen bildet in RFC~9868 eine begrenzte Ausnahme \cite[Sec.~6]{rfc9868}.

Da kein Verbindungsaufbau nötig ist, kann die erste Nachricht sofort gesendet werden
\cite[Sec.~1]{rfc8085}. UDP garantiert weder Zustellung noch Reihenfolge und wiederholt verlorene
Datagramme nicht \cite{rfc768}. Später eintreffende Datagramme werden auf UDP-Ebene deshalb nicht wegen
eines Verlusts zurückgehalten; dort entsteht kein \textit{Head-of-Line-Blocking} wie bei einem gesicherten
Bytestrom. Benötigte Zuverlässigkeit oder Reihenfolge muss die Anwendung selbst herstellen
\cite[Sec.~3.3]{rfc8085}.

Diese Eigenschaften eignen sich für latenzempfindliche oder verlusttolerante Anwendungen. Dem festen
UDP-Header fehlt jedoch ein eigener Erweiterungsraum. RFC~9868 nutzt dafür die Surplus Area, ohne den
Header zu verändern (\secref{sec:surplus-area}).

\section{Erweiterungsmuster im Vergleich}
\label{sec:erweiterungsmuster}

Ein Protokoll ohne freie Bits oder Versionsfeld lässt sich nur erweitern, wenn die Grenze zwischen Kopf,
Nutzdaten und Zusatzraum anderweitig erkennbar ist. \tabref{tab:erweiterungsmuster} vergleicht die drei
hier relevanten Muster.

\begin{table}[htbp]
  \centering
  \caption{Muster für nachträgliche Protokollerweiterungen}
  \label{tab:erweiterungsmuster}
  \footnotesize
  \begin{tabularx}{\textwidth}{@{}L{2.3cm}L{2.8cm}YL{3.2cm}L{2.1cm}@{}}
    \toprule
    \textbf{Muster} & \textbf{Lage und Kennzeichnung} & \textbf{Voraussetzung} &
      \textbf{Verhalten alter Empfänger} & \textbf{Grenze} \\
    \midrule
    TCP- und IPv4-Header\-optionen
      & zwischen festem Header und Nutzdaten; Offset im Header
      & das ursprüngliche Format besitzt ein Offsetfeld
      & Bestandteil des bereits definierten Headerformats; auf UDP nicht übertragbar
      & bei TCP höchstens 40~Byte \cite{rfc9293} \\
    Teredo-Trailer in den UDP-Nutzdaten
      & hinter dem inneren IPv6-Paket; dessen Länge markiert das Ende
      & Nutzdaten besitzen eine eigene, bekannte Grenze
      & im Teredo-Protokoll auswertbar; für beliebige UDP-Altempfänger In-Band-Daten
      & innerhalb der UDP-Nutzdaten \cite{rfc4380,rfc6081} \\
    UDP Options in der Surplus Area
      & hinter \texttt{UDP Length}, aber vor dem IP-Datagramm\-ende
      & redundante Längenangaben von UDP und IP
      & gewöhnliche Empfänger stoppen an \texttt{UDP Length}; dokumentierte Ausnahmen bleiben
      & bis zum IP-Datagramm\-ende \cite[Sec.~4 und 18]{rfc9868} \\
    \bottomrule
  \end{tabularx}
\end{table}

Das erste Muster kennen TCP und IPv4 (\secref{sec:ipv4}); beide teilen dabei auch das Füllmuster aus den
Ein-Byte-Codes \texttt{NOP} (No Operation) und \texttt{EOL} (End of Option List) \cite{rfc9293,rfc791}. UDP
besitzt dagegen vier feste 16-Bit-Felder und kein Offset- oder Ankündigungsfeld. Eine Headererweiterung wäre
daher nicht abwärtskompatibel. Eine In-Band-Konvention scheitert bei beliebigen Bestandsanwendungen, weil
sie die Zusatzfelder als Nutzdaten lesen \cite[Sec.~4]{rfc9868}. RFC~9868 nutzt stattdessen, dass UDP
seine Länge wiederholt: Endet \texttt{UDP Length} vor der IP-Nutzlast, bildet der Rest die Surplus Area.
Dieser Mechanismus bleibt mit Teredo-Trailern kompatibel. Der historische Grund für das UDP-Längenfeld ist
ungeklärt; RFC~9868 nennt vorgeschlagene Erklärungen, die nicht zu früheren UDP-Fassungen und zur
zeitgleichen Multiplex-Konstruktion passen, und lässt die Ursache offen \cite[Sec.~4]{rfc9868}. Für
bestehendes UDP ist damit nur der Surplus-Trailer allgemein einsetzbar. Seinen Aufbau behandelt der
folgende Abschnitt.

\section{Die Surplus Area}
\label{sec:surplus-area}

\subsection{Lage und Kompatibilität}
\label{subsec:surplus-lage}

\texttt{UDP Length} umfasst nur den UDP-Header und die UDP-Nutzdaten. Die Längenangaben des IP-Headers können
einen größeren Transportbereich ausweisen (\secref{sec:ipv4}). RFC~9868 nennt die gesamte Nutzlast hinter dem
IP-Header \textit{IP Transport Payload} und den Teil zwischen dem Ende der UDP-Nutzdaten und dem Ende des
IP-Datagramms \textit{Surplus Area}; dort liegen die UDP Options. Die Lage beider Bereiche zeigt
\figref{fig:surplus-area} an einem Beispieldatagramm mit \texttt{Total Length} 60, \texttt{IHL} 5 und
\texttt{UDP Length} 32 \cite[Sec.~7]{rfc9868}:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \draw[feld] (0,-0.92) rectangle (2.6,0);
    \node[feldtext] at (1.3,-0.34) {IP-Header};
    \node[font=\tiny, text=feldlinie] at (1.3,-0.66) {20~Byte (\texttt{IHL} 5)};
    \draw[feld] (2.6,-0.92) rectangle (4.6,0);
    \node[feldtext] at (3.6,-0.34) {UDP-Header};
    \node[font=\tiny, text=feldlinie] at (3.6,-0.66) {8~Byte};
    \draw[feld] (4.6,-0.92) rectangle (9.4,0);
    \node[feldtext] at (7.0,-0.34) {UDP-Nutzdaten};
    \node[font=\tiny, text=feldlinie] at (7.0,-0.66) {24~Byte};
    \draw[feldakzent] (9.4,-0.92) rectangle (12.4,0);
    \node[feldtext] at (10.9,-0.34) {Surplus Area};
    \node[font=\tiny, text=feldlinie] at (10.9,-0.66) {8~Byte};
    \node[bitindex, anchor=north] at (0,-1.04) {0};
    \node[bitindex, anchor=north] at (2.6,-1.04) {20};
    \node[bitindex, anchor=north] at (4.6,-1.04) {28};
    \node[bitindex, anchor=north] at (9.4,-1.04) {52};
    \node[bitindex, anchor=north] at (12.4,-1.04) {60};
    \draw[klammer] (2.6,0.12) -- (12.4,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {IP Transport Payload (40~Byte)};
    \draw[klammer] (9.4,-1.56) -- (2.6,-1.56)
      node[midway, below=7pt, font=\scriptsize, text=feldlinie] {\texttt{UDP Length} = 32};
  \end{tikzpicture}
  \caption{IP Transport Payload und Surplus Area eines Datagramms mit \texttt{Total Length} 60}
  \label{fig:surplus-area}
\end{figure}

\figref{fig:surplus-area} zeigt drei Grenzen ab Byte~0 des IP-Datagramms: Die IP Transport Payload beginnt
nach dem IP-Header, \texttt{UDP Length} deckt von dort nur UDP-Header und Nutzdaten ab, und die Surplus Area
reicht bis zum IP-Datagrammende. Im Beispiel bleiben von $60-20=40$~Byte IP Transport Payload nach
\texttt{UDP Length}~$=32$ genau 8~Byte. Allgemein gilt
\begin{equation}
  \textit{Surplus-Länge} = \textit{Total Length} - \textit{IP-Headerlänge} - \textit{UDP Length}.
  \label{eq:surplus-laenge}
\end{equation}
Die Obergrenze aus \eqnref{eq:udp-length-obergrenze} verhindert negative Werte; bei Überschreitung ist das
Datagramm ungültig und wird still verworfen \cite[Sec.~10]{rfc9868}. Ohne Optionen ist die Differenz null.
RFC~768 verlangt diese Gleichheit jedoch nicht; RFC~9868 nutzt die seit 1980 bestehende Freiheit
\cite[Sec.~7]{rfc9868}.

Ein Altempfänger verarbeitet gewöhnlich nur die durch \texttt{UDP Length} begrenzten Nutzdaten und
überliest den Rest. RFC~9868 dokumentiert neben diesem Verhalten getesteter Standardsysteme auch
Abweichungen: Ein eingebettetes Gerät reichte das ganze IP-Datagramm weiter, ein Erkennungssystem meldete
die Differenz als Angriff \cite[Sec.~18]{rfc9868}. Die Erweiterung ist daher für Endsysteme typischerweise,
nicht ausnahmslos, abwärtskompatibel; reale Pfade untersucht \chapref{chap:evaluation}. Für IP bleibt die
Surplus Area gewöhnliche Transportnutzlast innerhalb von \texttt{Total Length}; neue IP-Felder oder eine
geänderte IPv4-Verarbeitung entstehen nicht \cite[Sec.~16]{rfc9868}.

Die Surplus Area darf an jedem gültigen Byte-Offset, auch einem ungeraden, beginnen. Das
Ausrichtungsproblem wird in ihr gelöst (\secref{subsec:surplus-aufbau}). \texttt{UDP Length} bleibt eine
Länge und markiert zugleich den \textit{trailer options offset} \cite[Sec.~7]{rfc9868}. Damit erhält ein
unverändertes Feld eine zusätzliche Bedeutung \cite[Sec.~21]{rfc9868}.

\subsection{Aufbau und Ausrichtung}
\label{subsec:surplus-aufbau}

Trägt die Surplus Area Optionen, ist sie vollständig strukturiert; ungedeutete Restbytes gibt es dann nicht.
Sie beginnt mit der \textit{Option Checksum} (OCS), einem 2~Byte großen Prüfsummenfeld an der ersten
2-Byte-Grenze der Area, gemessen am Beginn des IP-Datagramms. Beginnt die Surplus Area an einem ungeraden
Offset, stellt genau ein Nullbyte vor dem OCS diese Ausrichtung her (das \textit{Pad}). Hinter dem OCS folgt
die Optionsliste im TLV-Format (Type-Length-Value, \secref{subsec:surplus-tlv}); bei gewöhnlichen
Datagrammen läuft sie bis zum Ende der Surplus Area oder endet vorzeitig mit \texttt{EOL}, worauf nur noch
Nullbytes folgen \cite[Sec.~8]{rfc9868}. Nur beim UDP-Fragment endet sie früher, denn dort folgt hinter den
Optionen noch der Fragmentdatenbereich (\secref{subsec:surplus-prinzipien}).
Diese Anatomie zeigt \figref{fig:surplus-struktur} an einem Datagramm mit 5~Byte Nutzdaten und einer
einzelnen Option:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \draw[feld] (0,-0.92) rectangle (2.0,0);
    \node[feldtext] at (1.0,-0.34) {IP-Header};
    \node[font=\tiny, text=feldlinie] at (1.0,-0.66) {20~Byte};
    \draw[feld] (2.0,-0.92) rectangle (3.7,0);
    \node[feldtext] at (2.85,-0.34) {UDP-Header};
    \node[font=\tiny, text=feldlinie] at (2.85,-0.66) {8~Byte};
    \draw[feld] (3.7,-0.92) rectangle (5.3,0);
    \node[feldtext] at (4.5,-0.34) {Nutzdaten};
    \node[font=\tiny, text=feldlinie] at (4.5,-0.66) {5~Byte};
    \draw[feldoptional] (5.3,-0.92) rectangle (5.85,0);
    \node[feldtext, font=\scriptsize\ttfamily] at (5.575,-0.46) {00};
    \draw[feldakzent] (5.85,-0.92) rectangle (6.95,0);
    \node[feldtext] at (6.4,-0.34) {OCS};
    \node[font=\tiny, text=feldlinie] at (6.4,-0.66) {2~Byte};
    \draw[feldakzent] (6.95,-0.92) rectangle (7.85,0);
    \node[feldtext] at (7.4,-0.34) {Kind};
    \node[font=\tiny, text=feldlinie] at (7.4,-0.66) {1~Byte};
    \draw[feldakzent] (7.85,-0.92) rectangle (8.95,0);
    \node[feldtext] at (8.4,-0.34) {Length};
    \node[font=\tiny, text=feldlinie] at (8.4,-0.66) {1~Byte};
    \draw[feldakzent] (8.95,-0.92) rectangle (10.55,0);
    \node[feldtext] at (9.75,-0.46) {Value \dots};
    \draw[feldakzent] (10.55,-0.92) rectangle (11.45,0);
    \node[feldtext] at (11.0,-0.34) {EOL};
    \node[font=\tiny, text=feldlinie] at (11.0,-0.66) {\texttt{0x00}};
    \draw[feldakzent] (11.45,-0.92) rectangle (13.05,0);
    \node[feldtext] at (12.25,-0.34) {Nullfüllung};
    \node[font=\tiny, text=feldlinie] at (12.25,-0.66) {\texttt{0x00} \dots};
    \node[bitindex, anchor=north] at (0,-1.04) {0};
    \node[bitindex, anchor=north] at (2.0,-1.04) {20};
    \node[bitindex, anchor=north] at (3.7,-1.04) {28};
    \node[bitindex, anchor=north east] at (5.42,-1.04) {33};
    \node[bitindex, anchor=north west] at (5.73,-1.04) {34};
    \node[bitindex, anchor=north] at (6.95,-1.04) {36};
    \draw[klammer] (2.0,0.12) -- (5.3,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {\texttt{UDP Length} = 13};
    \draw[klammer] (6.95,0.12) -- (10.55,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {eine Option (TLV)};
    \draw[klammer] (13.05,-1.56) -- (5.3,-1.56)
      node[midway, below=7pt, font=\scriptsize, text=feldlinie] {Surplus Area};
    \draw[draw=feldlinie, thin] (2.0,-2.30) -- (5.3,-2.30);
    \draw[draw=feldlinie, thin] (2.0,-2.22) -- (2.0,-2.38);
    \draw[draw=feldlinie, thin] (5.3,-2.22) -- (5.3,-2.38);
    \node[font=\tiny, text=feldlinie] at (3.65,-2.58) {Deckungsbereich der UDP-Prüfsumme};
    \draw[draw=akzent, semithick] (5.38,-2.30) -- (13.05,-2.30);
    \draw[draw=akzent, semithick] (5.38,-2.22) -- (5.38,-2.38);
    \draw[draw=akzent, semithick] (13.05,-2.22) -- (13.05,-2.38);
    \node[font=\tiny, text=akzent] at (9.2,-2.58) {Schutzbereich des OCS};
  \end{tikzpicture}
  \caption{Anatomie eines Datagramms mit UDP Options}
  \label{fig:surplus-struktur}
\end{figure}

Aus \figref{fig:surplus-struktur} geht hervor, wie die Bausteine ineinandergreifen: \texttt{UDP Length}
$= 8 + 5 = 13$ lässt die Surplus Area bei Byte $20 + 13 = 33$ beginnen. Dieser Offset ist ungerade, also
rückt ein Pad-Byte das OCS auf die 2-Byte-Grenze bei Byte 34, und die erste Option beginnt bei Byte 36.
Die unterste Ebene der Abbildung zeigt zugleich, dass sich die beiden Prüfbereiche im Datagramm lückenlos
ergänzen: Die UDP-Prüfsumme endet an \texttt{UDP Length} (hinzu kommt nur ihr Pseudo-Header,
\secref{subsec:udp-struktur}), das OCS schützt genau den Teil, den die UDP-Prüfsumme nie sieht
\cite[Sec.~8]{rfc9868}.

Für diese Stelle formuliert RFC~9868 verbindliche Regeln \cite[Sec.~8]{rfc9868}: Der Sender darf Optionen an
jedem gültigen \texttt{UDP-Length}-Offset beginnen lassen, und Ausrichtungsbytes vor dem OCS müssen null
sein. Findet der Empfänger dort einen anderen Wert, greift zum ersten Mal eine Grundregel, die alle weiteren
Prüfungen dieses Abschnitts teilen: Im Standardfall kostet ein Fehler in der Surplus Area nur die Optionen.
Der Empfänger verwirft die Optionsliste still und stellt die Nutzdaten trotzdem zu, wie es auch ein
Altempfänger täte \cite[Sec.~8 und 9]{rfc9868}. Standardfall heißt dabei: Das Datagramm selbst ist gültig,
denn eine falsche \texttt{UDP Length} oder eine fehlgeschlagene UDP-Prüfsumme verwirft es komplett, noch
bevor Optionen betrachtet werden, während eine zulässig ungenutzte Prüfsumme (\secref{subsec:udp-struktur})
kein Fehler ist \cite[Sec.~10 und 14]{rfc9868}; die Anwendung hat kein strengeres Verhalten angefordert; und
es ist kein UDP-Fragment, denn die Sonderwege von FRAG und UNSAFE behandelt \secref{subsec:surplus-prinzipien}.
Die 2-Byte-Ausrichtung selbst begründet der RFC pragmatisch: Das OCS wird, wie der nächste Unterabschnitt
zeigt, über 16-Bit-Wörter berechnet, und die Ausrichtung erspart dieser Rechnung das Vertauschen von Bytes
\cite[Sec.~8]{rfc9868}.

Ob ein Pad-Byte nötig ist, entscheidet allein die Parität des Startoffsets; es gibt nur zwei Fälle, kein
Pad oder genau eines. Den Unterschied zeigt \figref{fig:surplus-pad} an zwei sonst gleichen Datagrammen,
deren Nutzdaten sich um ein einziges Byte unterscheiden:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \node[feldtext, anchor=west] at (0,-0.30) {5~Byte Nutzdaten};
    \node[font=\tiny, text=feldlinie, anchor=west] at (0,-0.62) {Start bei $20 + 13 = 33$};
    \draw[feld] (3.2,-0.92) rectangle (5.0,0);
    \node[feldtext] at (4.1,-0.46) {UDP-Header};
    \draw[feld] (5.0,-0.92) rectangle (6.4,0);
    \node[feldtext] at (5.7,-0.46) {Nutzdaten};
    \draw[feldoptional] (6.4,-0.92) rectangle (6.95,0);
    \node[feldtext, font=\scriptsize\ttfamily] at (6.675,-0.46) {00};
    \draw[feldakzent] (6.95,-0.92) rectangle (8.05,0);
    \node[feldtext] at (7.5,-0.46) {OCS};
    \draw[feldakzent] (8.05,-0.92) rectangle (10.0,0);
    \node[feldtext] at (9.025,-0.46) {Optionen \dots};
    \node[font=\tiny, text=feldlinie, anchor=west] at (10.3,-0.46) {ungerade: ein Pad, OCS ab 34};
    \node[feldtext, anchor=west] at (0,-1.70) {6~Byte Nutzdaten};
    \node[font=\tiny, text=feldlinie, anchor=west] at (0,-2.02) {Start bei $20 + 14 = 34$};
    \draw[feld] (3.2,-2.32) rectangle (5.0,-1.40);
    \node[feldtext] at (4.1,-1.86) {UDP-Header};
    \draw[feld] (5.0,-2.32) rectangle (6.68,-1.40);
    \node[feldtext] at (5.84,-1.86) {Nutzdaten};
    \draw[feldakzent] (6.68,-2.32) rectangle (7.78,-1.40);
    \node[feldtext] at (7.23,-1.86) {OCS};
    \draw[feldakzent] (7.78,-2.32) rectangle (9.73,-1.40);
    \node[feldtext] at (8.755,-1.86) {Optionen \dots};
    \node[font=\tiny, text=feldlinie, anchor=west] at (10.3,-1.86) {gerade: kein Pad, OCS ab 34};
  \end{tikzpicture}
  \caption{Paritätsentscheid für das Pad-Byte vor dem OCS}
  \label{fig:surplus-pad}
\end{figure}

Aus \figref{fig:surplus-pad} geht hervor, dass bereits ein einziges Nutzdatenbyte über das Pad entscheidet:
Bei einem IP-Header ohne Optionen liegt der Startoffset bei $20 + \textit{UDP Length}$, sodass allein die
Parität von \texttt{UDP Length} den Ausschlag gibt. An der Größe der Surplus Area hängt zugleich, ob sie
überhaupt Optionen trägt: Nötig sind mindestens 2~Byte bei geradem und 3~Byte bei ungeradem Start, damit das
ausgerichtete OCS vollständig hineinpasst. Ein kleinerer Überschuss enthält schlicht keine Optionen und ist
kein Fehlerfall; passt genau das OCS hinein und sonst nichts, ist die Optionsliste leer und das Datagramm
ebenfalls gültig \cite[Sec.~7 und 8]{rfc9868}. Nutzdaten braucht es dagegen keine: Auch ein Datagramm mit
\texttt{UDP Length} 8, also leerem Nutzdatenteil, kann Optionen tragen \cite[Sec.~8]{rfc9868}; davon macht
später die FRAG-Option Gebrauch, deren Fragmente ihre Daten vollständig in der Surplus Area transportieren.

\subsection{OCS und Fehlerbehandlung}
\label{subsec:surplus-ocs}

Das OCS selbst ist eine gewöhnliche Internet-Prüfsumme mit derselben Einerkomplementarithmetik, die
\secref{subsec:udp-struktur} für die UDP-Prüfsumme beschreibt \cite{rfc1071}. Zwei Eigenheiten unterscheiden
die beiden. Erstens schützt das OCS genau den Bereich, den die UDP-Prüfsumme ausspart: Die Summe läuft ab
dem ausgerichteten OCS-Feld über den Rest der Surplus Area, wobei das OCS-Feld selbst als null gilt; das Pad
davor wird getrennt auf null geprüft und ist so mitgeschützt \cite[Sec.~8 und 9]{rfc9868}. Zweitens geht
zusätzlich die Länge der gesamten Surplus Area einschließlich des Pads als vorzeichenloser 16-Bit-Summand in
die Rechnung ein, obwohl dieser Wert in keinem eigenen Feld des Pakets steht \cite[Sec.~9]{rfc9868}. Das
folgt demselben Gedanken wie der Pseudo-Header aus \secref{subsec:udp-struktur}:
Wie die UDP-Prüfsumme dort die \texttt{UDP Length} einbezieht, bezieht das OCS die Surplus-Länge ein, und
beide Seiten können diesen Summanden unabhängig voneinander aus den Längenfeldern ableiten. Der Sender trägt
das invertierte Ergebnis in das Feld ein. Abschnitt~9 verlangt ein OCS ungleich null, sobald die
UDP-Prüfsumme ungleich null ist, und kennzeichnet ein ungenutztes OCS mit null \cite[Sec.~9]{rfc9868}. Eine
ausdrückliche Senderegel für eine errechnete Null enthält der Abschnitt nicht; weil der Nullwert die
unbenutzte Form kennzeichnet, bleibt für ein verwendetes OCS im Einerkomplement nur die Abbildung auf
\texttt{0xFFFF}, analog zur UDP-Prüfsumme \cite{rfc768}. Der Empfänger summiert die Wörter der Surplus Area
einschließlich des OCS-Felds sowie den Längensummanden und erwartet wie in \secref{subsec:udp-struktur}
lauter Einsen, also \texttt{0xFFFF} \cite{rfc1071}.

\tabref{tab:ocs-rechenbeispiel} führt diese Rechnung an einem bewusst kleinen Beispiel vollständig vor.

\begin{table}[htbp]
  \centering
  \caption{OCS-Rechnung einer 6-Byte-Surplus-Area bei geradem Startoffset}
  \label{tab:ocs-rechenbeispiel}
  \small
  \begin{tabularx}{\textwidth}{@{}L{4.0cm}YY@{}}
    \toprule
    \textbf{Schritt} & \textbf{Sender} & \textbf{Empfänger} \\
    \midrule
    Optionswörter hinter dem OCS & \texttt{0x0101}, \texttt{0x0000}
      & \texttt{0x0101}, \texttt{0x0000} \\
    Längensummand & \texttt{0x0006} & \texttt{0x0006} \\
    OCS-Wort & \texttt{0x0000} als Platzhalter & empfangenes \texttt{0xFEF8} \\
    Einerkomplementsumme & \texttt{0x0107} & \texttt{0xFFFF} \\
    Ergebnis & Invertierung ergibt \texttt{0xFEF8} & OCS ist gültig \\
    \bottomrule
  \end{tabularx}
\end{table}

Die Optionswörter stehen für zwei \texttt{NOP}, ein \texttt{EOL} und ein Nullbyte. Die zwei \texttt{NOP}
dienen nur der Rechnung; als Füllung vor \texttt{EOL} rät RFC~9868 von ihnen ab. Ein regulärer Sender
schriebe \texttt{EOL} unmittelbar hinter das OCS \cite[Sec.~11.1]{rfc9868}.

Diese Bauart hat einen praktischen Hintergrund: Manche fehlerhafte Middleboxes berechnen die UDP-Prüfsumme
über die gesamte IP-Nutzlast statt nur bis \texttt{UDP Length}. Weil das OCS über die Surplus Area und deren
Länge gebildet und anschließend invertiert wird, neutralisiert es den Beitrag der fälschlich einbezogenen
Surplus Area: Mit gesetztem OCS fällt die UDP-Prüfsumme in beiden Rechnungen gleich aus; an dieser fehlerhaften
Ausdehnung des Prüfbereichs scheitert das Datagramm bei solchen Geräten also nicht \cite[Sec.~9]{rfc9868}.
In erster Linie dient das OCS ohnehin dem
Erkennen zufälliger Fehler und anderweitiger, nicht standardkonformer Nutzungen der Surplus Area; ein Schutz
gegen gezielte Angriffe ist es ausdrücklich nicht \cite[Sec.~9]{rfc9868}. Wie die UDP-Prüfsumme ist das OCS
zudem bedingt abschaltbar: Der Wert \texttt{0x0000} bedeutet ungenutzt, zulässig ist das nur, wenn auch die
UDP-Prüfsumme null ist, und Implementierungen müssen standardmäßig ein echtes OCS setzen
\cite[Sec.~9]{rfc9868}. Schlägt die Prüfung beim Empfänger fehl, gilt die Grundregel aus
\secref{subsec:surplus-aufbau}: Optionen still verwerfen, Nutzdaten bei bestandener oder zulässig
ungenutzter UDP-Prüfsumme dennoch zustellen \cite[Sec.~9]{rfc9868}.

\subsection{TLV-Format und Empfangsentscheidung}
\label{subsec:surplus-tlv}

Die Optionen hinter dem OCS folgen dem TLV-Muster, dessen Syntax RFC~9868 bewusst an TCP anlehnt
(\secref{sec:erweiterungsmuster}): Ein 1~Byte großes Feld \texttt{Kind}, benannt nach dem TCP-Vorbild,
identifiziert die Option; das 1~Byte große Feld \texttt{Length} zählt die Gesamtlänge der Option
einschließlich \texttt{Kind} und \texttt{Length} selbst; dahinter kann ein Wert folgen
\cite[Sec.~8 und 10]{rfc9868}. Für die Größe des Werts ist das nur scheinbar eine enge Grenze: Im Standardformat
fasst eine Option höchstens 254~Byte einschließlich \texttt{Kind} und \texttt{Length}; größere Optionen
weichen auf ein erweitertes Format aus, in dem der Wert 255 im \texttt{Length}-Feld ein zusätzliches
16-Bit-Längenfeld ankündigt. Damit sind je Option höchstens 65535~Byte einschließlich der Kopffelder
kodierbar \cite[Sec.~10]{rfc9868}. Eine zusätzliche Gesamtgrenze für den Optionsraum kennt RFC~9868 dagegen
bewusst nicht: Wie das Entwurfsprinzip der fehlenden Längengrenze in \secref{subsec:surplus-prinzipien}
festhält, gilt allein die Grenze des UDP-Pakets selbst; bei einem nicht fragmentierten Datagramm begrenzt
somit der im IP-Datagramm verfügbare Platz, wie viele Optionen es trägt. Auch die beiden Ein-Byte-Codes aus
\secref{sec:erweiterungsmuster} kehren als einzige Ausnahmen ohne Längenfeld wieder: \texttt{NOP} (Kind 1)
dient mit impliziter Länge eins der Ausrichtung zwischen Optionen und darf sich dafür wiederholen,
\texttt{EOL} (Kind 0) beendet die Liste vorzeitig; die Bytes dahinter muss der Sender bis zum Ende der
Surplus Area beziehungsweise des Optionsbereichs eines UDP-Fragments auf null setzen
\cite[Sec.~8 und 11.1]{rfc9868}. Der Empfänger darf diese Nullfüllung prüfen,
muss es aber nicht; findet er dabei ein Byte ungleich null, verwirft er nach der Grundregel aus
\secref{subsec:surplus-aufbau} die gesamte Optionsliste still \cite[Sec.~11.1]{rfc9868}. Variable
Optionslängen und künftige Erweiterungen sind damit im Format selbst angelegt.

Für die Anzahl der Optionen gilt dasselbe Bild wie für ihre Länge: Eine feste Obergrenze gibt es nicht;
ohne vorzeitiges \texttt{EOL} endet die Liste erst am Ende der Surplus Area. Begrenzend wirken stattdessen
zwei Regelgruppen. Erstens die
Wiederholungsregeln: Außer FRAG, \texttt{NOP} und den Experimentaloptionen soll jede Option höchstens einmal
je Datagramm vorkommen, ein Empfänger wertet von einer solchen Option ohnehin nur die erste Instanz, und
ein mehrfaches FRAG macht
die gesamte Optionsliste ungültig \cite[Sec.~10]{rfc9868}; \texttt{NOP} wiederum soll höchstens siebenmal in
Folge stehen und nicht als Ersatz für \texttt{EOL} samt Nullfüllung dienen \cite[Sec.~11.2]{rfc9868}.
Zweitens die Vollständigkeitsregel: Jede Option muss ganz in die Surplus Area passen. Meldet ein
\texttt{Length}-Feld eine Länge über deren Ende hinaus, gilt die gesamte Surplus Area als fehlerhaft, und
der Empfänger verwirft still alle Optionen, nicht nur die angeschnittene \cite[Sec.~10]{rfc9868}; das
verifizierte Erratum EID~8834 bestätigt diese Regel gegen eine ältere, weichere Formulierung in Abschnitt~14
\cite{rfc9868-eid8834}. Im Regelfall entsteht diese Lage nicht, denn der Sender muss die Surplus Area von
vornherein so bemessen, dass das OCS und alle Optionen vollständig hineinpassen \cite[Sec.~8]{rfc9868}.

Damit sind die Prüfschritte des Standardpfads beisammen. \figref{fig:surplus-empfang} ordnet sie in der
Reihenfolge, in der ein optionsfähiger Empfänger sie durchläuft, und stellt ihnen die vorgelagerte Prüfung
des Datagramms selbst voran:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      >={Stealth[length=5pt, width=4pt]},
      frage/.style={draw=feldlinie, fill=feldgrau, rounded corners=2pt, thin, align=center,
        font=\scriptsize, minimum width=3.6cm, minimum height=0.55cm},
      ausgang/.style={rounded corners=2pt, align=center, font=\scriptsize, inner sep=4pt},
      kante/.style={->, draw=feldlinie, thin},
      kantentext/.style={font=\scriptsize, text=feldlinie, inner sep=1.5pt},
    ]
    \node[frage] (q0) at (6.0,0.1)   {\texttt{UDP Length} gültig, UDP-Prüfsumme\\ gültig oder zulässig ungenutzt?};
    \node[frage] (q1) at (6.0,-1.2)  {Surplus-Länge $> 0$?};
    \node[frage] (q2) at (6.0,-2.2)  {Platz für das ausgerichtete OCS?};
    \node[frage] (q3) at (6.0,-3.2)  {Pad (falls nötig) gleich null?};
    \node[frage] (q4) at (6.0,-4.2)  {OCS gültig oder zulässig ungenutzt?};
    \node[frage] (q5) at (6.0,-5.2)  {Kein listenweiter\\ Ablehnungsgrund?};
    \node[ausgang, draw=warnton, fill=warnhell, semithick] (drop) at (11.8,0.1)
      {Datagramm verwerfen};
    \node[ausgang, draw=feldlinie, fill=feldgrau] (keine) at (1.6,-1.7)
      {keine Optionen:\\ Nutzdaten wie bisher};
    \node[ausgang, draw=warnton, fill=warnhell] (verworfen) at (11.8,-4.2)
      {Optionen still verwerfen,\\ Nutzdaten trotzdem zustellen};
    \node[ausgang, draw=akzent, fill=akzenthell] (erfolg) at (6.0,-6.4)
      {Optionen einzeln auswerten,\\ Nutzdaten zustellen};
    \node[ausgang, draw=feldlinie, dashed, fill=white, font=\scriptsize\itshape, anchor=west]
      (sonder) at (8.7,-6.4) {Sonderwege: FRAG puffert bis zur\\ Reassemblierung, UNSAFE nach 2.4.5};
    \draw[kante] (q0) -- node[kantentext, anchor=west] {ja} (q1);
    \draw[kante] (q1) -- node[kantentext, anchor=west] {ja} (q2);
    \draw[kante] (q2) -- node[kantentext, anchor=west] {ja} (q3);
    \draw[kante] (q3) -- node[kantentext, anchor=west] {ja} (q4);
    \draw[kante] (q4) -- node[kantentext, anchor=west] {ja} (q5);
    \draw[kante] (q5) -- node[kantentext, anchor=west] {ja} (erfolg);
    \draw[kante] (q0.east) -- node[kantentext, anchor=south] {nein} (drop.west);
    \draw[kante] (q1.west) -| node[kantentext, anchor=south, pos=0.25] {nein} (keine.north);
    \draw[kante] (q2.west) -| node[kantentext, anchor=north, pos=0.25] {nein} (keine.south);
    \draw[kante] (q3.east) -| node[kantentext, anchor=south, pos=0.25] {nein} (verworfen.north);
    \draw[kante] (q4.east) -- node[kantentext, anchor=south] {nein} (verworfen.west);
    \draw[kante] (q5.east) -| node[kantentext, anchor=north, pos=0.25] {nein} (verworfen.south);
    \draw[draw=feldlinie, dashed, thin] (erfolg.east) -- (sonder.west);
  \end{tikzpicture}
  \caption{Empfangsentscheidung für die Surplus Area im Standardfall}
  \label{fig:surplus-empfang}
\end{figure}

Aus \figref{fig:surplus-empfang} geht hervor, dass nur die vorgelagerte Prüfung des Datagramms selbst
Nutzdaten kosten kann: Eine ungültige \texttt{UDP Length} oder eine falsche UDP-Prüfsumme verwirft das
ganze Datagramm \cite[Sec.~10 und 14]{rfc9868}. Alle Prüfungen der Surplus Area treffen dagegen höchstens
die Optionen: Die beiden Größenfragen führen auf den harmlosen Ausgang links, die drei Prüfungen dahinter
auf den Fehlerausgang rechts; genau das ist die Grundregel aus \secref{subsec:surplus-aufbau} im Bild. Bei
der Auswertung selbst wird einzeln ignoriert, was nur lokal stört: eine unbekannte SAFE-Option ebenso wie
spätere Instanzen einer gewöhnlichen wiederholten Option. Die ganze Liste ist bei den strukturellen
Längenfehlern aus dem vorigen Absatz und bei mehrfachem FRAG ungültig; daneben darf der Empfänger sie
verwerfen, wenn verpflichtend zu unterstützende Optionen (im RFC \textit{must-support}, ausgenommen
\texttt{NOP} und \texttt{EOL}) nicht vor den übrigen SAFE-Optionen stehen, und er muss es, wenn seine
freiwillige Nullfüllungsprüfung hinter \texttt{EOL} ein Byte ungleich null findet
\cite[Sec.~10 und 11.1]{rfc9868}. Die
gestrichelte Randnotiz markiert die Sonderwege: Ein UDP-Fragment wird erst gepuffert und nach der
Reassemblierung zugestellt; UNSAFE-Optionen und strengere, von der Anwendung angeforderte Empfangsregeln
behandelt der folgende Unterabschnitt.

\subsection{Entwurfsprinzipien und Optionsklassen}
\label{subsec:surplus-prinzipien}

Wie dieser Bereich genutzt werden darf, bindet RFC~9868 an sechs Entwurfsprinzipien, die ein gemeinsames Ziel
haben: UDP soll durch die Optionen seinen Charakter nicht verlieren \cite[Sec.~6]{rfc9868}:

\begin{itemize}
    \item \textbf{Zustandslos:} Nötiger Zustand gehört in die Anwendung oder in eine Bibliothek, die in ihrem
    Auftrag arbeitet. Die einzige, ausdrücklich begrenzte Ausnahme ist die Zusammenführung von Fragmenten.

    \item \textbf{Unidirektional:} Die UDP-Schicht erzeugt nie von sich aus eine Antwort auf eine Option; ob
    und wann geantwortet wird, entscheidet die Anwendung oder eine Schicht beziehungsweise Bibliothek in
    ihrem Auftrag. Verfahren, die auf Antworten angewiesen sind, verlagert der RFC in eigene Dokumente. Als
    Einordnung dieser Arbeit, nicht als Begründung des RFC: Diese Zurückhaltung verhindert, dass schon der
    Grundmechanismus mit gefälschter Absenderadresse als Reflektor missbraucht wird; ein ausdrücklich
    aktiviertes Antwortverfahren muss diesen Missbrauch selbst abwehren, etwa durch die Ratenbegrenzung, die
    RFC~9869 für eigens als Antwort erzeugte Datagramme vorschreibt \cite[Sec.~3.1]{rfc9869}.

    \item \textbf{Keine eigene Längengrenze:} Für den Optionsraum gilt allein die Grenze des UDP-Pakets
    selbst; bei einem nicht fragmentierten Paket ist das der im IP-Datagramm verfügbare Platz. Feste
    Grenzen werden erfahrungsgemäß überschritten; der RFC verweist auf die Erfahrung mit TCP-Optionen
    (40~Byte Optionsraum \cite{rfc9293}) und IPv4-Adressen (32~Bit \cite{rfc791}). Grenzen darf eine
    Implementierung setzen, nicht die Spezifikation.

    \item \textbf{Kein Protokollersatz:} Optionen liefern Funktionen für den eigenen Verkehr einer Anwendung;
    sie sollen NTP, ICMP (insbesondere Echo) und Netzwerk-Testgeräte weder ersetzen noch nachbauen.

    \item \textbf{Rahmenwerk statt Protokoll:} RFC~9868 definiert Optionsformate auch dann, wenn sie noch
    kein vollständiges Verfahren ergeben; die Nutzung regeln andere Dokumente. Für REQ/RES übernimmt das
    RFC~9869 \cite{rfc9869}, für TIME bleibt sie bewusst offen. Das entspricht dem Muster von TCP, dessen
    Basisnorm den Optionsmechanismus festlegt, während einzelne Optionen in eigenen Dokumenten folgten.

    \item \textbf{Voreinstellung wie beim Altempfänger:} Empfangene Pakete werden standardmäßig auch dann
    zugestellt, wenn Prüfsummen-, Authentifizierungs- oder Entschlüsselungsprüfungen der Optionen scheitern;
    einzige Ausnahme sind UDP-Fragmente. Strengeres Verhalten muss die Anwendung ausdrücklich anfordern.
    Optionen sind ein Angebot, kein Filter.
\end{itemize}

Der gemeinsame Nenner der sechs Prinzipien: RFC~9868 legt die Optionsformate und ihre Grundverarbeitung
fest, aber kein vollständiges Anwendungsprotokoll. UDP bleibt ein minimales Transportprotokoll, und jede
zusätzliche Fähigkeit wird von der Anwendung ausdrücklich aktiviert \cite[Sec.~6]{rfc9868}.

An das letzte Prinzip knüpft die zentrale Zweiteilung der Optionen an, und sie folgt unmittelbar aus der
Abwärtskompatibilität: Weil ein Altempfänger die Surplus Area überliest, muss jede Option damit rechnen,
ignoriert zu werden. \textit{SAFE}-Optionen (Kind 0 bis 191) sind genau dafür entworfen: Ein Empfänger, der
sie nicht versteht, ignoriert sie, ohne dass sich die Bedeutung der Nutzdaten ändert. In diese Klasse
gehören die Ein-Byte-Codes \texttt{NOP} und \texttt{EOL} ebenso wie FRAG \cite[Sec.~10 und 11]{rfc9868}.

\textit{UNSAFE}-Optionen (Kind 192 bis 255) können dagegen die Nutzdaten verändern oder eine Änderung ihrer
Deutung signalisieren, etwa durch Kompression oder Verschlüsselung; ein Empfänger, der die Option nicht
versteht, würde die Nutzdaten falsch deuten. RFC~9868 löst das mit einer Transportbeschränkung:
UNSAFE-Optionen dürfen nur in UDP-Fragmenten auftreten, deren regulärer Nutzdatenteil leer ist; die
transportierten Daten liegen im Fragmentdatenbereich der Surplus Area hinter der Optionsliste
\cite[Sec.~11.4]{rfc9868}. Ein Altempfänger sieht dann nur ein leeres Datagramm und verwirft die Daten
still, statt sie falsch zu deuten. Ein optionsfähiger Empfänger, der eine UNSAFE-Option nicht unterstützt,
muss die reassemblierten Nutzdaten ebenso still verwerfen, und dasselbe gilt, wenn eine UNSAFE-Option
außerhalb eines Fragment- oder Reassemblierungskontexts erscheint; zugestellt wird in beiden Fällen
höchstens ein leeres Datagramm
\cite[Sec.~10, 12 und 14]{rfc9868}. Die Einzelbewertung der 67 Matrixzeilen steht in
Anhang~\ref{chap:anhang-konformitaet}; die Umsetzung der Optionen beschreibt \chapref{chap:entwurf}.

Zwei Folgen dieser Prinzipien verdienen eine eigene Erwähnung. Erstens verteilt die FRAG-Option ein
logisches Anwendungsdatagramm (im RFC \textit{user datagram}) künftig gegebenenfalls auf mehrere
IP-Datagramme. Zweitens nimmt der RFC einen ausdrücklich benannten Unterschied in Kauf: Ein Altempfänger
sieht jedes UDP-Fragment als leeres Datagramm, ein optionsfähiger Empfänger das reassemblierte Datagramm mit
Inhalt. Diese Zählungen gleicht der RFC nicht an, weil leere Datagramme als Lebenszeichen ohnehin
unzuverlässig sind \cite[Sec.~6 und 18]{rfc9868}.

\section{Betriebssystem und Netzpfad}
\label{sec:netzpfad}

Ob ein Datagramm mit Surplus Area sein Ziel erreicht, entscheidet sich an zwei Stellen außerhalb des
Protokolls: im Betriebssystem der beiden Endpunkte und auf dem Netzpfad dazwischen. Betrachtet wird dabei
durchgehend der Linux-Netzwerkstapel für IPv4, auf dem die Referenzimplementierung dieser Arbeit läuft;
andere Betriebssysteme und IPv6 bleiben ausgeklammert. Dieser Abschnitt stellt zunächst die zwei
Socket-Sichten vor, mit denen der Kernel UDP-Verkehr an Anwendungen übergibt, und anschließend die Akteure
des Pfades; am Ende fügt \figref{fig:akteurskette} beides zu einem Gesamtbild zusammen. Allgemeine
Netzwerkgrundlagen bleiben bewusst ausgespart: Es geht genau um die Kette, die die Messpfade dieser Arbeit
tatsächlich durchlaufen.

\paragraph{Zwei Socket-Sichten.}
Beim gewöhnlichen UDP-Socket übernimmt der Kernel die Protokollarbeit. Empfangsseitig prüft er Länge und
Prüfsumme, demultiplexiert nach Adresse, Port und Socketmerkmalen und liefert nur die Nutzdaten bis
\texttt{UDP Length} \cite{stephan2024packetpath}. Damit entspricht diese Sicht dem Altempfänger aus
\secref{sec:surplus-area}; getestete Standardsysteme schneiden die Surplus Area ab
\cite[Sec.~18]{rfc9868}. Sendeseitig baut der Kernel beide Header und lässt hinter den Nutzdaten keinen
Raum. Ein UDP-Socket kann die Surplus Area daher weder lesen noch erzeugen.

Der normale Kernelweg verläuft beim Senden von \texttt{write()} über \texttt{sock\_sendmsg()} und
\texttt{udp\_sendmsg()} zu \texttt{ip\_send\_skb()} und \texttt{dev\_queue\_xmit()};
\texttt{udp\_send\_skb()} schreibt dabei den UDP-Header. Empfangsseitig führt der Weg von
\path{netif_receive_skb()} über \texttt{ip\_rcv()}, \texttt{udp\_recvmsg()} und
\texttt{sock\_recvmsg()} zu \texttt{read()}; ausgeliefert wird erst nach der UDP-Prüfsumme
\cite{linux510udp,stephan2024packetpath}. Diese Folge berichtigt zwei Beschriftungen der Vorlage (dort
Abbildung~5): Auf der Socket-Schicht stehen \texttt{sock\_sendmsg()} und \texttt{sock\_recvmsg()}, und der
UDP-Sendepfad übergibt an \texttt{ip\_send\_skb()} statt \texttt{ip\_queue\_xmit()}.

Wer beides will, braucht die zweite Sicht: den \textit{Raw Socket}, den Linux nur Prozessen mit der
Berechtigung \texttt{CAP\_NET\_RAW} öffnet \cite{man7raw}. Er zweigt auf der IP-Schicht ab: Der Sender
speist dort ein selbst gebautes Datagramm ein, der Empfänger erhält vor dem UDP-Pfad eine Kopie; die
folgenden Abbildungen trennen beide Richtungen. Er arbeitet unterhalb von UDP direkt mit
IP-Datagrammen: Die Anwendung schreibt den UDP-Header selbst, und die IP-Längenangabe bemisst der Kernel an
der übergebenen Pufferlänge statt am UDP-Datagramm; alles, was die Anwendung hinter das UDP-Datagramm in
den Puffer legt, wird so zur Surplus Area \cite{man7raw}. Mit der Socket-Option \texttt{IP\_HDRINCL} baut
sie darüber hinaus den IP-Header selbst, statt ihn vom Kernel erzeugen zu lassen \cite{man7raw}; diesen Weg
nutzt die Referenzimplementierung, die damit das vollständige Datagramm selbst baut
(\secref{sec:technologieauswahl}). Die vertauschten Rollen beider Sendewege zeigt \figref{fig:sendepfade}:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      station/.style={feld, minimum width=4.7cm, minimum height=1.05cm, align=center, font=\scriptsize},
      akzentbox/.style={station, feldakzent},
      fluss/.style={->, draw=feldlinie, thin, rounded corners=3pt},
    ]
    \node[station] (ua) at (2.6,0) {Anwendung\\{\tiny übergibt nur die Nutzdaten}};
    \node[akzentbox] (ra) at (9.0,0)
      {Anwendung\\{\tiny baut IP-Header, UDP-Header,}\\{\tiny Nutzdaten und Surplus Area}};
    \node[akzentbox] (us) at (2.6,-1.85) {UDP-Socket\\{\tiny Kernel baut beide Header,}\\{\tiny Längen bündig}};
    \node[station] (rs) at (9.0,-1.85) {Raw Socket mit \texttt{IP\_HDRINCL}\\{\tiny Kernel übernimmt das Datagramm}};
    \node[station] (ip) at (5.8,-3.55) {IP-Schicht und Netzwerkkarte\\{\tiny Routing, netfilter, Draht}};
    \draw[fluss] (ua) -- (us);
    \draw[fluss] (ra) -- (rs);
    \draw[fluss] (us.south) |- (ip.west);
    \draw[fluss] (rs.south) |- (ip.east);
  \end{tikzpicture}
  \caption{Die zwei Sendepfade durch den Linux-Kernel \cite{stephan2024packetpath,man7raw}}
  \label{fig:sendepfade}
\end{figure}

Aus \figref{fig:sendepfade} geht hervor, dass die Arbeitsteilung kippt: Am UDP-Socket baut der Kernel beide
Header und bemisst die Längen bündig, am Raw Socket liefert die Anwendung das fertige Datagramm ab, und nur
auf dem Raw-Socket-Weg entsteht hinter den Nutzdaten Platz für eine Surplus Area. Der Unterbau bleibt
derselbe: Beide Wege münden in die IP-Schicht mit ihren netfilter-Stationen und enden an der Netzwerkkarte
\cite{stephan2024packetpath}.

Ganz entlässt der Kernel den Raw-Sender allerdings nicht aus seiner Obhut, und diese Restarbeit prägt den
Sendepfad der Implementierung. Routing, Nachbarauflösung und Link-Schicht bleiben Kernelsache, und im
übergebenen IP-Header trägt der Kernel \texttt{Total Length} und die IP-Header-Prüfsumme stets selbst ein;
UDP-Header und Surplus Area lässt er selbst dagegen unangetastet, die netfilter-Stationen dieses Wegs
können das Datagramm gleichwohl noch verwerfen oder verändern \cite{man7raw,stephan2024packetpath}.
Zugleich fragmentiert dieser Pfad nicht: Mit \texttt{IP\_HDRINCL} ist ein Datagramm auf die MTU des
Interfaces begrenzt, ein größerer Sendeaufruf schlägt fehl \cite{man7raw}. Für RFC~9868 ist das kein
Verlust: IP-Fragmentierung sollen UDP-Anwendungen ohnehin vermeiden (\secref{sec:ipv4})
\cite[Sec.~3.2]{rfc8085}, und für große Nutzlasten stellt der RFC stattdessen die FRAG-Option auf
Transportebene bereit.

Beim Empfang entscheidet sich, an welcher Stelle ein Datagramm geprüft wird; den Weg vom Draht zu den
beiden Sichten zeichnet \figref{fig:empfangspfad} nach:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      station/.style={feld, minimum width=3.05cm, minimum height=1.05cm, align=center, font=\scriptsize},
      akzentbox/.style={station, feldakzent},
      fluss/.style={->, draw=feldlinie, thin, rounded corners=3pt},
    ]
    \node[station] (nic) at (0,0) {Netzwerkkarte\\{\tiny Empfang vom Draht}};
    \node[station] (ips) at (3.55,0) {IP-Schicht\\{\tiny prüft den IP-Header}};
    \node[station] (nf) at (7.1,0) {netfilter\\{\tiny lokaler Paketfilter}};
    \node[station] (udp) at (10.65,0) {UDP-Pfad\\{\tiny prüft Länge, Prüfsumme, Port}};
    \node[akzentbox] (raw) at (7.1,-2.2)
      {Raw Socket\\{\tiny Kopie des ganzen Datagramms}\\{\tiny samt Surplus Area,}\\{\tiny ohne UDP-Prüfung}};
    \node[station] (usock) at (10.65,-2.2) {UDP-Socket\\{\tiny Nutzdaten bis \texttt{UDP Length}}};
    \draw[fluss] (nic) -- (ips);
    \draw[fluss] (ips) -- (nf);
    \draw[fluss] (nf) -- (udp);
    \draw[fluss] (nf.south) -- node[left, font=\tiny, text=feldlinie] {Kopie} (raw.north);
    \draw[fluss] (udp) -- (usock);
  \end{tikzpicture}
  \caption{Der Empfangspfad durch den Linux-Kernel \cite{stephan2024packetpath,man7raw}}
  \label{fig:empfangspfad}
\end{figure}

Aus \figref{fig:empfangspfad} geht hervor, dass sich der Weg hinter dem Paketfilter gabelt: Der Raw Socket
erhält eine Kopie des gesamten IP-Datagramms samt Surplus Area, und zwar bevor der UDP-Pfad des Kernels
Länge, Prüfsumme und Portzuordnung geprüft hat \cite{man7raw,stephan2024packetpath}. Ungeprüft heißt dabei:
frei von jeder UDP-Prüfung; den IP-Header hat die IP-Schicht zu diesem Zeitpunkt dagegen bereits geprüft,
wie die Abbildung zeigt. Ein Datagramm mit falscher UDP-Prüfsumme oder einer \texttt{UDP Length}, die
über die IP-Nutzlast hinausweist, erreicht den Raw Socket deshalb unverändert. Die Prüfungen, die der Kernel dem
UDP-Socket abnimmt, muss eine Raw-Empfangsschicht deshalb vollständig selbst ausführen, und zwar in der
Reihenfolge, die RFC~9868 vorgibt: erst Mindestlänge und Längenkonsistenz des UDP-Headers, dann die
UDP-Prüfsumme, erst danach Surplus-Ortung und OCS \cite[Sec.~10 und 14]{rfc9868}; wie die Implementierung
das umsetzt, zeigt \chapref{chap:entwurf}. Zwei Arbeiten behält der Kernel dagegen auch hier. Die
netfilter-Hooks bleiben beiden Sichten vorgeschaltet: Ein lokaler Paketfilter kann ein Datagramm
verwerfen, bevor irgendeine Anwendung es sieht \cite{stephan2024packetpath}. Und ankommende IP-Fragmente
setzt die IP-Schicht bereits vor der Zustellung wieder zusammen, auch für Raw Sockets; eine
Raw-Empfangsschicht muss die IPv4-Reassemblierung deshalb nicht nachbauen
\cite{man7raw,stephan2024packetpath}.

\paragraph{Akteure auf dem Netzpfad.}
Zwischen den Endpunkten kann das Datagramm auf \textit{Middleboxes} treffen. RFC~8085 nennt damit
Zwischensysteme wie NATs und Firewalls, die zusätzliche Funktionen ausführen und typischerweise Zustand je
Fluss führen \cite[Sec.~3.5]{rfc8085}. Für die Interpretation der späteren Messungen genügt der
Sammelbegriff nicht, denn die Akteure setzen an verschiedenen Stellen an und hinterlassen verschiedene
Spuren. Im Zugangsnetz ersetzt die NAT eines Heimrouters oder Hotspots Adressen und Ports und bindet
jeden Fluss an einen Zustandseintrag mit Zeitschranke; ihre Provider-Variante \textit{Carrier-Grade NAT}
(CGNAT) teilt dieselben öffentlichen Adressen zusätzlich unter vielen Kunden auf. Firewalls verwerfen an
Netzgrenzen und Endsystemen nach Regelwerk, etwa anhand von Ports und Protokollen. Leicht zu übersehen
sind die letzten beiden Akteure: die Virtualisierungsschicht eines Testrechners, deren eigener Netz- und
NAT-Stack Kopffelder auf erwartete Normwerte umschreiben kann, und das Transitnetz selbst, die AS-Kette
zwischen den Zugangsnetzen, in der einzelne Netze filtern.

Ob ein einzelner Akteur UDP Options kennt, lässt sich von außen nicht feststellen. Unbekannte
Zwischensysteme können eine Surplus Area unverändert weiterleiten, verändern, entfernen oder das gesamte
Datagramm verwerfen. Zustandsbehaftete NATs und Firewalls können dabei je Richtung und Messzeitpunkt
unterschiedlich reagieren \cite[Sec.~3.5]{rfc8085}. Eine Kapselung, etwa in einen WireGuard-Tunnel,
verbirgt das innere Datagramm vor den Akteuren zwischen den Tunnelendpunkten, die nur noch das äußere
Tunnelpaket behandeln. Welche dieser Eingriffe auf den untersuchten Pfaden beobachtet wurden, bewertet
\chapref{chap:evaluation}.

Diese Kette erklärt zugleich, warum RFC~9868 seine Optionen ausgerechnet in einem Bereich ablegt, den
Altempfänger nie lesen. Weil Middleboxes bevorzugt durchlassen, was sie kennen, weichen
Protokollerweiterungen in Bereiche aus, die Bestandssysteme nicht deuten; dieses Erstarren des Netzes unter
seinen Zwischensystemen wird als \textit{Ossifikation} bezeichnet. Unsichtbar ist die Surplus Area
allerdings nur für die Endpunkte alter Anwendungen; für die Geräte auf dem Pfad bleibt sie ohne
Verschlüsselung sichtbar \cite[Sec.~16]{rfc9868}, und RFC~9868 rechnet ausdrücklich mit deren Eingriffen:
Fehlerhaft rechnende Middleboxes begründen Details der OCS-Berechnung (\secref{sec:surplus-area})
\cite[Sec.~9]{rfc9868}, und UDP-Relays können eine Surplus Area beim Weiterleiten vollständig abstreifen
\cite[Sec.~25.6]{rfc9868}.

Alle Stationen zusammen ordnet \figref{fig:akteurskette} zu einem Messmodell des Weges von der sendenden
Anwendung bis zu dem Raw-Empfänger, der die Surplus Area auswertet; je nach Pfad können einzelne Stationen
fehlen oder zusammenfallen.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      station/.style={feld, minimum width=3.05cm, minimum height=1.05cm, align=center, font=\scriptsize},
      fremd/.style={station, feldwarn},
      fluss/.style={->, draw=feldlinie, thin, rounded corners=3pt},
    ]
    \node[station] (anw)   at (0,0)        {Anwendung\\{\tiny übergibt Nutzdaten}};
    \node[station] (sock)  at (3.55,0)     {Socket-Schicht\\{\tiny UDP- oder Raw-Pfad, netfilter}};
    \node[fremd]   (virt)  at (7.1,0)      {Virtualisierung\\{\tiny VM-NAT, venet}};
    \node[fremd]   (zug)   at (10.65,0)    {Zugangsnetz\\{\tiny NAT, CGNAT}};
    \node[fremd]   (trans) at (0,-2.2)     {Transitnetz\\{\tiny AS-Kette}};
    \node[fremd]   (ingr)  at (3.55,-2.2)  {Ingress-Filter\\{\tiny Firewall des Ziels}};
    \node[station] (kern)  at (7.1,-2.2)   {Empfänger-Kernel\\{\tiny netfilter vor Zustellung}};
    \node[station] (raw)   at (10.65,-2.2) {Raw-Empfänger\\{\tiny liest die Surplus Area}};
    \draw[fluss] (anw) -- (sock);
    \draw[fluss] (sock) -- (virt);
    \draw[fluss] (virt) -- (zug);
    \draw[fluss] (zug.south) -- (10.65,-1.1) -- (0,-1.1) -- (trans.north);
    \draw[fluss] (trans) -- (ingr);
    \draw[fluss] (ingr) -- (kern);
    \draw[fluss] (kern) -- (raw);
  \end{tikzpicture}
  \caption{Messmodell des Datagrammwegs}
  \label{fig:akteurskette}
\end{figure}

Aus \figref{fig:akteurskette} geht hervor, dass zwischen Socket-Schicht und Empfänger-Kernel bis zu vier
Stationen liegen, die das Datagramm außerhalb der Endpunkte berühren: Sie greifen teils mit Zustand ein und
sind von außen nicht direkt beobachtbar. Für die Feldmessungen folgt daraus die Interpretationsregel dieser
Arbeit: Geht ein optionsbehaftetes Datagramm verloren oder kommt es verändert an, ist der Befund zuerst
dieser Kette zuzuordnen, bevor er der Implementierung oder dem Standard angelastet wird. Messpunkte an
beiden Enden grenzen ihn dabei auf ein Intervall der Kette ein; welches Gerät eingreift, bleibt ohne
zusätzliche Messpunkte oder unabhängige Evidenz offen (\chapref{chap:evaluation}).

Wireshark und tshark 4.6 dekodieren RFC~9868 nicht: Ihre UDP-Ansicht endet ohne Feld, Trailer oder Warnung
an \texttt{UDP Length}. Die Kampagnen benötigen deshalb einen eigenen Prüfer für PCAP-Dateien
(\chapref{chap:evaluation}); der fehlende Dissektor zeigt zugleich die geringe bisherige
Werkzeugunterstützung.

Die Auswertungsmethode folgt in \chapref{chap:methodik}.
