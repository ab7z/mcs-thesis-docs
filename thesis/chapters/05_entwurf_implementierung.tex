% =============================================================================
% Kapitel 5: Entwurf und Implementierung
% =============================================================================

\chapter{Entwurf und Implementierung}
\label{chap:entwurf}

Dieses Kapitel beschreibt die Referenzimplementierung und das Messinstrument. Es folgt den Datenpfaden
der Bibliothek: von der Architektur über das Senden und Empfangen bis zu den Pflichtoptionen, FRAG und
den Betriebsgrenzen.

\section{Architektur}
\label{sec:architektur}

Die Implementierung umfasst eine Bibliothek sowie die Messprogramme \texttt{udpopt-send} und
\texttt{udpopt-recv}, mit denen die Messungen in \chapref{chap:evaluation} laufen. \figref{fig:komponenten}
zeigt ihre Aufgaben und ausgewählte Abhängigkeiten.

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      >={Stealth[length=5pt, width=4pt]},
      modul/.style={draw=sqblaurand, fill=sqblau, rounded corners=3pt, semithick, align=center,
        font=\scriptsize\linespread{1.0}\selectfont, inner sep=4pt, minimum width=3.0cm,
        minimum height=1.05cm},
      akzent/.style={modul, draw=sqgoldrand, fill=sqgold},
      quer/.style={draw=sqgrau, fill=sqhell, rounded corners=3pt, semithick, align=center,
        font=\scriptsize\linespread{1.0}\selectfont, inner sep=4pt, minimum width=2.4cm,
        minimum height=1.05cm},
      betriebssystem/.style={draw=sqgrau, fill=sqhell, rounded corners=3pt, thin, align=center,
        font=\tiny\linespread{1.0}\selectfont, inner sep=3pt, minimum height=0.8cm},
      import/.style={->, draw=sqdunkel, thin, rounded corners=2pt},
      oskontakt/.style={draw=sqgrau, dashed, thin},
      anno/.style={font=\tiny\linespread{1.0}\selectfont, text=feldlinie, align=center},
    ]
    % Spalten: frag 1.3 | bin, api, recv, options, wire 5.3 | socket 9.0 | error, model 12.5
    % Zeilen: bin 7.0 | api 5.3 | frag, recv, socket 3.6 | options 1.9 | wire, OS 0.2
    \node[modul] (bin) at (5.3,7.0)
      {\textbf{bin}: udpopt-send, udpopt-recv\\ zusammen: direkte Importe\\ aus allen acht Modulen};
    \node[modul] (api) at (5.3,5.3) {\textbf{api} (\texttt{Peer})\\ importiert die sieben\\ übrigen Module};
    \node[modul] (frag) at (1.3,3.6) {\textbf{frag}\\ split, reassembly};
    \node[modul] (recv) at (5.3,3.6) {\textbf{recv}\\ pipeline};
    \node[akzent] (socket) at (9.0,3.6) {\textbf{socket}\\ send, recv};
    \node[modul] (options) at (5.3,1.9) {\textbf{options}\\ kind, parse, serialize,\\ ocs, typed};
    \node[modul, minimum width=4.8cm] (wire) at (5.3,0.2) {\textbf{wire}\\ ip, udp, checksum, surplus};
    \node[quer] (error) at (12.8,5.0) {\textbf{error}\\ Fehlertypen};
    \node[quer] (model) at (12.8,3.6) {\textbf{model}\\ Konstanten, Grenzen};
    \node[anno] at (12.8,2.2) {von allen übrigen\\ Modulen importiert,\\ selbst ohne Bezüge zu\\ anderen Modulen};
    \node[betriebssystem] (rawsock) at (11.2,0.2)
      {OS: Sockets\\ (SOCK\_RAW; Senden mit\\ IP\_HDRINCL; Empfang\\ zusätzlich mit UDP-Socket)};
    \node[betriebssystem] (urandom) at (0.7,0.2) {OS:\\ /dev/urandom};
    % Importe entlang der Mittelachse
    \draw[import] (bin) -- (api);
    \draw[import] (api) -- (recv);
    \draw[import] (recv) -- (options);
    \draw[import] (options) -- (wire);
    % api zu den Seitenmodulen, Querimporte in der mittleren Zeile
    \draw[import] (api.west) -| (frag.north);
    \draw[import] (api.east) -| (socket.north);
    \draw[import] (recv.west) -- (frag.east);
    \draw[import] (socket.west) -- (recv.east);
    % Importe von links: frag zu options, recv an options vorbei zu wire
    \draw[import] ([xshift=0.6cm]frag.south) |- (options.west);
    \draw[import] ([yshift=-0.3cm]recv.west) -| ([xshift=0.3cm]wire.north west);
    % Importe von rechts: socket zu options und zu wire
    \draw[import] ([xshift=-0.8cm]socket.south) |- (options.east);
    \draw[import] (socket.south) |- (wire.east);
    % Betriebssystemkontakte
    \draw[oskontakt] ([xshift=0.8cm]socket.south) -- ([xshift=0.8cm]socket.south |- rawsock.north);
    \draw[oskontakt] ([xshift=-0.6cm]frag.south) -- ([xshift=-0.6cm]frag.south |- urandom.north);
    \node[anno, anchor=north west, align=left] at (-0.2,-0.75)
      {Pfeil: ausgewählte Importbeziehung, vom importierenden zum importierten Modul. Gestrichelte Linie:\\
       ausgewählter Betriebssystemkontakt. Hervorgehoben: das Modul mit den Netz-Systemaufrufen.};
  \end{tikzpicture}
  \caption{Module der Bibliothek, Messprogramme und ihre Abhängigkeiten}
  \label{fig:komponenten}
\end{figure}

Die Kanten zeigen ausgewählte tatsächliche Importe und keine strikte Schichtung. \texttt{wire}
verarbeitet IPv4- und UDP-Köpfe sowie Prüfsummen und lokalisiert die Surplus Area. \texttt{options} serialisiert und
liest den Optionsstrom. \texttt{recv} führt für eingehende UDP-Datagramme einschließlich ihrer UDP-Optionen
die von RFC~9868 vorgegebenen Prüfschritte nacheinander aus \cite[Sec.~14]{rfc9868}. \texttt{frag} zerlegt
und reassembliert Datagramme, und \texttt{api} bündelt diese Funktionen im Typ \texttt{Peer}.
\texttt{error} und \texttt{model} stellen gemeinsame Fehlertypen, Konstanten und Grenzen bereit.

Im Produktionsquelltext unter \texttt{src/} liegen die Netz-Systemaufrufe der Bibliothek in
\texttt{socket}; dort stehen auch die einzigen zwei \texttt{unsafe}-Blöcke. Beim Empfang bindet
\texttt{socket} zusätzlich einen gewöhnlichen UDP-Socket auf den Zielport; sonst würde der Kernel
eintreffende Datagramme per ICMP als unerreichbar zurückmelden.

Für die Kennungsgeneratoren von \texttt{Peer} und \texttt{udpopt-send} liest \texttt{frag} beim Aufbau
einmalig vier Bytes aus \texttt{/dev/urandom}, falls kein Startwert vorgegeben ist. Die Kennung ist das Feld
\texttt{Identification} der FRAG-Option: Zusammen mit Adressen und Ports zeigt sie dem Empfänger, welche
Fragmente zusammengehören \cite[Sec.~11.4]{rfc9868}. Der zufällige Start macht es unwahrscheinlich, dass
ein neu gestarteter Sender Kennungen wiederholt, die ein Empfänger noch gespeichert hat
(\secref{sec:optionsverarbeitung}).

\texttt{api} und \texttt{socket} verwenden die monotone Uhr. Sie misst nur, wie viel Zeit vergangen ist,
und wird durch Änderungen der Systemzeit nicht verstellt. \texttt{api} liefert damit den Zeitpunkt, mit dem
\texttt{frag} unvollständige Fragmentgruppen nach Fristablauf verwirft. \texttt{socket} kann damit die
gesamte Empfangsfrist zuverlässig einhalten. Die Protokolllogik in \texttt{recv} und \texttt{frag} greift
selbst weder auf das Netz noch auf die Uhr zu; sie erhält Zeitpunkte und Kennungen als Parameter. So lässt
sich jede Protokollentscheidung ohne Netz und ohne erhöhte Rechte prüfen (\secref{subsec:pruefmittel}).
Beide Messprogramme schreiben auf die Konsole. Nur \texttt{udpopt-send} kann zusätzlich eine Manifestdatei
fortschreiben, je Sendevorgang eine Zeile im JSON-Format (JSONL).

Die folgenden Abschnitte nutzen drei Pflichtoptionen als roten Faden. Die Option zur maximalen
Datagrammgröße (MDS) veranschaulicht den Sendepfad, die zusätzliche Nutzdatenprüfsumme (APC) den
Empfangspfad und die Fragmentierungsoption (FRAG) die Zerlegung und spätere Zusammensetzung großer
Datagramme.

\section{Sendepfad und gemeinsamer Optionskern}
\label{sec:sendepfad}

Beim Senden baut die Bibliothek aus Nutzdaten und Optionen ein vollständiges IPv4-Datagramm und übergibt
es dem Raw Socket. Als Leitbeispiel dient eine Anwendung, die drei Nutzdatenbytes \texttt{odd} verschickt
und ihrem Gegenüber dabei mitteilt, wie große Datagramme sie selbst empfangen kann. Dafür dient die Option
Maximum Datagram Size (MDS). Im Beispiel nimmt die Anwendung an, dass IPv4-Datagramme bis 1.500~Byte ohne
IP-Fragmentierung bei ihr ankommen. Nach Abzug des 20~Byte langen IPv4-Kopfs und des 8~Byte langen
UDP-Kopfs trägt MDS deshalb den 16-Bit-Wert $1.500 - 20 - 8 = 1.472$~Byte
\cite[Sec.~11.5]{rfc9868}.

MDS ist nur ein Hinweis, keine bekannte Pfadgrenze, und lässt den Platz für Optionen unberücksichtigt.
Über seine Nutzung und den nötigen Abzug entscheidet der Aufrufer. Die Bibliothek meldet einen empfangenen
Wert daher nur. Für das Beispiel übergibt die Anwendung dem Sendepfad die Nutzdaten und genau eine
MDS-Option. \figref{fig:paketentstehung} zeigt die drei Zustände bis zum fertigen Datagramm.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      >={Stealth[length=5pt, width=4pt]},
      zustand/.style={font=\small, text=feldlinie, anchor=west},
      feldgross/.style={font=\footnotesize, align=center},
      bytes/.style={font=\footnotesize\ttfamily, text=feldlinie},
      uebergang/.style={->, draw=feldlinie, semithick},
      uebergangtext/.style={font=\footnotesize, text=feldlinie, anchor=west, align=left},
    ]
    % Zustand 1: Übergabe der Anwendung
    \node[zustand] at (0,0.45) {Zustand 1: Die Anwendung übergibt Nutzdaten und Optionswunsch};
    \draw[feld] (0,-0.85) rectangle (3.6,0);
    \node[feldgross] at (1.8,-0.425) {Nutzdaten \texttt{odd}, 3 Byte};
    \draw[feldakzent] (4.0,-0.85) rectangle (8.4,0);
    \node[feldgross] at (6.2,-0.425) {Option MDS, Wert 1472};
    % Übergang 1
    \draw[uebergang] (9.6,-0.3) -- (9.6,-2.55);
    \node[uebergangtext] at (9.9,-1.45) {Optionskern:\\ prüfen, ordnen, auslegen};
    % Zustand 2: Optionsbereich
    \node[zustand] at (0,-1.95) {Zustand 2: fertiger Optionsbereich, 8 Byte};
    \draw[feldakzent] (0,-3.25) rectangle (2.9,-2.4);
    \node[feldgross] at (1.45,-2.825) {OCS-Platzhalter};
    \draw[feldakzent] (2.9,-3.25) rectangle (5.6,-2.4);
    \node[feldgross] at (4.25,-2.825) {MDS};
    \draw[feldakzent] (5.6,-3.25) rectangle (6.9,-2.4);
    \node[feldgross] at (6.25,-2.825) {EOL};
    \draw[feldakzent] (6.9,-3.25) rectangle (8.4,-2.4);
    \node[feldgross] at (7.65,-2.825) {Füllbyte};
    \node[bytes] at (1.45,-3.55) {00 00};
    \node[bytes] at (4.25,-3.55) {04 04 05 c0};
    \node[bytes] at (6.25,-3.55) {00};
    \node[bytes] at (7.65,-3.55) {00};
    % Übergang 2
    \draw[uebergang] (9.6,-3.4) -- (9.6,-5.05);
    \node[uebergangtext] at (9.9,-4.2) {Sendepfad: Köpfe, Pad-Byte,\\ UDP-Prüfsumme, OCS};
    % Zustand 3: Datagramm
    \node[zustand] at (0,-4.75) {Zustand 3: vollständiges IPv4-Datagramm, 40 Byte};
    \draw[feld] (0,-6.05) rectangle (3.2,-5.2);
    \node[feldgross] at (1.6,-5.625) {IP-Kopf};
    \draw[feld] (3.2,-6.05) rectangle (5.2,-5.2);
    \node[feldgross] at (4.2,-5.625) {UDP-Kopf};
    \draw[feld] (5.2,-6.05) rectangle (6.3,-5.2);
    \node[feldgross] at (5.75,-5.625) {\texttt{odd}};
    \draw[feldoptional] (6.3,-6.05) rectangle (7.0,-5.2);
    \node[feldgross] at (6.65,-5.625) {\texttt{00}};
    \draw[feldakzent] (7.0,-6.05) rectangle (8.5,-5.2);
    \node[feldgross] at (7.75,-5.625) {OCS\\ \texttt{f6 32}};
    \draw[feldakzent] (8.5,-6.05) rectangle (11.0,-5.2);
    \node[feldgross] at (9.75,-5.625) {MDS\\ \texttt{04 04 05 c0}};
    \draw[feldakzent] (11.0,-6.05) rectangle (11.9,-5.2);
    \node[feldgross] at (11.45,-5.625) {EOL};
    \draw[feldakzent] (11.9,-6.05) rectangle (12.7,-5.2);
    \node[feldgross] at (12.3,-5.625) {\texttt{00}};
    \node[bytes] at (1.6,-6.35) {20};
    \node[bytes] at (4.2,-6.35) {8};
    \node[bytes] at (5.75,-6.35) {3};
    \node[bytes] at (6.65,-6.35) {1};
    \node[bytes] at (7.75,-6.35) {2};
    \node[bytes] at (9.75,-6.35) {4};
    \node[bytes] at (11.45,-6.35) {1};
    \node[bytes] at (12.3,-6.35) {1};
    \draw[klammer] (6.3,-6.7) -- (3.2,-6.7)
      node[midway, below=6pt, font=\footnotesize, text=feldlinie] {\texttt{UDP Length} = 11};
    \draw[klammer] (12.7,-6.7) -- (6.3,-6.7)
      node[midway, below=6pt, font=\footnotesize, text=feldlinie] {Surplus Area, 9 Byte};
    \draw[klammer] (12.7,-7.6) -- (0,-7.6)
      node[midway, below=6pt, font=\footnotesize, text=feldlinie] {\texttt{IP Total Length} = 40};
  \end{tikzpicture}
  \caption{Entstehung des Beispieldatagramms mit MDS in drei Zuständen}
  \label{fig:paketentstehung}
\end{figure}

Den Optionsbereich aus Zustand~2 erzeugt der gemeinsame Optionskern. Er prüft zuerst, ob die gewählte
Option unterstützt wird und ihre Länge stimmt. Bei mehreren Einträgen stellt er die Pflichtoptionen vor
andere SAFE-Optionen und legt danach die Bytes in einer festen eigenen Reihenfolge ab
\cite[Sec.~10]{rfc9868}. Im Beispiel entsteht so der acht Byte lange Bereich aus Zustand~2; der MDS-Wert
\texttt{0x05C0} entspricht den angekündigten 1.472~Byte. Beim Empfang liest derselbe Optionskern diesen
Aufbau wieder zurück.

Sobald der Optionsbereich feststeht, kann der Sendepfad die endgültigen Längen bestimmen. Die drei
Nutzdatenbytes ergeben zusammen mit dem acht Byte langen UDP-Kopf eine \texttt{UDP Length} von 11.
Hinter dem 20~Byte langen IP-Kopf beginnt die Surplus Area deshalb bei Byte 31. Da dieser Offset ungerade
ist, fügt die Bibliothek vor dem OCS ein Nullbyte zur Ausrichtung ein
(\secref{subsec:surplus-aufbau}). Mit diesem Pad-Byte und dem acht Byte langen Optionsbereich wächst das
IPv4-Datagramm auf 40~Byte. Die UDP-Prüfsumme schützt den UDP-Kopf und die Nutzdaten, aber nicht die
Surplus Area.

Das OCS wird zuletzt eingesetzt, weil erst jetzt der vollständige Optionsbereich und die gesamte
Surplus-Länge feststehen. Mit dem noch leeren OCS-Feld ergeben die vier Wörter des Optionsbereichs die
Summe \texttt{0x09C4}. Hinzu kommt die gesamte Surplus-Länge von neun Byte, also \texttt{0x0009}; das
Pad-Byte selbst gehört nicht zu den summierten Wörtern. Nach der Invertierung entsteht \texttt{0xF632}.
Der Empfänger erhält mit \texttt{0x09C4 + 0xF632 + 0x0009 = 0xFFFF} das erwartete Ergebnis
(\secref{subsec:surplus-ocs}). Ergibt eine verwendete Prüfsumme rechnerisch null, schreibt die Bibliothek
stattdessen \texttt{0xFFFF}. Für UDP verlangt dies RFC~768. Beim OCS folgt es daraus, dass
\texttt{0x0000} ein ungenutztes OCS kennzeichnet \cites{rfc768}[Sec.~9]{rfc9868}.

Mit \texttt{IP\_HDRINCL} übergibt die Bibliothek anschließend das vollständige Datagramm an den Kernel;
dieser ergänzt nur IPv4-Gesamtlänge, IP-Kopfprüfsumme und eine noch leere IPv4-Identifikation
\cite{man7raw}. UDP-Kopf und Surplus Area bleiben auf diesem Weg unverändert; selbst
die vom RFC erlaubte Kombination aus UDP-Prüfsumme null und OCS null gelangt so auf die Leitung
(\secref{sec:soll-ist}). Spätere Paketfilter können das Datagramm weiterhin verändern oder verwerfen
(\secref{sec:netzpfad}).

Eine zweite Grenze betrifft die Größe: Auf diesem Pfad fragmentiert der Kernel nicht, ein Datagramm über
der MTU der Schnittstelle scheitert mit \texttt{EMSGSIZE} \cite{man7raw}. Größere Nachrichten muss die
Bibliothek deshalb selbst mit FRAG zerlegen.

\section{Empfangspfad und öffentliche Schnittstelle}
\label{sec:empfangspfad}

Der Raw Socket erhält das IPv4-Datagramm, bevor der Kernel UDP-Länge, Prüfsumme und Ports prüft. Deshalb
kontrolliert die Bibliothek diese Angaben selbst, bevor sie die Surplus Area und deren Optionen auswertet.

Als Leitbeispiel dient ein Datagramm mit den neun Nutzdatenbytes \texttt{123456789} und einer Additional
Payload Checksum (APC). Die Option enthält eine 32-Bit-Prüfsumme nach dem Verfahren CRC32c über die
Nutzdaten, hier \texttt{0xE3069283}. Der Sender trägt den Wert in Netzwerkbytefolge hinter Kind 2 und
Länge 6 ein \cite[Sec.~11.3]{rfc9868}.

Wichtig ist die Aufgabenteilung. Die APC schützt nur die Nutzdaten. Pseudo-Header und UDP-Kopf bleiben
Aufgabe der UDP-Prüfsumme, sofern diese verwendet wird. Für die Surplus Area greifen zwei andere
Prüfungen zusammen: Das Pad vor dem ausgerichteten OCS wird getrennt auf null geprüft. Die OCS-Rechnung
beginnt am OCS-Feld und reicht bis zum Ende der Surplus Area; zusätzlich geht die Länge des gesamten
Bereichs einschließlich Pad in die Rechnung ein. Die APC ersetzt keine dieser Prüfungen. Sie soll
Nutzdatenfehler erkennen, die der UDP-Prüfsumme entgehen \cite[Sec.~8, 9 und 11.3]{rfc9868}.
\figref{fig:empfangspipeline}~(a) zeigt die Schutzbereiche; Teil~(b) zeigt die Folgen eines Fehlers.

\begin{figure}[H]
  \centering
  {\small (a) Schutzbereiche im Beispieldatagramm\par}\vspace{2pt}
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      feldgross/.style={font=\footnotesize, align=center},
      bytes/.style={font=\footnotesize\ttfamily, text=feldlinie},
      deckung/.style={font=\footnotesize, text=feldlinie, align=center},
    ]
    \draw[feld] (0,-0.85) rectangle (2.8,0);
    \node[feldgross] at (1.4,-0.425) {IP-Kopf};
    \draw[feld] (2.8,-0.85) rectangle (4.8,0);
    \node[feldgross] at (3.8,-0.425) {UDP-Kopf};
    \draw[feld] (4.8,-0.85) rectangle (8.6,0);
    \node[feldgross] at (6.7,-0.425) {Nutzdaten \texttt{123456789}};
    \draw[feldoptional] (8.6,-0.85) rectangle (9.3,0);
    \node[feldgross] at (8.95,-0.425) {\texttt{00}};
    \draw[feldakzent] (9.3,-0.85) rectangle (10.4,0);
    \node[feldgross] at (9.85,-0.425) {OCS};
    \draw[feldakzent] (10.4,-0.85) rectangle (13.0,0);
    \node[feldgross] at (11.7,-0.425) {APC\\ \texttt{e3 06 92 83}};
    \draw[feldakzent] (13.0,-0.85) rectangle (13.9,0);
    \node[feldgross] at (13.45,-0.425) {EOL};
    \draw[feldakzent] (13.9,-0.85) rectangle (14.7,0);
    \node[feldgross] at (14.3,-0.425) {\texttt{00}};
    \node[bytes] at (1.4,-1.15) {20};
    \node[bytes] at (3.8,-1.15) {8};
    \node[bytes] at (6.7,-1.15) {9};
    \node[bytes] at (8.95,-1.15) {1};
    \node[bytes] at (9.85,-1.15) {2};
    \node[bytes] at (11.7,-1.15) {6};
    \node[bytes] at (13.45,-1.15) {1};
    \node[bytes] at (14.3,-1.15) {1};
    \draw[klammer] (2.8,0.12) -- (8.6,0.12)
      node[midway, above=6pt, font=\footnotesize, text=feldlinie] {\texttt{UDP Length} = 17};
    \draw[klammer] (8.6,0.12) -- (14.7,0.12)
      node[midway, above=6pt, font=\footnotesize, text=feldlinie] {Surplus Area, 11 Byte};
    \draw[klammer] (0,0.85) -- (14.7,0.85)
      node[midway, above=6pt, font=\footnotesize, text=feldlinie] {\texttt{IP Total Length} = 48};
    \draw[draw=feldlinie, thin] (2.8,-1.7) -- (8.6,-1.7);
    \draw[draw=feldlinie, thin] (2.8,-1.62) -- (2.8,-1.78);
    \draw[draw=feldlinie, thin] (8.6,-1.62) -- (8.6,-1.78);
    \node[deckung] at (5.7,-2.0) {UDP-Prüfsumme: UDP-Kopf und Nutzdaten, dazu der Pseudo-Header};
    \draw[draw=akzent, semithick] (4.8,-2.55) -- (8.6,-2.55);
    \draw[draw=akzent, semithick] (4.8,-2.47) -- (4.8,-2.63);
    \draw[draw=akzent, semithick] (8.6,-2.47) -- (8.6,-2.63);
    \node[deckung, text=akzent] at (6.7,-2.85) {APC: nur die Nutzdaten};
    \draw[draw=feldlinie, thin] (9.3,-3.4) -- (14.7,-3.4);
    \draw[draw=feldlinie, thin] (9.3,-3.32) -- (9.3,-3.48);
    \draw[draw=feldlinie, thin] (14.7,-3.32) -- (14.7,-3.48);
    \node[deckung, anchor=east] at (14.7,-3.7)
      {OCS: Bytes ab OCS; zusätzlich die gesamte Surplus-Länge};
  \end{tikzpicture}
  \par\vspace{10pt}
  {\small (b) Folgen eines Fehlers bei einem gewöhnlichen Datagramm\par}\vspace{2pt}
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      >={Stealth[length=5pt, width=4pt]},
      befund/.style={draw=feldlinie, fill=feldgrau, rounded corners=3pt, thin, align=center,
        font=\footnotesize, minimum width=6.2cm, minimum height=1.0cm, inner sep=4pt},
      verwurf/.style={draw=warnton, fill=warnhell, rounded corners=3pt, semithick, align=center,
        font=\footnotesize, minimum width=7.0cm, minimum height=1.0cm, inner sep=4pt},
      ohneopt/.style={draw=feldlinie, fill=white, rounded corners=3pt, semithick, align=center,
        font=\footnotesize, minimum width=7.0cm, minimum height=1.0cm, inner sep=4pt},
      apcfehl/.style={draw=akzent, fill=akzenthell, rounded corners=3pt, semithick, align=center,
        font=\footnotesize, minimum width=7.0cm, minimum height=1.0cm, inner sep=4pt},
      pfeil/.style={->, draw=feldlinie, semithick},
    ]
    \node[befund] (b1) at (3.2,0) {IPv4-Kopf, \texttt{UDP Length} oder\\ UDP-Prüfsumme ungültig};
    \node[verwurf] (f1) at (10.9,0) {Datagramm verworfen};
    \node[befund] (b2) at (3.2,-1.35) {Pad oder OCS fehlerhaft;\\ Optionsbereich strukturell ungültig};
    \node[ohneopt] (f2) at (10.9,-1.35) {Nutzdaten ohne Optionen zugestellt};
    \node[befund] (b3) at (3.2,-2.7) {APC stimmt nicht mit\\ den Nutzdaten überein};
    \node[apcfehl] (f3) at (10.9,-2.7) {Nutzdaten zugestellt, APC als Fehlschlag gemeldet\\
      (nur eine strenge Empfangsrichtlinie filtert aus)};
    \draw[pfeil] (b1) -- (f1);
    \draw[pfeil] (b2) -- (f2);
    \draw[pfeil] (b3) -- (f3);
  \end{tikzpicture}
  \caption{Schutzbereiche der APC und Folgen von Empfangsfehlern}
  \label{fig:empfangspipeline}
\end{figure}

Ein früher Vorversuch zeigte, warum diese eigene Prüfung nötig ist. Von 97 erzeugten Fällen erreichten 69
den Raw-Empfang. Die übrigen 28 überschritten die MTU von 1.500~Byte und scheiterten bereits beim Senden
mit \texttt{EMSGSIZE}. Unter den empfangenen Fällen waren vier gezielte
Grenzfälle: eine UDP-Prüfsumme von null, eine absichtlich falsche UDP-Prüfsumme, eine
\texttt{UDP Length} von 500 bei nur 48 verfügbaren Bytes und eine \texttt{UDP Length} von 4, die kürzer
als der UDP-Kopf war. Der Raw Socket lieferte alle vier Fälle an den Benutzerraum. Der Versuch erfasste
dabei die Ankunft, die Längenfelder und die Surplus-Bytes, aber keinen vollständigen Vergleich mit dem
Sendepuffer. Außerdem enthielt der Sendepuffer in 42 der 69 empfangenen Fälle einen falschen Wert für
\texttt{IP Total Length}. Der Kernel ersetzte alle 42 Werte vor der Übertragung durch die tatsächliche
Pufferlänge.

Zuerst prüft die Bibliothek das Datagramm selbst. Der IPv4-Kopf muss gültig sein. \texttt{UDP Length}
muss mindestens 8 betragen und darf die IP-Nutzlast nicht überschreiten. Eine UDP-Prüfsumme ungleich
null muss stimmen; eine gespeicherte Null bleibt unter IPv4 zulässig (\secref{subsec:udp-struktur}).
Scheitert eine dieser Prüfungen, wird das gesamte Datagramm verworfen
\cite[Sec.~10 und 14]{rfc9868}.

Danach sucht die Bibliothek die Surplus Area. Im Beispiel liegen zwischen \texttt{UDP Length} 17 und
\texttt{IP Total Length} 48 elf Byte, beginnend bei Byte 37. Der Start ist ungerade, also ist das erste
Byte das Pad vor dem ausgerichteten OCS. Passt das OCS nicht mehr vollständig in den Überschuss, sind
keine Optionen vorhanden. Das ist kein Fehler: Die Nutzdaten werden ohne Optionen zugestellt, und der
OCS-Bericht meldet ein fehlendes OCS.

Anschließend prüft die Bibliothek den Optionsbereich. Das Pad-Byte vor dem OCS muss null sein. Ein OCS
ungleich null muss zusammen mit den übrigen Optionsbytes und dem Längensummanden aufgehen. Der Nullwert
bedeutet nur dann ein zulässig ungenutztes OCS, wenn auch die UDP-Prüfsumme null ist. Scheitert eine
dieser Prüfungen, verwirft die Bibliothek bei einem gewöhnlichen Datagramm nur die Optionen und stellt
die bereits geprüften Nutzdaten weiter zu \cite[Sec.~8, 9 und 14]{rfc9868}.

Erst danach liest der Parser die Optionsliste. Zwei Längenfehler machen den gesamten Optionsbereich
strukturell ungültig: Die Länge liegt unter dem bekannten Mindestmaß ihrer Art oder reicht über das Ende
der Surplus Area hinaus. Ein zweites FRAG hat dieselbe Folge \cite[Sec.~10]{rfc9868}. Für den
Überlängenfall löst Erratum~8834 den Widerspruch zu Abschnitt~14 zugunsten dieses Verfahrens auf
\cite{rfc9868-eid8834}.

Eine längere APC ist davon zu unterscheiden. Ihre gewöhnliche Gesamtlänge beträgt 6. Ist die angegebene
Länge größer, passt aber noch vollständig in die Surplus Area, behandelt der RFC die Option wie eine
falsche APC-Prüfsumme und nicht wie einen Strukturfehler \cite[Sec.~11.3]{rfc9868}.

Bei einer gültig aufgebauten APC berechnet die Bibliothek die CRC32c erneut über die neun
Nutzdatenbytes. Stimmen beide Werte überein, gilt die APC als erfolgreich und wird der Anwendung
gemeldet. Andernfalls meldet die Bibliothek einen Fehlschlag und schreibt eine gedrosselte Warnung in das
Protokoll. Enthält das Datagramm mehrere APC-Optionen, wertet die
Bibliothek nur die erste aus und übergeht die weiteren \cite[Sec.~10]{rfc9868}.

Zum Schluss entscheidet die Empfangsrichtlinie über die Zustellung. In der Voreinstellung erhält die
Anwendung die Nutzdaten auch bei einer fehlgeschlagenen APC. Eine strenge Richtlinie kann dagegen eine
erfolgreiche APC verlangen. Fehlt sie oder schlägt sie fehl, liefert die Richtlinienauswertung den
Ausgang \texttt{Filtered}. \texttt{Dropped} bezeichnet dagegen einen Verwerfungsweg der
Protokollverarbeitung, etwa einen gescheiterten Fragmentsatz. So bleibt intern sichtbar, ob eine
Anwendungsrichtlinie oder die Verarbeitung selbst die Zustellung verhindert hat
\cite[Sec.~11.3, 14 und 19]{rfc9868}.

Die öffentliche Schnittstelle bündelt den Netzpfad im Typ \texttt{Peer}. Er wird mit Adressen,
Sendekonfiguration, Empfangsrichtlinie und Empfangsfrist gebunden. Die Empfangsrichtlinie
(\texttt{ReceivePolicy}) kann eine oder mehrere meldefähige Pflichtoptionen verlangen, alle
optionstragenden Datagramme ausfiltern oder ein bestätigtes OCS fordern. Die Berichtsarten
\texttt{OcsReport} und \texttt{OptionReport} halten fest, welche Prüfungen die Bibliothek ausgeführt hat
und wie sie endeten \cite[Sec.~14 und 15]{rfc9868}.

Vor dieser Verarbeitung muss die Socket-Schicht zunächst ein passendes Datagramm finden. Ein Raw Socket
sieht auch fremden UDP-Verkehr. In einer frühen Fassung beendete bereits das erste fremde Datagramm den
Empfangsaufruf ohne Ergebnis; eine laufende Messung konnte dadurch still enden. Die heutige Fassung
überspringt unlesbare Köpfe, eigene Kopien und nicht passende Ports innerhalb einer Empfangsschleife.

Für diese Schleife gilt eine einzige Gesamtfrist. Vor jedem weiteren Leseversuch erhält der Socket nur
die verbleibende Zeit; ständiger fremder Verkehr verlängert die Wartezeit deshalb nicht. Erst wenn bis
zum Ablauf der Gesamtfrist kein passendes Datagramm eintrifft, meldet die
Socket-Schicht einen Fristablauf. Die öffentliche Schnittstelle fasst diesen Ausgang weiterhin mit
Dekodierfehlern zusammen. Dasselbe gilt für ein noch unvollständig gepuffertes Fragment, einen
Verwerfungsweg und eine Richtlinienentscheidung. Alle erscheinen dort nur als \zitat{kein Datagramm}; die
Ursache ist nicht unterscheidbar.

\section{Fragmentierung und weitere Pflichtoptionen}
\label{sec:optionsverarbeitung}

Der Pflichtkern umfasst acht Arten: End of Options List (EOL), No Operation (NOP), Additional Payload
Checksum (APC), Fragmentation (FRAG), Maximum Datagram Size (MDS), Maximum Reassembled Datagram Size
(MRDS), Echo Request (REQ) und Echo Response (RES) \cite[Sec.~10]{rfc9868}. EOL und NOP entstehen
beim Optionsbau, FRAG im Splitter. APC, MDS, MRDS, REQ und RES stehen als typisierte Sendeoptionen
bereit und entstehen nur auf Anforderung.

TIME, AUTH und EXP besitzen keine typisierten, sendbaren Varianten. Empfangsseitig erscheinen ihre
Kind-Werte als \texttt{Other}. Nur TIME und EXP haben Mindestlängenprüfungen; diese Prüfungen sind keine
vollständige Unterstützung der Optionen.

FRAG überträgt ein Datagramm in einem oder mehreren Fragmenten und setzt mehrere Fragmente beim
Empfänger wieder zusammen. Dazu verschiebt FRAG die Fragmentdaten aus den UDP-Nutzdaten in die Surplus
Area. Deshalb beträgt \texttt{UDP Length} in jedem Fragment 8, und jedes Fragment trägt erneut beide
UDP-Ports \cite[Sec.~11.4]{rfc9868}. Die FRAG-Option ist im nichtterminalen Fragment zehn Byte und im
terminalen Fragment zwölf Byte lang.

\texttt{Frag.Offset} bezeichnet die Lage der Fragmentdaten im ursprünglichen UDP-Datagramm, gemessen
ab dessen UDP-Kopf. \texttt{Frag.Start} bezeichnet den Beginn der Fragmentdaten im aktuellen Fragment,
ebenfalls ab dem UDP-Kopf. Er beträgt hier 20 Byte im nichtterminalen und 22 Byte im terminalen
Fragment. Nur das terminale Format enthält den Reassembled Datagram Option Start (RDOS). Dieser Zeiger
markiert im wiederhergestellten Datagramm das Ende der Nutzdaten und den Beginn der Datagrammoptionen.
RDOS ergibt zugleich die \texttt{UDP Length} des wiederhergestellten Datagramms; etwaige
Datagrammoptionen folgen dahinter in dessen Surplus Area \cite[Sec.~11.4]{rfc9868}.

MRDS begrenzt die Größe des wiederhergestellten UDP-Datagramms einschließlich UDP-Kopf und
Datagrammoptionen. Ein zweites Feld begrenzt die Zahl der Fragmente \cite[Sec.~11.6]{rfc9868}. Ohne
empfangene MRDS legt die Implementierung für IPv4 die vom RFC verlangten Mindestwerte von 2.926 Byte
und zwei Fragmenten zugrunde.

Empfangene MRDS-Werte übernimmt die Bibliothek ebenso wenig automatisch in ihre Sendekonfiguration wie
empfangene MDS-Werte. Der Aufrufer muss sie selbst in \texttt{PeerFragmentLimits} übertragen. Auch MRDS
lässt sich je Sendung erzeugen.

Bei einer maximalen Datagrammlänge von 1.500 Byte bleiben nach IPv4- und UDP-Kopf 1.472 Byte für die
Surplus Area. OCS und FRAG verringern die Datenkapazität auf 1.460 Byte im nichtterminalen und
1.458 Byte im terminalen Fragment. Das Beispiel in \figref{fig:frag-ablauf} zerlegt deshalb
2.918 Nutzdatenbytes in genau zwei Fragmente.

Passt der gesamte Rest in ein terminales Fragment, verwendet dieses \texttt{Frag.Offset} null. Sonst
erzeugt eine Schleife nichtterminale Fragmente und überlässt den Rest dem terminalen Fragment. Der
Splitter prüft zuerst die MRDS-Größe und danach die Zahl der Fragmente. Ausgelöst wird die Zerlegung durch die
Datagrammkapazität, nicht durch MRDS.

\texttt{Peer} und \texttt{udpopt-send} besitzen je einen monotonen Generator für die
Fragmentidentifikation. Sie verwenden einen vorgegebenen Startwert oder lesen ihn aus
\texttt{/dev/urandom}. Der Generator wiederholt keinen Wert. Nach \texttt{u32::MAX} meldet er einen
Fehler. Die tiefere Funktion \texttt{build\_outgoing\_datagrams} besitzt keinen Generator und verlangt
bei notwendiger Fragmentierung eine explizite Identifikation.

\begin{figure}[p]
  \centering
  {\small (a) Zerlegung im Sendepfad\par}\vspace{1pt}
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      >={Stealth[length=5pt, width=4pt]},
      anno/.style={font=\scriptsize\linespread{1.0}\selectfont, text=feldlinie, align=left},
    ]
    \draw[feld] (0,-0.55) rectangle (1.4,0);
    \draw[feld] (1.4,-0.55) rectangle (7.4,0);
    \node[feldtext, font=\tiny] at (0.7,-0.275) {UDP-Kopf};
    \node[feldtext] at (4.4,-0.275) {Nutzdaten};
    \node[bitindex] at (0.7,-0.76) {8};
    \node[bitindex] at (4.4,-0.76) {2918};
    \draw[klammer] (0,0.12) -- (7.4,0.12)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie]
      {ursprüngliches Datagramm, \texttt{UDP Length} = 2926};
    \draw[->, draw=feldlinie, thin] (3.7,-0.95) -- (3.7,-1.42);
    \node[anno, anchor=west] at (4.0,-1.21)
      {Zerlegung, Budget je Fragment: 1472 Byte Surplus Area};
    \draw[klammer] (2.7,-2.13) -- (12.9,-2.13)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {Surplus Area, 1472 Byte};
    \draw[feld] (0,-2.80) rectangle (1.2,-2.25);
    \draw[feld] (1.2,-2.80) rectangle (2.7,-2.25);
    \draw[feldakzent] (2.7,-2.80) rectangle (3.3,-2.25);
    \draw[feldakzent] (3.3,-2.80) rectangle (5.5,-2.25);
    \draw[feld] (5.5,-2.80) rectangle (12.9,-2.25);
    \node[feldtext, font=\tiny] at (0.6,-2.525) {IP-Kopf};
    \node[feldtext, font=\tiny] at (1.95,-2.525) {UDP-Kopf};
    \node[feldtext, font=\tiny] at (3.0,-2.525) {OCS};
    \node[feldtext, font=\tiny] at (4.4,-2.525) {FRAG};
    \node[feldtext] at (9.2,-2.525) {Fragmentdaten};
    \node[bitindex] at (0.6,-3.01) {20};
    \node[bitindex] at (1.95,-3.01) {8};
    \node[bitindex] at (3.0,-3.01) {2};
    \node[bitindex] at (4.4,-3.01) {10};
    \node[bitindex] at (9.2,-3.01) {1460};
    \node[anno, anchor=west] at (0,-3.56)
      {Fragment 1: FRAG mit \texttt{Len} = 10, Identification \texttt{0x12345678},\\
       \texttt{Frag.Offset} 8, \texttt{UDP Length} 8};
    \draw[klammer] (2.7,-4.48) -- (12.9,-4.48)
      node[midway, above=7pt, font=\scriptsize, text=feldlinie] {Surplus Area, 1472 Byte};
    \draw[feld] (0,-5.15) rectangle (1.2,-4.60);
    \draw[feld] (1.2,-5.15) rectangle (2.7,-4.60);
    \draw[feldakzent] (2.7,-5.15) rectangle (3.3,-4.60);
    \draw[feldakzent] (3.3,-5.15) rectangle (5.8,-4.60);
    \draw[feld] (5.8,-5.15) rectangle (12.9,-4.60);
    \node[feldtext, font=\tiny] at (0.6,-4.875) {IP-Kopf};
    \node[feldtext, font=\tiny] at (1.95,-4.875) {UDP-Kopf};
    \node[feldtext, font=\tiny] at (3.0,-4.875) {OCS};
    \node[feldtext, font=\tiny] at (4.55,-4.875) {FRAG};
    \node[feldtext] at (9.35,-4.875) {Fragmentdaten};
    \node[bitindex] at (0.6,-5.36) {20};
    \node[bitindex] at (1.95,-5.36) {8};
    \node[bitindex] at (3.0,-5.36) {2};
    \node[bitindex] at (4.55,-5.36) {12};
    \node[bitindex] at (9.35,-5.36) {1458};
    \node[anno, anchor=west] at (0,-5.91)
      {Fragment 2 (terminal): FRAG mit \texttt{Len} = 12, \texttt{Frag.Offset} 1468,\\
       RDOS 2926 = \texttt{UDP Length} des Ergebnisses};
  \end{tikzpicture}
  \par\vspace{6pt}
  {\small (b) Reassemblierung im Empfangspfad\par}\vspace{1pt}
  \begin{tikzpicture}[
      x=1cm, y=1cm,
      luecke/.style={draw=feldlinie, fill=none, thin, dashed},
      anno/.style={font=\scriptsize\linespread{1.0}\selectfont, text=feldlinie, align=left},
    ]
    \node[anno, anchor=west] at (0,0.72)
      {Puffer nach Nutzdatenposition im wiederhergestellten Datagramm:};
    \node[bitindex, anchor=south] at (0.9,0.10) {0};
    \node[bitindex, anchor=south] at (6.4,0.10) {1460};
    \node[bitindex, anchor=south] at (11.9,0.10) {2918};
    \draw[luecke] (0.9,-0.55) rectangle (6.4,0);
    \node[feldtext, text=feldlinie] at (3.65,-0.275) {Lücke};
    \draw[feldakzent] (6.4,-0.55) rectangle (11.9,0);
    \node[feldtext] at (9.15,-0.275) {Daten des terminalen Fragments};
    \node[bitindex] at (3.65,-0.76) {1460};
    \node[bitindex] at (9.15,-0.76) {1458};
    \node[anno, anchor=west] at (0,-1.40)
      {Schritt 1: Das terminale Fragment trifft zuerst ein. \texttt{Frag.Offset} 1468 ergibt die Ablage\\
       ab Position 1460. Sein Ende legt das Satzende auf 2918 fest; RDOS 2926 setzt die\\
       \texttt{UDP Length} des Ergebnisses. Das Fragment bleibt gepuffert.};
    \draw[feldakzent] (0.9,-2.60) rectangle (6.4,-2.05);
    \node[feldtext] at (3.65,-2.325) {Daten des nichtterminalen Fragments};
    \draw[feld] (6.4,-2.60) rectangle (11.9,-2.05);
    \node[feldtext] at (9.15,-2.325) {Daten des terminalen Fragments};
    \node[bitindex] at (3.65,-2.81) {1460};
    \node[bitindex] at (9.15,-2.81) {1458};
    \node[anno, anchor=west] at (0,-3.45)
      {Schritt 2: \texttt{Frag.Offset} 8 füllt den Bereich ab Position 0. Der Puffer ist bis 2918\\
       lückenlos, der Satz damit vollständig.};
    \draw[feld] (0,-4.75) rectangle (1.2,-4.20);
    \node[feldtext, font=\tiny] at (0.6,-4.475) {UDP-Kopf};
    \draw[feld] (1.2,-4.75) rectangle (11.9,-4.20);
    \node[feldtext] at (6.55,-4.475) {Nutzdaten};
    \node[bitindex] at (0.6,-4.96) {8};
    \node[bitindex] at (6.55,-4.96) {2918};
    \draw[klammer] (11.9,-5.30) -- (0,-5.30)
      node[midway, below=7pt, font=\scriptsize, text=feldlinie] {\texttt{UDP Length} = 2926 = RDOS};
    \node[anno, anchor=west] at (0,-6.55)
      {Schritt 3: Aus Daten und \texttt{UDP Length} entsteht ein neues Datagramm. Es durchläuft die\\
       Empfangspipeline (\secref{sec:empfangspfad}) genau einmal erneut; weitere Reassemblierung\\
       ist gesperrt. Der UDP-Kopf ist nicht maßstäblich gezeichnet.};
  \end{tikzpicture}
  \caption[Zerlegung und Reassemblierung eines Datagramms]{Zerlegung und Reassemblierung eines Datagramms
    mit 2918 Nutzdatenbytes}
  \label{fig:frag-ablauf}
\end{figure}

Der Empfangscache ordnet Fragmente nach UDP-Vierertupel und Identifikation
\cite[Sec.~11.4]{rfc9868}. Das Ende des terminalen Fragments bestimmt das Ende des Satzes; sein RDOS
liefert die \texttt{UDP Length} des Ergebnisses. \lstref{lst:frag-lueckenpruefung} prüft, ob alle
Bereiche lückenlos vorliegen.

\noindent\begin{minipage}{\linewidth}
\begin{lstlisting}[style=codestyle, language=Rust, basicstyle=\ttfamily\scriptsize,
    caption={Lückenprüfung eines Reassemblierungssatzes}, label={lst:frag-lueckenpruefung}]
fn complete(&self) -> InsertResult {
    let (Some(terminal_end), Some(udp_length)) =
        (self.terminal_end, self.udp_length)
    else {
        return InsertResult::Incomplete;
    };

    let mut cursor = 0;
    for segment in &self.segments {
        if segment.start > cursor {
            return InsertResult::Incomplete;
        }
        if segment.start < cursor {
            return InsertResult::Abort(AbortReason::Overlap);
        }
        cursor = segment.end;
    }
    if cursor != terminal_end {
        return InsertResult::Incomplete;
    }
    ..
}
\end{lstlisting}
\end{minipage}

Nur ein lückenloser Bereich bis zum terminalen Ende erzeugt ein Ergebnis; eine Überlappung verwirft den
Satz.

Ein intern vollständig identisches Duplikat wird übergangen. Lage, Endwert, Daten, Fragmentoptionen und
Fehlerliste müssen dafür übereinstimmen. Andernfalls gelten die normalen Grenz- und
Überlappungsprüfungen. Die OCS-Beobachtung wird getrennt zusammengeführt.

Die Fragmentberichte werden ebenfalls zusammengeführt. Bei MDS gilt der kleinste Wert. Bei MRDS werden
Größe und Fragmentzahl getrennt minimiert. Von REQ und RES bleibt jeweils das zuletzt gesehene Token;
ein Fehlschlag in einem Fragment gilt für den ganzen Satz. Das neu gebildete Datagramm durchläuft
danach die Empfangspipeline genau einmal erneut (\figref{fig:frag-ablauf}). Erst in diesem Durchlauf
wird eine APC des Ursprungsdatagramms geprüft; eine APC in einem einzelnen Fragment wertet die
Bibliothek nicht aus \cite[Sec.~11.3]{rfc9868}.

Der Parser wahrt die Abwärtskompatibilität auch bei unbekannten Optionen. Eine unbekannte SAFE-Option
übergeht er und meldet sie als übergangen. Bei einer unbekannten UNSAFE-Option darf er die Nutzdaten
dagegen nicht weitergeben. Ein gewöhnliches oder bereits zusammengesetztes Datagramm wird deshalb mit
leerer Nutzlast zugestellt \cite[Sec.~12 und 14]{rfc9868}. Die Sendeseite erzeugt selbst keine
UNSAFE-Optionen; der Optionskern weist solche Arten schon beim Aufbau zurück.

Innerhalb eines Fragments bildet eine unbekannte UNSAFE-Option einen Sonderfall. Steht zuvor ein
gültiges FRAG, kann die Bibliothek das Fragment zuordnen und verwirft den ganzen Fragmentsatz. Steht die
UNSAFE-Option vor FRAG, ist noch keine Zuordnung möglich. Dann stellt die Bibliothek eine leere Nutzlast
zu und lässt einen bereits gespeicherten Satz mit derselben Kennung unberührt
\cite[Sec.~10, 12 und 14]{rfc9868}.

Für den Aufbau aller Optionen gelten gemeinsame Regeln. Der Sender stellt die Pflichtoptionen außer
\texttt{EOL} und \texttt{NOP} vor andere SAFE-Optionen. Der interne Optionskern lässt gewöhnliche
Wiederholungen für Tests zu. Die öffentliche Schnittstelle weist wiederholte meldefähige Optionen dagegen
ab; FRAG darf schon im Optionskern nur einmal vorkommen \cite[Sec.~10]{rfc9868}.

EOL beendet die Liste, NOP richtet die folgende Option aus. Nach EOL fügt der Serialisierer höchstens ein
Nullbyte für eine gerade Länge an. Die öffentliche Schnittstelle kann weder eine größere Nullfüllung noch
eine Mindestlänge des Optionsbereichs anfordern. Beim Empfang prüft der Parser die Bytes hinter EOL
nicht; der RFC stellt diese Prüfung frei \cite[Sec.~11.1]{rfc9868}. Im Empfangsbericht bleiben EOL, NOP
und FRAG unsichtbar.

Beim erweiterten Längenformat trifft der Parser eine eigene strenge Entscheidung. Verwendet eine
vollständige Option dieses Format für eine Gesamtlänge unter 255, verwirft er alle Optionen. RFC~9868
verlangt hier nur vom Sender die kürzere Darstellung und legt keine besondere Reaktion des Empfängers
fest \cite[Sec.~10]{rfc9868}.

REQ und RES tragen jeweils ein vier Byte langes Token. Die Bibliothek serialisiert und dekodiert beide
Arten, sendet aber keine automatische Antwort. Die Anwendung muss das Token eines empfangenen REQ für
ein zugehöriges RES übernehmen \cite[Sec.~11.7]{rfc9868}.

\section{Betriebs- und Sicherheitsgrenzen}
\label{sec:betriebsgrenzen}

Raw Sockets verlangen unter Linux \texttt{CAP\_NET\_RAW} oder Root
\cite{man7capabilities, man7raw}. Dieses Privileg erlaubt den Zugriff auf rohe IPv4-Datagramme. Mit
\texttt{IP\_HDRINCL} kann der Prozess auch eine von null verschiedene Quelladresse vorgeben. Die
Bibliothek kapselt den Zugriff, hebt seine Wirkung aber nicht auf.

Der \texttt{ReassemblyCache} ist der einzige fachliche Empfangszustand über Datagrammgrenzen hinweg.
Jeder \texttt{Peer} besitzt einen Cache, filtert eingehende Datagramme jedoch nur nach Ports.
Verschiedene Adresspaare mit gleichen Ports teilen deshalb Cache und Mengengrenze. Der im Modul
\texttt{frag} zugesagte Vertrag eines Caches je UDP-Vierertupel ist auf dieser Ebene nur teilweise
erfüllt.

Globale Warnzähler dienen nur der Diagnose. Der Parser besitzt keine Gesamtgrenze für die Zahl der
Optionen. Mehr als sieben aufeinanderfolgende \texttt{NOP} erzeugen eine Warnung
\cite[Sec.~11.2]{rfc9868}. Sie erscheint beim ersten und danach bei jedem 64. Vorkommnis einer
Kategorie.

\texttt{ReassemblyLimits} begrenzt Datagrammgröße, Fragmentzahl, gleichzeitig unvollständige Sätze und
Frist. Die Voreinstellungen sind 2.926 Byte, zwei Fragmente und zwei Minuten
\cite[Sec.~11.4, 11.6 und 25.4]{rfc9868} sowie 64 unvollständige Sätze je Cache. Die Zahl 64 ist eine
lokale Schutzgrenze; RFC~9868 empfiehlt nur eine Begrenzung.

Die Frist beginnt mit dem ersten Fragment und wird nicht erneuert. Abgelaufene Sätze verschwinden bei
der nächsten Einfügung oder bei einem Bereinigungsaufruf. Ein voller Cache weist neue unvollständige
Sätze ab und verdrängt keine vorhandenen. Ein für sich vollständiges Fragment kann weiterhin
zugestellt werden.

Die maximale Sendelänge ist eine Vorgabe des Aufrufers und keine Pfad-MTU-Ermittlung. Oberhalb der MTU
meldet der Kernel \texttt{EMSGSIZE} \cite{man7raw}; die Bibliothek reicht den E/A-Fehler weiter
(\secref{sec:sendepfad}).

Damit sind das Messinstrument und seine Betriebsgrenzen beschrieben; \chapref{chap:evaluation} setzt
es auf die Forschungsfragen an.
