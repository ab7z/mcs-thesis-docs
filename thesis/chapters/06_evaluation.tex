% =============================================================================
% Kapitel 6: Evaluation und Diskussion
% =============================================================================

\chapter{Evaluation und Diskussion}
\label{chap:evaluation}

Mit \chapref{chap:entwurf} liegen die Referenzimplementierung und ihre Testarchitektur vor und sind als
Messinstrument beschrieben.
Dieses Kapitel setzt das Instrument auf die beiden Forschungsfragen aus \secref{sec:zielsetzung} an: FF1
fragt, welche Anforderungen von RFC~9868 sich unter Linux und IPv4 im Benutzerraum erfüllen lassen; FF2
fragt, in welchem Umfang die Surplus Area auf realen Pfaden bis zur Gegenstelle erhalten bleibt. Der
Maßstab dafür steht seit \chapref{chap:analyse} fest: die Konformitätsmatrix mit ihren 67 normativen
Aussagen, die Bewertungskategorien aus \secref{sec:ff1-kriterien} und das Pfad- und Fehlermodell aus
\secref{sec:ff2-pfadmodell}.

Das Kapitel arbeitet in vier Schritten: Zunächst legt \secref{sec:testkonzept} das Test- und Messkonzept
fest, also Begriffe, Messläufe, Eingabestände, Werkzeuge und Verzichte. Ferner berichtet
\secref{sec:beobachtbarkeit-eval} die Pfadergebnisse zu FF2, denn ein verlorenes oder verändertes
Datagramm ist zuerst der Kette der Zwischensysteme zuzuordnen, bevor es der Implementierung oder dem
Standard angelastet wird (\secref{sec:netzpfad}). Erst danach bewertet \secref{sec:soll-ist} die
Anforderungen für FF1, weil mehrere Negativfälle auf realen Pfaden nur vor dem Hintergrund der
Pfadbefunde messbar und deutbar sind. Abschließend beschreibt \secref{sec:ki-reflexion} die
KI-gestützte Arbeitsweise anhand belegter Fälle, bevor \secref{sec:diskussion-grenzen} die Ergebnisse
und ihre Grenzen bündelt.

\section{Test- und Messkonzept}
\label{sec:testkonzept}

Für die Ergebnisse gelten die in \chapref{chap:analyse} definierten Begriffe unverändert. Ein
Optionsdatagramm kommt je Pfad, Richtung und Messzeitpunkt \textbf{erhalten}, \textbf{verändert} oder
\textbf{entfernt} an, oder es wird \textbf{verworfen} (\secref{sec:ff2-pfadmodell}); Anforderungen werden
als \textbf{vollständig}, \textbf{teilweise} oder \textbf{nicht erfüllbar} bewertet, die Sonderklasse
\textbf{nicht anwendbar} bleibt sichtbar, wird aber nicht bewertet (\secref{sec:ff1-kriterien}). Dabei
dürfen Kategorien leer bleiben: Findet die Bewertung keine grundsätzlich unerfüllbare Anforderung, ist
das ein Ergebnis und kein Mangel des Maßstabs.

Die Messläufe selbst tragen unterschiedliche Beweiskraft, und genau diese Abstufung legt das Testkonzept
vorab fest. Eine \textbf{Kampagne} umfasst beidseitige Mitschnitte, ein Sendemanifest, einen
Auswerterlauf und ein versiegeltes Archiv; eine \textbf{vorregistrierte Messreihe} folgt einem vorab
festgelegten Zellplan, verzichtet aber auf einen Teil dieses Vollpakets; ein \textbf{Pilot} ist eine
Funktionsprobe ohne Mitschnitt oder Manifest; eine \textbf{Vorstufe} prüft eine Richtung mit einem
Datagramm je Klasse. Alle Läufe dieser Arbeit ordnet \tabref{tab:eval-messlaeufe} ein:

\begin{table}[!htb]
    \centering
    \caption{Messläufe der Arbeit mit Pfad, Quellstand, Umfang und Einstufung}
    \label{tab:eval-messlaeufe}
    \footnotesize
    \setlength{\tabcolsep}{4pt}
    \begin{tabularx}{\textwidth}{L{2.5cm} L{2.2cm} L{1.8cm} Y L{1.9cm} L{1.5cm}}
        \toprule
        \textbf{Messlauf} & \textbf{Pfad} & \textbf{Quellstand} & \textbf{Umfang} & \textbf{Einstufung} &
        \textbf{Archiv} \\
        \midrule
        Lokale Lanes (Schritte 17, 18) & vier Namespace-Topologien & \file{f847895} &
        5~Szenarien, 7~Verdikt-Klassen & Referenz\-läufe & Impl.-Repo \\
        Externe Kampagne 10.08. & achim $\rightarrow$ mcs & \file{7b11140} &
        je 1~Probe IPv4/IPv6, Tunnelkontrolle & Kampagne & extern \\
        Bidirektionale Kampagne 11.08. & achim $\leftrightarrow$ mcs & \file{7b11140} &
        Options\-datagramme 0/6 gegen Kontrollen 15/15; 18~Kontrollformen & Kampagne & bidir \\
        Piloten 12./13.08. & achim, Mac, 1blu, mcs & \file{b12ba8e} &
        0/13; 0/10; 1blu-Pfad je Richtung 6/6 & Piloten & piloten \\
        Vorstufe S-00 14.08. & mcs $\rightarrow$ 1blu & Meta im Archiv &
        11~Szenarien, 15~Datagramme, $n=1$ je Klasse & Vorstufe & piloten \\
        P0/P1/P2 14./15.08. & 1blu $\leftrightarrow$ mcs & Meta im Archiv &
        über 10\,000~Pakete (P0); 15~Klassen $\times$ 6 je Richtung (P1); S-49 570/570; S-50 12/12 &
        Kampagnen & 1blu-mcs \\
        Helsinki-Paare 15.08. & mcs $\leftrightarrow$ hel, hel $\leftrightarrow$ 1blu & \file{d7187eb} &
        18~Szenarien je Paar, alle Läufe fehlerfrei & Kampagnen & hel \\
        Prüfsummen-Gate-Zellen 15.08. & drei EU-Paare & \file{d7187eb} &
        je Richtung 50~Pakete in drei Prüfformen; Erstlauf ohne Mitschnitt getrennt & Messzellen mit
        Mitschnitt & hel \\
        Hotspot-Reihen 2 bis 4, 15.08. & achim $\leftrightarrow$ mcs, 1blu & Meta im Archiv &
        je Zelle $n=30$, atomare Zelle $n=10$, vorregistriert & Messreihen & hotspots \\
        AWS-Suiten 16.08. & aws $\leftrightarrow$ mcs, hel, 1blu & \file{d7187eb}, \file{9d6c5bd} &
        54~Szenarioläufe, alle fehlerfrei & Kampagnen & aws \\
        GCP- und AWS-Zellen 16.08. & gcp, aws, aws2 & \file{d7187eb} &
        Piloten; Diskriminator-Matrizen $n=20$ je Zelle & Piloten, Messreihen & gcp-us, aws-us \\
        AP-Durchlauf 16.08. & 6~Regionen $\leftrightarrow$ mcs, 1blu & \file{d7187eb} &
        Gate- und Kontrollzellen $n=20$, zwei Phasen & Messreihe mit Mitschnitt und Manifest & aws-ap \\
        \bottomrule
    \end{tabularx}
\end{table}

Aus \tabref{tab:eval-messlaeufe} geht hervor, dass die tragenden Aussagen der Ergebnisabschnitte auf
Kampagnen ruhen, während Piloten und Messreihen Lücken schließen und Mechanismen isolieren. Diese
Trennung hat zur direkten Konsequenz, dass Pilotwerte nie mit Kampagnensummen verrechnet werden und
jede Zahl im Folgenden die Einstufung ihres Laufs erbt. Gefahren wurden 43 der 45 Szenarien des
Testkatalogs vollständig auf realen Pfaden; S-24 ist empfängerseitig durch die Ersatzzelle S-24r belegt
und senderseitig eine begründete Grenze, S-51 wurde begründet gestrichen (dazu unten). Die Spalte
Quellstand nennt den Commit der bitidentisch auf allen Endpunkten verteilten Messprogramme; wo mehrere
Stände beteiligt waren, dokumentieren die Metadateien im Archiv jeden Lauf einzeln. Davon getrennt ist
der Auswertungsstand der Implementierung: Alle Aussagen über Quelltext und Tests beziehen sich auf
\file{f847895}. Die Spalte Archiv verweist auf die elf versiegelten Archive der Arbeit; jedes trägt eine
SHA-256-Prüfsumme, deren Sollwerte das Archivverzeichnis dokumentiert.\footnote{Verzeichnis
\file{thesis/evidence/} im Repositorium der Arbeit
(\url{https://github.com/ab7z/mcs-thesis-docs}); die Kurzkennungen der Tabelle stehen für die dort
abgelegten Archivdateien. Ein Hashwert belegt die Unverändertheit eines Artefakts, nicht die Wahrheit
seines Inhalts (\chapref{chap:methodik}).}

Die Werkzeugkette folgt \secref{subsec:mitschnittwerkzeuge}: \texttt{tcpdump} zeichnet auf, der eigene
Prüfer deutet die Surplus Area, \texttt{tshark} liefert die fremde Gegenprobe der IPv4- und UDP-Felder.
Der Messbetrieb ergänzt dazu vier Betriebsregeln, die aus Fehlversuchen auf der Loopback-Schnittstelle
stammen: Ausgehende Pakete werden dort nicht über Richtungsfilter erfasst, Mitschnittdateien wechseln
nach dem Rechteabstieg von \texttt{tcpdump} den Eigentümer, die Schnittstelle meldet sich mit einem
künstlichen Ethernet-Rahmen, und ein vom Filter gezähltes Paket ist noch kein geschriebenes Paket.
Aus diesem Grund beweist vor jedem Lauf ein Aufwärmpaket die Mitschnittbereitschaft, und ein Lauf endet
erst, wenn die Mitschnittdatei nicht mehr wächst. Für die Deutung der Ergebnisse wichtig ist zudem die
Arbeitsteilung im Sendeweg: Der Kernel schreibt auf dem \texttt{IP\_HDRINCL}-Pfad nur die
IPv4-Gesamtlänge und die Kopfprüfsumme um und lässt UDP-Kopf und Surplus Area unangetastet; damit sind
auch Grenzfälle wie ein Datagramm mit UDP-Prüfsumme null und OCS null exakt konstruierbar.

Den Prüfumfang je Lauf legt der Szenarienkatalog fest: 18~Treiberszenarien je Hostpaar (elf P0, vier P1,
drei P2), dazu ein vorregistrierter Zellplan mit 21 etikettierten Zellen, dessen Etiketten je Zelle
festhalten, ob eine Erwartung aus dem RFC, aus dem Implementierungsstand oder aus einer Pfadannahme
stammt.\footnote{Treiber \file{scripts/pair-campaign.sh}, Zellplan \file{scripts/p2-cellplan.json},
Auswerter \file{scripts/p0-eval.py} bis \file{p2-eval.py} im Implementierungsrepositorium; die vier
lokalen Topologien und die sieben Verdikt-Klassen definiert \file{docs/evaluation.md}.} Die lokalen
Referenzläufe stellen denselben Katalog in vier Namespace-Topologien nach und liefern den Nullpunkt, an
dem jeder Realpfadbefund gespiegelt wird.

Ob die Messpfade während der Kampagnen stabil blieben, prüft eine eigene Auswertung der
Lebensdauerfelder aller Mitschnitte:

\begin{figure}[H]
    \centering
    \input{tikz/ttl-uniformitaet}
    \caption{Ankunfts-TTL je Richtung über die Kampagnen-Mitschnitte der europäischen Messpfade}
    \label{fig:eval-ttl}
\end{figure}

Aus \figref{fig:eval-ttl} geht hervor, dass Optionsdatagramme, Fragmente und Kontrollpakete auf keinem
Pfad eine klassenspezifische TTL-Behandlung erfahren: Je Richtung tragen alle angekommenen Pakete
denselben Wert, und die drei Abweichungsgruppen sind Pfadeigenschaften statt Klasseneffekte. Auf dem
Rückweg von 1blu nach mcs liegen 137 Pakete eines einzigen zusammenhängenden Zeitfensters und zehn
Einzelpakete mit eigenen Flusskennungen auf einer um einen Router kürzeren Variante, und von 1blu nach
Helsinki teilt dieselbe Verzweigung den Verkehr dauerhaft in zwei Zweige; beide Effekte kehren in
\secref{sec:beobachtbarkeit-eval} als Wegevielfalt des Rückpfads wieder. Die abgeleitete Routerzahl
beruht dabei auf der Annahme, dass jede Weiterleitung den Wert um genau eins senkt
(\secref{sec:netzpfad}). Dass je Richtung weniger Pakete ankommen als abgesendet wurden, ist kein
Messverlust, sondern Ergebnis: Die Differenz besteht aus den Negativzellen, deren Verwerfen die
Ergebnisabschnitte gerade nachweisen. Diese Beobachtung hat zur direkten Konsequenz, dass ein späterer
Befund nicht als Nebenwirkung wechselnder Wegewahl im Messfluss erklärbar ist; sie gilt für die
gemessenen Zeitfenster und ersetzt keine allgemeine Stabilitätsaussage über die Pfade.

Zwei Verzichte gehören zum Konzept. Das Szenario S-24 (unterschiedliche Optionssätze je Fragment) wird
senderseitig nicht erzeugt, weil die Bibliothek je Datagramm einen Optionssatz führt; die
Empfängerpflicht ist mit der Ersatzzelle S-24r real belegt. Die netem-Störlane S-51 entfiel aus vier
Gründen: Erstens ist jeder Mechanismus, den sie stochastisch anstoßen würde, bereits deterministisch mit
exakten Sollwerten belegt (S-41 bis S-44, S-49, S-50); zweitens hätte ihre Verlustzelle den
Prüfgegenstand verfehlt, weil unfragmentierter Verkehr keinen RFC-9868-Codepfad berührt; drittens ist
die Verlustzuordnung am realen Objekt stärker demonstriert als unter künstlicher Störung; viertens stand
das Betriebsrisiko eines Root-Qdisc auf dem einzigen Messserver, dessen Verwaltungszugang über
dieselbe Schnittstelle läuft, ohne eigenen Messpunkt hinter der Störstufe in keinem Verhältnis zum
Nutzen. Der Kampagnentreiber verweigert das Szenario seither ausdrücklich. Als Folge bleibt das
Ende-zu-Ende-Verhalten unter stochastisch gestörtem Pfad ungemessen; die Ergebnisse gelten für saubere,
kalibrierte Pfade, und \secref{sec:soll-ist} weist diese Grenze bei den betroffenen Aussagen aus.

Mit diesen Läufen ist das Messprogramm abgeschlossen; weitere Kampagnen sind nicht vorgesehen. Zwei
Grenzen begleiten deshalb alle Ergebniskapitel: Als Zugangsnetz wurde ausschließlich ein
Mobilfunk-Hotspot-Pfad untersucht, und ohne Messpunkt im Betreibernetz endet jede Ursachenzuordnung an
einem Intervall zwischen zwei eigenen Messpunkten. Der folgende Abschnitt wendet das Konzept auf die
realen Pfade an.

\section[Beobachtbarkeit und Middlebox-Verträglichkeit auf realen Pfaden]%
{Beobachtbarkeit und Middlebox-\\Verträglichkeit auf realen Pfaden}
\label{sec:beobachtbarkeit-eval}

FF2 fragt, in welchem Umfang die Surplus Area auf realen Pfaden bis zur Gegenstelle erhalten bleibt
(\secref{sec:zielsetzung}). Die Ergebnisse folgen den fünf Pfadstufen aus \secref{sec:ff2-pfadmodell} in
aufsteigender Netzferne: zunächst die kontrollierten Stufen Loopback, virtualisierte Topologien und
Tunnel, ferner die öffentlichen Pfade in Europa, abschließend die interkontinentalen Pfade. Quer zu den
Stufen ordnet das Fehlermodell jeden Befund einer der drei Mechanismenklassen aus
\figref{fig:kantenmodell} zu; den Abschluss bildet die messbasierte Fassung dieser Typologie. Keiner der
untersuchten Pfade vervollständigt FF2: Jeder Befund gilt für das beobachtete Paar, die Richtung und das
Messfenster.

\subsection{Kontrollierte Stufen: Loopback, eigene Topologien, Tunnel}
\label{subsec:eval-kontrolliert}

Die ersten drei Pfadstufen liefern den Nullpunkt, an dem jeder Realpfadbefund gespiegelt wird. Ihre
Umgebungen zeigt \figref{fig:eval-lokal}:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        host/.style={draw=sqtealrand, fill=sqteal, rounded corners=2pt, font=\scriptsize,
            minimum height=5mm, inner sep=3pt},
        mb/.style={draw=sqgoldrand, fill=sqgold, rounded corners=2pt, font=\scriptsize,
            minimum height=5mm, inner sep=3pt},
        wolke/.style={draw=sqgrau, fill=sqhell, dashed, rounded corners=6pt, font=\scriptsize,
            inner sep=4pt},
        titel/.style={font=\scriptsize\bfseries},
        pf/.style={->, >=Stealth, semithick},
        anm/.style={font=\tiny, text=feldlinie}]
        % Kachel 1: Loopback-Wire-Lane
        \node[titel, anchor=west] at (0,1.0) {Stufe 1: Loopback (achim)};
        \node[host] (s1) at (0.9,0.2) {Sender};
        \node[host] (e1) at (5.0,0.2) {Empfänger};
        \draw[pf] (s1) -- node[anm, above] {lo, Raw Socket} (e1);
        \node[anm, anchor=west] at (0,-0.5) {Mitschnitt, eigener Prüfer und tshark auf demselben Host};
        % Kachel 2: veth-Spike
        \node[titel, anchor=west] at (7.6,1.0) {Stufe 2a: veth-Spike};
        \node[host] (s2) at (8.5,0.2) {ns 1};
        \node[host] (e2) at (12.6,0.2) {ns 2};
        \draw[pf] (s2) -- node[anm, above] {veth, MTU 1500} (e2);
        \node[anm, anchor=west] at (7.6,-0.5) {zwei Namespaces, direkte Kante};
        % Kachel 3: netns-Lanes
        \node[titel, anchor=west] at (0,-1.4) {Stufe 2b: Namespace-Lanes (Schritte 17, 18)};
        \node[host] (s3) at (0.9,-2.2) {uoe-l};
        \node[mb] (m3) at (2.95,-2.2) {uoe-m};
        \node[host] (e3) at (5.0,-2.2) {uoe-r};
        \draw[pf] (s3) -- (m3);
        \draw[pf] (m3) -- (e3);
        \node[anm, anchor=west, text width=6.4cm] at (0,-3.05) {vier Varianten: veth direkt, Weiterleitung,
            nft-Masquerade, nft-Portfilter; Mitschnitte an beiden Enden};
        % Kachel 4: WireGuard
        \node[titel, anchor=west] at (7.6,-1.4) {Stufe 3: Tunnel (einmalige Kontrolle)};
        \node[host] (s4) at (8.4,-2.2) {achim};
        \node[wolke] (w4) at (10.55,-2.2) {öffentl. Pfad};
        \node[host] (e4) at (12.85,-2.2) {mcs};
        \draw[pf] (s4) -- (w4);
        \draw[pf] (w4) -- node[anm, above] {} (e4);
        \node[anm, anchor=west, text width=6.0cm] at (7.6,-3.05) {WireGuard-Hülle; keine
            Repository-Lane, eine Messung (10.08.)};
    \end{tikzpicture}
    \caption{Kontrollierte Messumgebungen der Pfadstufen 1 bis 3}
    \label{fig:eval-lokal}
\end{figure}

Aus \figref{fig:eval-lokal} geht hervor, dass in den Stufen 1 und 2 jede Kante entweder bekannt oder
absichtlich konfiguriert ist: Ein abweichender Befund wäre dort dem Endpunkt oder der ausdrücklich
eingerichteten Lane zuzuordnen, nicht einem fremden Akteur. Genau diese Eigenschaft macht die Lanes zum
Gegenbeleg für die Pfadbefunde der späteren Stufen: Dieselben absichtlich beschädigten Datagramme, die
reale Pfade verwerfen, werden auf dem Loopback zugestellt und regelkonform behandelt. Die Tunnelstufe
steuert eine einzelne Kontrolle bei: Durch die WireGuard-Kapselung erreichte das innere Datagramm die
Gegenstelle mit unveränderten 26 Surplus-Bytes und gültiger OCS; wie in \secref{sec:netzpfad} festgehalten,
belegt das die Zustellung nach der Entkapselung, nicht den nativen Transport.

\subsection{Öffentliche Pfade in Europa}
\label{subsec:eval-europa}

Die vierte Pfadstufe umfasst zwei sehr verschiedene Umgebungen: das Cloud-Dreieck aus den Endpunkten mcs,
helsinki und 1blu sowie den Mobilfunk-Zugangspfad des Entwicklungsrechners. Den Aufbau des Dreiecks zeigt
\figref{fig:eval-eudreieck}:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        ep/.style={draw=sqtealrand, fill=sqteal, rounded corners=2pt, font=\scriptsize, align=center,
            inner sep=3pt},
        as/.style={draw=sqblaurand, fill=sqblau, rounded corners=8pt, font=\scriptsize, align=center,
            inner sep=3pt},
        uebergabe/.style={draw=feldlinie, fill=feldgrau, font=\tiny, inner sep=2pt},
        unbekannt/.style={draw=sqgrau, fill=sqhell, dashed, font=\tiny, inner sep=2pt},
        pf/.style={->, >=Stealth, semithick},
        kante/.style={font=\tiny, text=feldlinie, align=center}]
        \node[ep] (mcs) at (0,0) {mcs\\Hetzner-Cloud (KVM)\\Nürnberg, AS24940};
        \node[ep] (hel) at (10.25,0) {helsinki\\Hetzner-Cloud, HEL1\\AS24940};
        \node[ep] (blu) at (5.6,-4.1) {1blu\\OpenVZ-Container auf vh5011\\AS42730 (EVANZO)};
        \node[as] (lumen) at (2.3,-2.35) {Lumen\\AS3356};
        \node[as] (colt) at (5.6,-2.05) {COLT\\AS8220};
        \node[uebergabe] (ear) at (0.75,-3.95) {1BLU-AG.ear5.Frankfurt1};
        \node[unbekannt] at (1.75,-0.95) {2 stille Fabric-Hops};
        \draw[pf, transform canvas={yshift=2pt}] (mcs) -- node[kante, above]
            {Hetzner-Backbone, 13 Router je Richtung, RTT $\sim$24\,ms} (hel);
        \draw[pf, transform canvas={yshift=-2pt}] (hel) -- (mcs);
        \draw[pf] (mcs.south) .. controls (0.4,-2.6) .. node[kante, left, pos=0.40]
            {hin: 11 Router\\Übergabe Frankfurt} (blu.west);
        \draw[pf] (blu.north west) .. controls (3.4,-2.9) .. (lumen.south east);
        \draw[pf] (lumen.north west) .. controls (0.6,-1.4) .. (mcs.south east);
        \draw[pf] (blu.north) -- (colt.south);
        \draw[pf] (colt.north) .. controls (4.6,-0.9) .. (mcs.east);
        \draw[pf] (hel.south) .. controls (9.7,-2.6) .. node[kante, right, pos=0.55]
            {hin: 12 Router\\zurück: 10 oder 11 (ECMP)} (blu.east);
        \draw[pf, transform canvas={xshift=-4pt}] (blu.north east) .. controls (9.0,-2.9) .. (hel.south west);
        \node[kante, anchor=north west, align=left, text width=10.6cm] at (0,-4.95) {zurück über die
            1blu-Aggregation (ECMP): Lumen-Zweig 11 Router (RTT 14 bis 17\,ms), COLT-Zweig 10 Router
            (RTT $\sim$4\,ms)};
    \end{tikzpicture}
    \caption{Europäisches Messdreieck mit zwei Endpunkt- und zwei beobachteten Transit-Netzen}
    \label{fig:eval-eudreieck}
\end{figure}

Aus \figref{fig:eval-eudreieck} geht hervor, dass die drei Messstrecken zwei Endpunkt-Netze (AS24940,
AS42730) und zwei beobachtete Transit-Netze (Lumen, COLT) berühren; die abgeleiteten Routerzahlen stehen
unter der Annahme aus \secref{sec:netzpfad}, dass jede Weiterleitung den TTL-Wert um genau eins senkt, und
zwei Weiterleitungen existieren nur als TTL-Differenz, weil sie nie antworten. Auf diesem Dreieck wurde
zuerst die Grundlast geprüft: Die Verlustbasislinie ergab 3000 von 3000 zugestellten Paketen in
Sandwich-Tripeln gleicher Gesamtlänge (95-Prozent-Schranke der klassenweisen Verlustrate unter 0,6 Prozent
je Richtung), der MTU-Sweep trug bis exakt 1500 Byte Gesamtlänge vollständig, und über zehntausend
P0-Pakete blieben ohne unerklärten Verlust. Ferner überstand das goldene Prüfszenarien-Set aus
\chapref{chap:entwurf} den Pfad von mcs nach 1blu unverändert: elf Szenarien mit 15 Datagrammen und 0 bis
308 Byte Surplus, an beiden Enden aufgezeichnet und beidseitig mit elf von elf Szenarien bestanden; als
Vorstufe mit einer Richtung und einem Datagramm je Klasse ist das ein Pfadtransparenz-, kein
Zustellungsnachweis. Zusammen mit dem Piloten vom 13.08. (je Richtung sechs von sechs Optionsdatagramme
intakt) liefert der OpenVZ-Endpunkt zugleich einen eigenen Virtualisierungs-Datenpunkt: Auch dieser
Netzstapel normalisiert die Surplus Area nicht. Die vollständigen Suiten liefen anschließend fehlerfrei
auf beiden Helsinki-Paaren (je 18 Szenarien und Richtung).

Dennoch ist das Dreieck kein neutrales Medium, und zwar in zwei Punkten: der Wegevielfalt des Rückpfads
und einem Prüfsummen-Gate. Die Wegevielfalt dokumentiert \figref{fig:eval-reroute}:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        seg53/.style={draw=akzent, fill=akzenthell, semithick},
        seg54/.style={draw=sqgoldrand, fill=sqgold, semithick},
        anm/.style={font=\tiny, text=feldlinie},
        wert/.style={font=\scriptsize}]
        \node[font=\scriptsize\bfseries, anchor=west] at (0,1.35) {S-53-Zeitverlauf der Ankunfts-TTL
            (1blu $\rightarrow$ mcs, 2000 Pakete, verlustfrei)};
        \draw[seg53] (0,0) rectangle node[wert] {TTL 53: 1518} (9.11,0.55);
        \draw[seg54] (9.11,0) rectangle (9.93,0.55);
        \node[wert] at (9.52,0.85) {TTL 54: 137};
        \draw[seg53] (9.93,0) rectangle node[wert] {TTL 53: 345} (12.0,0.55);
        \node[anm, anchor=west] at (0,-0.25) {0\,s};
        \node[anm, anchor=east] at (9.06,-0.25) {32,73\,s};
        \node[anm, anchor=west] at (9.98,-0.25) {35,68\,s};
        \node[anm, anchor=east] at (12.0,-0.55) {43,12\,s};
        \node[font=\scriptsize\bfseries, anchor=west] at (0,-1.2) {Quellport-Sweep (30 Flüsse, kein
            Fluss gemischt)};
        \draw[seg53] (0,-2.1) rectangle node[wert] {16 Ports: TTL 53, Lumen, 11 Router} (6.4,-1.55);
        \draw[seg54] (6.65,-2.1) rectangle node[wert] {14 Ports: TTL 54, COLT, 10 Router} (11.95,-1.55);
    \end{tikzpicture}
    \caption{Reroute-Fenster im Szenario S-53 und Aufteilung des Quellportraums auf die ECMP-Zweige}
    \label{fig:eval-reroute}
\end{figure}

Aus \figref{fig:eval-reroute} geht hervor, dass derselbe Fluss für 2,93 Sekunden verlustfrei auf eine um
einen Router kürzere Variante wechselte und zurück, und dass die 1blu-Aggregation den Portraum nahezu
hälftig auf einen Lumen- und einen COLT-Zweig verteilt; der Port-Sweep ist dabei eine Stichprobe des
Hash-Raums, keine Lastmessung. Außerhalb des Fensters lag der feste Messfluss durchgehend auf der längeren
Lumen-Variante. Diese Beobachtung hat zur direkten Konsequenz, dass ein Pfad seine Struktur auch ohne
jedes Verlustsignal ändern kann; jede Pfadaussage dieser Arbeit gilt deshalb nur für ihr Messfenster.

Der zweite Befund ist das \textbf{Prüfsummen-Gate} aus \secref{sec:ff2-pfadmodell}, hier erstmals
gemessen: Ein Datagramm mit Surplus Area kommt auf diesen Pfaden genau dann an, wenn sein
UDP-Prüfsummenfeld null ist oder die Einerkomplementsumme über Pseudo-Header und gesamte IP-Nutzlast
aufgeht, wobei die Pseudo-Header-Länge aus der IP-Gesamtlänge stammt statt aus dem UDP-Längenfeld. Der
Nachweis gelang mit 248 einzeln nachgerechneten Paketen ohne eine einzige Abweichung, und der
entscheidende Kreuztest bestand aus zwei Zellen: Zelle G trägt ein Pad-Byte \texttt{ff} bei rechnerisch
gültiger OCS; sie ist wegen des Null-Pad-Gebots aus \secref{sec:extraktionsmethode} regelwidrig gebaut,
ihre Legacy-Faltung geht nicht auf, und sie verschwindet vollständig. Zelle C kompensiert denselben
Pad-Fehler durch eine gegenkorrigierte OCS \texttt{5b53}; sie ist ebenso regelwidrig, ihre Faltung geht
auf, und sie kommt vollständig an. Der Pfad prüft also die Legacy-Summe und nicht die OCS. Algebraisch
schließt sich das: Bei RFC-korrekter OCS ist die Legacy-Summe gleich dem Pad-Byte, denn der Längenzuschlag
des Pseudo-Headers hebt sich exakt gegen den Längensummanden der OCS-Definition auf
(\secref{subsec:surplus-ocs}). Damit ist die Frage aus \secref{sec:ff2-pfadmodell} beantwortet, ob
regelkonform gebaute Datagramme ein solches Gate passieren: Ja. Ein Datagramm mit Null-Pad und korrekter
OCS besteht die Legacy-Prüfung zwingend, und genau das erklärt die verlustfreie P0-Grundlast; die
Prüfsummenneutralität der Surplus Area ist der gemessene Entwurfsgrund der OCS
\cite[Sec.~9]{rfc9868}. Zugleich folgt eine Prüfbarkeitsgrenze für \secref{sec:soll-ist}: Die
Empfängerpflichten für verfälschte Pads und Prüfsummen sind auf solchen Pfaden nur messbar, wenn das
Datagramm pfadneutral gehalten wird, entweder durch die kompensierte OCS oder durch eine genullte
UDP-Prüfsumme; beide Umgehungen wurden mit je 40 Paketen validiert.

Wo dieses Gate sitzt, klärt die systematische Wiederholung des Kreuztests auf allen Paaren, ergänzt um
einen reinen Amazon-Vergleichspfad; \figref{fig:eval-gate} stellt alle Richtungen zusammen:

\begin{figure}[H]
    \centering
    \input{tikz/gate-matrix}
    \caption{Prüfsummen-Gate-Kreuzzellen je Messrichtung}
    \label{fig:eval-gate}
\end{figure}

Aus \figref{fig:eval-gate} geht hervor, dass die regelwidrige, aber faltungsgültige Zelle C jede der 14
Richtungen vollständig passiert, während die faltungsungültige Zelle G in zehn Richtungen vollständig
verschwindet und in vier Richtungen vollständig ankommt. Das Muster der Überlebensrichtungen trägt die
Verortung: Der Amazon-Backbone lässt G beidseitig durch, ebenso der Weg von 1blu zu aws; die Ausfälle
konzentrieren sich auf alle Richtungen mit Hetzner-Beteiligung und auf den 1blu-Eingang. Als sparsamstes
Modell bleibt eine kanten- oder empfangsseitige Legacy-Prüfsummenvalidierung bei Hetzner in beiden
Richtungen und bei 1blu nur eingehend, keine bei Amazon; das ist eine Schlussfolgerung aus
Endpunkt-Mitschnitten, denn zwischen Provider-Kante und letztem Transit-Abschnitt kann ohne Messpunkt im
Netz nicht unterschieden werden. Der Asien-Durchlauf replizierte beide Kantenbefunde aus sechs weiteren
Transiten und ergänzte drei weitere G-Überlebensrichtungen von 1blu nach Mumbai, Singapur und Osaka. In
der ersten Überlebensrichtung wurde zudem erstmals das Empfängerbild einer zugestellten G-Zelle
beobachtet: Der Empfänger verwirft die Optionen wegen des Pad-Bytes regelkonform und stellt die Nutzdaten
zu. Diese Beobachtung hat zur direkten Konsequenz, dass ein regelwidriges Pad ein Datagramm auf vielen
realen Pfaden unzustellbar macht, bevor irgendein Empfänger urteilen kann; die Null-Pad-Regel ist damit
keine Formalie, sondern Zustellbedingung.

Die zweite europäische Umgebung ist der Zugangspfad über einen Mobilfunk-Hotspot;
\figref{fig:eval-heimpfad} zeigt beide Betriebsarten:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        ep/.style={draw=sqtealrand, fill=sqteal, rounded corners=2pt, font=\scriptsize, align=center,
            inner sep=3pt},
        mb/.style={draw=sqgoldrand, fill=sqgold, rounded corners=2pt, font=\scriptsize, align=center,
            inner sep=3pt},
        wolke/.style={draw=sqgrau, fill=sqhell, dashed, rounded corners=8pt, font=\scriptsize,
            align=center, inner sep=4pt},
        uebergabe/.style={draw=feldlinie, fill=feldgrau, font=\tiny, inner sep=2pt, align=center},
        pf/.style={->, >=Stealth, semithick},
        kante/.style={font=\tiny, text=feldlinie, align=center},
        mp/.style={font=\tiny, text=akzent}]
        \node[ep] (gast1) at (0,0) {achim-Gast\\(NAT-Modus)};
        \node[mb] (nat) at (2.5,0) {VMware-\\NAT};
        \node[ep] (mac) at (4.6,0) {Mac\\en0};
        \node[mb] (hot) at (6.6,0) {iPhone-\\Hotspot};
        \node[wolke] (tele) at (9.75,-0.75) {Telefónica-Mobilfunknetz\\mit CGNAT\\(Attributionsintervall)};
        \node[uebergabe] (dcix) at (12.15,-2.3) {DE-CIX\\Frankfurt};
        \node[ep] (ziel) at (12.15,-3.65) {Ziel: mcs oder 1blu\\(16 bzw. 15 Router)};
        \node[ep] (gast2) at (0,-1.75) {achim-Gast\\(Bridge-Modus)};
        \draw[pf] (gast1) -- (nat);
        \draw[pf] (nat) -- (mac);
        \draw[pf] (mac) -- (hot);
        \draw[pf] (hot.east) -- (tele.west);
        \draw[pf] (gast2) .. controls (4.6,-1.75) .. node[kante, below=0.5mm, pos=0.5]
            {direkt ins Hotspot-Subnetz} (hot.south);
        \draw[pf] (tele) -- (dcix);
        \draw[pf] (dcix) -- (ziel);
        \node[mp] at (0,0.62) {$\triangle$ Mitschnitt Gast};
        \node[mp] at (4.6,0.62) {$\triangle$ en0 (nur 10./11.08.)};
        \node[mp, anchor=north] at (ziel.south) {$\triangle$ Mitschnitt Ziel};
        \node[kante, anchor=north west, text width=7.6cm, align=left] at (0,-3.05) {Rückrichtung im
            NAT-Modus: Antworten erreichen den Gast mit TTL 128, die NAT erzeugt sie als UDP-Vermittler
            neu. Im Bridge-Modus echte End-zu-End-TTL (48 von mcs, 51 von 1blu); alle Ziel-TTLs des
            Uplinks steigen um genau eins, der entfernte NAT-Sprung wird im Zahlenwerk sichtbar};
    \end{tikzpicture}
    \caption{Zugangspfad über den Mobilfunk-Hotspot in den Betriebsarten NAT und Bridge}
    \label{fig:eval-heimpfad}
\end{figure}

Auf diesem Pfad scheiterte der erste Realpfadversuch dieser Arbeit zweifach, und beide Mechanismen ließen
sich später sauber trennen. Im NAT-Modus kamen abgehende Optionsdatagramme zwar an, aber verändert: neun
von neun ohne ihre Surplus Area, gegen 18 von 18 intakte Kontrollen (Fisher exakt, einseitig,
$p = 2{,}13 \cdot 10^{-7}$). Der Mechanismus steckt im TTL-Bild aus \figref{fig:eval-heimpfad}: Die
Rückantworten erreichen den Gast mit TTL 128 statt eines dekrementierten Werts, die NAT arbeitet also als
UDP-Vermittler, der Datagramme aus seinem Verbindungszustand neu aufbaut; wer nur die UDP-Länge kennt,
erzeugt zwangsläufig Pakete ohne Surplus Area. Nach \secref{sec:ff2-pfadmodell} ist das ein
\textbf{Normalisierer} mit der Ergebnisklasse entfernt. Sein härtester Fall betrifft atomare
FRAG-Datagramme, deren gesamter Inhalt in der Surplus Area liegt: Sie kommen als leere 28-Byte-Hüllen an
und werden zugestellt, zehn von zehn; die Anwendung erhält leere statt fehlender Datagramme, ohne jedes
Verlustsignal. In der Rückrichtung verschwanden Optionsdatagramme dagegen vollständig: null von sechs
gegen 15 von 15 gleich große Kontrollen (Fisher exakt, einseitig, $p = 1{,}84 \cdot 10^{-5}$); offen
blieb zunächst, welches Merkmal der Klasse den Ausschlag gibt.

Diese Frage beantwortete die vorregistrierte Wiederholung mit Kontrollzellen zu je 30 Paketen: Kontrollen
ohne Surplus Area passieren in beiden Größen (58 und 44 Byte) vollständig, während gültige Optionen,
inhaltsloser Zufallsrumpf mit korrekter OCS und die Variante mit genullter UDP-Prüfsumme gleichermaßen
vollständig verschwinden (je null von 30; jeder Vergleich Fisher exakt, einseitig,
$p = 8{,}5 \cdot 10^{-18}$). Damit diskriminiert der Verwerfer allein die \textbf{Präsenz} einer Surplus
Area, also die Bedingung UDP-Länge kleiner als IP-Nutzlast: nicht den Optionsinhalt, nicht die
Prüfsummenlage, nicht die Größe; ein Prüfsummen-Gate ist doppelt ausgeschlossen, weil auch die genullte
Prüfsumme stirbt. Die Triangulation mit getauschtem fernen Ende (1blu statt mcs, anderes Endpunkt-Netz,
anderer Transit, identisches Zellenbild) verortet den Mechanismus in das gemeinsame Segment aus
Hotspot-Anschluss und Telefónica-Mobilfunknetz. Der Bridge-Betrieb schließlich nahm die VMware-NAT aus
dem Pfad: Erstmals verließen Surplus-Datagramme den Gast unversehrt, und nun verwarf der Pfad sie auch
im Uplink klassenrein (null von 30 je Zelle, atomare Zelle null von zehn, an beiden Zielen; die um genau
eins erhöhten Ziel-TTLs belegen den entfernten NAT-Sprung). Der Zugangspfad verwirft die Klasse also in
beiden Richtungen, und der NAT-Modus hatte den Uplink-Verwerfer zuvor verdeckt, weil die Normalisierung
jedes Datagramm surplus-frei machte, bevor es den Carrier erreichte: Ein früher Eingriff kann einen
späteren verdecken, hier als Messergebnis. Was ohne Messpunkt im Telefon oder Betreibernetz offen bleibt,
ist die Trennung zwischen Hotspot-NAT und Mobilfunknetz; jede engere Zuschreibung wäre eine Behauptung
ohne Beleg.

\subsection{Interkontinentale Pfade}
\label{subsec:eval-interkontinental}

Die fünfte Pfadstufe erweitert die Messbasis um zwei Hyperscaler-Fabrics und lange Transitketten;
\figref{fig:eval-interkontinental} ordnet die Endpunkte:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        ep/.style={draw=sqtealrand, fill=sqteal, rounded corners=2pt, font=\scriptsize, align=center,
            inner sep=3pt},
        block/.style={draw=sqblaurand, fill=sqblau, rounded corners=4pt, font=\scriptsize, align=center,
            inner sep=4pt},
        pf/.style={->, >=Stealth, semithick},
        pfpilot/.style={->, >=Stealth, thin, dashed},
        kante/.style={font=\tiny, text=feldlinie, align=center},
        warn/.style={font=\scriptsize, text=warnton}]
        \node[block] (eu) at (0,0) {\textbf{Europa}\\mcs (NBG) · helsinki (HEL)\\1blu (AS42730)\\
            aws2 (eu-central-1)};
        \node[block] (na) at (8.3,1.35) {\textbf{Nordamerika}\\aws (us-east-1)\\gcp (us-east1)};
        \node[block] (ap) at (8.3,-2.75) {\textbf{Asien-Pazifik (je t3.micro)}\\Mumbai · Singapur ·
            Tokio\\Osaka · Seoul · Sydney};
        \draw[pf, transform canvas={yshift=2pt}] (eu.18) -- (na.west);
        \draw[pf, transform canvas={yshift=-3pt}] (na.west) -- (eu.18);
        \node[kante] at (4.05,1.95) {aws-Paare: Zellen und Suiten;\\14 bis 23 Hops, $\sim$91 bis
            100\,ms};
        \draw[pfpilot] (eu.-8) -- (na.200);
        \node[kante] at (4.85,-0.42) {gcp: Piloten und Zellen; 7 bis 8 Router,\\$\sim$102\,ms;
            \textcolor{warnton}{Fabric verwirft $\times$}};
        \draw[pf] (eu.south) .. controls (1.4,-2.3) .. (ap.west);
        \node[kante] at (2.4,-2.85) {G/C/Baseline-Zellen\\je Region};
        \draw[pf] (na.south) -- (ap.north);
        \node[kante, anchor=west] at (8.75,-0.7) {Amazon-intern\\(Piloten)};
        \node[kante, anchor=west, align=left] at (0,-4.15) {aws2 nur für den Backbone-Vergleich;
            Rückweg-Trace aws $\rightarrow$ 1blu fehlt};
    \end{tikzpicture}
    \caption{Interkontinentale Messendpunkte mit Belegstufe und Rundlaufzeiten}
    \label{fig:eval-interkontinental}
\end{figure}

Die beiden Fabrics verhalten sich entgegengesetzt. Der komplette Amazon-Pfad aus Nitro-Fabrik,
Adressumsetzung am Internet-Gateway und Transatlantik-Transit transportierte die Klasse in allen sechs
Pilot-Lanes unversehrt (54 von 54 Datagrammen mit intaktem Surplus und gültiger OCS; die Zustellung mit
gültiger OCS belegt nebenbei, dass die Adressumsetzung die UDP-Prüfsumme korrekt anpasst, ohne die
Surplus Area anzutasten). Die anschließende Diskriminator-Matrix mit sechs Zellformen bestätigte das
vollständig (240 von 240), und schließlich liefen die kompletten Suiten auf allen drei aws-Paaren
fehlerfrei durch, einschließlich der MTU-Kante bei exakt 1500 auf jedem Paar. Damit ist zweierlei
belegt: Der Präsenz-Verwerfer ist keine Eigenschaft von Hyperscaler-Fabrics an sich, und der
EU-US-Transit verwirft die Klasse nicht; dies sind die ersten interkontinentalen Pfade dieser Arbeit,
auf denen RFC~9868 in beiden Richtungen funktioniert. Die Google-Fabric dagegen verwarf die Klasse
bidirektional und inhaltsblind: Baselines 40 von 40, alle fünf Surplus-Zellformen null von 200, keine
leeren Hüllen; auch die Zelle, die jedes Legacy-Gate passieren würde, stirbt. Die Endpunktmitschnitte
lokalisieren den Verlust vor beziehungsweise hinter der Gast-Schnittstelle, also in der Fabric; der
Transit ist durch das direkte Peering mit nur sieben bis acht sichtbaren Routern und durch den
identischen Befund gegen 1blu entlastet. Ein \textbf{Präsenz-Verwerfer} tritt hier folglich als
Eigenschaft einer Cloud-Fabric auf, providerspezifisch statt klassentypisch.

Auf den Hinwegen nach Asien-Pazifik zeigte sich dieselbe Mechanismenklasse ein drittes Mal, nun im
Transit und quellabhängig; \figref{fig:eval-opfermatrix} fasst die Lage zusammen:

\begin{figure}[H]
    \centering
    \input{tikz/ap-opfermatrix}
    \caption{Schicksal der Surplus-Klasse auf den Hinwegen nach Asien-Pazifik je Quelle}
    \label{fig:eval-opfermatrix}
\end{figure}

Aus \figref{fig:eval-opfermatrix} geht hervor, dass die Opfermenge von der Quelle abhängt: Beide
Hetzner-Standorte verlieren die gesamte Surplus-Klasse nach Tokio und Seoul, 1blu zusätzlich nach
Sydney, während der Amazon-Backbone alle vier getesteten Ziele erreicht und sämtliche Rückwege sauber
blieben (120 von 120 in den Eskalationsmatrizen). Drei Kontrollen stützen die Deutung: Die
Eskalationsmatrizen zeigen für die betroffenen Lanes exakt die inhaltsblinde Zellensignatur der
Google-Fabric; Osaka teilt mit Tokio die sichtbare AS-Kette und bleibt dennoch transparent, die
Verwerfer liegen also in zielspezifischen Teilpfaden hinter der sichtbaren Kette; und die
Sydney-Anomalie blieb im zeitversetzten Gegentest quellabhängig. Interkontinentale Erreichbarkeit der
Surplus Area ist damit eine Eigenschaft des konkreten Paars aus Quelle und Ziel, nicht des Ziels.

\subsection{Mechanismenklassen und Folgen}
\label{subsec:eval-mechanismen}

Die Messungen füllen die drei Mechanismenklassen aus \secref{sec:ff2-pfadmodell} mit belegten Wirkorten,
jeweils als Intervall zwischen zwei Messpunkten und nie als Gerät \cite[Sec.~25.6]{rfc9868}: der
\textbf{Normalisierer} an der Grenze aus VMware-NAT und VMnet mit der Ergebnisklasse entfernt; das
\textbf{Prüfsummen-Gate} an der Hetzner-Kante in beiden Richtungen und am 1blu-Eingang, während Amazon
nirgends prüft, mit der Ergebnisklasse verworfen für alle faltungsungültigen Datagramme; der
\textbf{Präsenz-Verwerfer} in drei Habitaten, nämlich Zugangsnetz, Cloud-Fabric und quellabhängigem
Transit, mit der Ergebnisklasse verworfen für die gesamte Klasse. Dazu tritt der Maskierungseffekt als
Wechselwirkung: Ein Normalisierer vor einem Präsenz-Verwerfer macht diesen unsichtbar, weil er dessen
Auslöser entfernt. Was das für ein einzelnes Datagramm bedeutet, bündelt \figref{fig:eval-paketformen}
am Beispieldatagramm aus \chapref{chap:entwurf} und seinen zwei härtesten Gegenstücken:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        iph/.style={feld, minimum height=4.8mm, inner sep=1pt, font=\tiny},
        udph/.style={feldakzent, minimum height=4.8mm, inner sep=1pt, font=\tiny},
        nutz/.style={feld, minimum height=4.8mm, inner sep=1pt, font=\tiny},
        sur/.style={feldwarn, minimum height=4.8mm, inner sep=1pt, font=\tiny},
        pf/.style={->, >=Stealth, semithick},
        anm/.style={font=\tiny, text=feldlinie},
        reihe/.style={font=\scriptsize\bfseries}]
        % Reihe 1: pad-odd unveraendert (Skala 0.055 cm/Byte)
        \node[reihe, anchor=west] at (0,0.55) {pad-odd (42 Byte): unverändert, mcs $\rightarrow$ 1blu};
        \node[iph, anchor=west, minimum width=1.1cm] at (0,0) {IPv4 20};
        \node[udph, anchor=west, minimum width=0.44cm] at (1.1,0) {8};
        \node[nutz, anchor=west, minimum width=0.17cm] at (1.54,0) {3};
        \node[sur, anchor=west, minimum width=0.61cm] at (1.71,0) {11};
        \draw[pf] (2.7,0) -- node[anm, above] {Cloud-Dreieck} (4.5,0);
        \node[iph, anchor=west, minimum width=1.1cm] at (4.6,0) {IPv4 20};
        \node[udph, anchor=west, minimum width=0.44cm] at (5.7,0) {8};
        \node[nutz, anchor=west, minimum width=0.17cm] at (6.14,0) {3};
        \node[sur, anchor=west, minimum width=0.61cm] at (6.31,0) {11};
        \node[anm, anchor=west] at (7.3,0) {Surplus samt Pad-Byte byteidentisch};
        % Reihe 2: TYPED gestrippt
        \node[reihe, anchor=west] at (0,-0.95) {Optionsdatagramm (58 Byte): Surplus entfernt, NAT-Modus};
        \node[iph, anchor=west, minimum width=1.1cm] at (0,-1.5) {IPv4 20};
        \node[udph, anchor=west, minimum width=0.44cm] at (1.1,-1.5) {8};
        \node[nutz, anchor=west, minimum width=0.88cm] at (1.54,-1.5) {16};
        \node[sur, anchor=west, minimum width=0.77cm] at (2.42,-1.5) {14};
        \draw[pf] (3.6,-1.5) -- node[anm, above] {VMware-NAT} (4.5,-1.5);
        \node[iph, anchor=west, minimum width=1.1cm] at (4.6,-1.5) {IPv4 20};
        \node[udph, anchor=west, minimum width=0.44cm] at (5.7,-1.5) {8};
        \node[nutz, anchor=west, minimum width=0.88cm] at (6.14,-1.5) {16};
        \node[anm, anchor=west] at (7.3,-1.5) {44 Byte: Nutzdaten erhalten, Optionen fort (30/30)};
        % Reihe 3: ATOM leere Huelle
        \node[reihe, anchor=west] at (0,-2.45) {atomares FRAG-Datagramm (106 Byte): leere Hülle};
        \node[iph, anchor=west, minimum width=1.1cm] at (0,-3.0) {IPv4 20};
        \node[udph, anchor=west, minimum width=0.44cm] at (1.1,-3.0) {8};
        \node[sur, anchor=west, minimum width=4.29cm] at (1.54,-3.0) {Surplus 78 (gesamter Inhalt)};
        \draw[pf] (6.0,-3.0) -- node[anm, below=0.5mm] {VMware-NAT} (6.9,-3.0);
        \node[iph, anchor=west, minimum width=1.1cm] at (7.0,-3.0) {IPv4 20};
        \node[udph, anchor=west, minimum width=0.44cm] at (8.1,-3.0) {8};
        \node[anm, anchor=west] at (8.75,-3.0) {28 Byte, zugestellt (10/10): Inhalt vernichtet};
    \end{tikzpicture}
    \caption{Drei beobachtete Schicksale von Beispieldatagrammen auf realen Pfaden}
    \label{fig:eval-paketformen}
\end{figure}

Aus \figref{fig:eval-paketformen} geht hervor, dass zwischen Erhalt und Verlust eine dritte, betrieblich
gefährlichste Form liegt: das zugestellte leere Datagramm, bei dem der Inhalt ohne jedes Signal
verschwindet. Für Anwendungsentwickler hat das zur direkten Konsequenz, dass ein Einsatz von UDP Options
mit klassenbasiertem Totalverlust und mit stiller Inhaltsvernichtung rechnen muss; für Betreiber folgt
die Pflicht, beide Richtungen getrennt zu prüfen, weil eine NAT einen dahinterliegenden Verwerfer
maskieren kann. In der Summe bleibt die Surplus Area auf den untersuchten Pfaden dort vollständig
erhalten, wo weder Normalisierer noch Verwerfer eingreifen, namentlich im europäischen Cloud-Dreieck und
auf den Amazon-Pfaden; sie scheitert klassenrein an einem Mobilfunk-Zugangsnetz, an einer Cloud-Fabric
und auf einzelnen Transitwegen, und sie verliert regelwidrige Pads an einem Prüfsummen-Gate. Ob die
Anforderungen des Standards unter diesen Bedingungen erfüllbar bleiben, bewertet der folgende
Soll-Ist-Abgleich.

\section{Soll-Ist-Abgleich mit RFC 9868}
\label{sec:soll-ist}

FF1 fragt, welche Anforderungen von RFC~9868 sich unter Linux und IPv4 im Benutzerraum vollständig,
teilweise oder nicht erfüllen lassen (\secref{sec:zielsetzung}). Bewertet werden die 67 normativen
Aussagen der Konformitätsmatrix aus \secref{sec:anforderungsmatrix}; die FR- und NFR-Kennungen dienen
dabei als Nachweisindex und nicht als zweiter Nenner, denn 58 der 67 Zeilen verweisen auf sie. Je
Aussage folgt der Nachweis einer festen Reihenfolge: zuerst die Notiz der Matrixzeile, dann die
verknüpfte Anforderungskennung, dann Quelltext und Test, zuletzt, wo vorhanden, das Realpfadszenario.
Die Abbildung des Repository-Status auf die Kategorien folgt \chapref{chap:methodik}: Nicht gewählte
MAY-Funktionen, Aussagen außerhalb des Umfangs und Sätze ohne BCP-14-Schlüsselwort bleiben sichtbar,
zählen aber weder als erfüllt noch als technisch unmöglich. Damit ist auch die aus
\chapref{chap:entwurf} übergebene Frage zu beantworten, ob die zusammengefassten Empfangsberichte für
die Messkampagnen ausreichen: allein nicht. Weil ein Pad-Fehler und eine fehlgeschlagene OCS-Rechnung
im selben Status zusammenfallen und verworfene Ausgänge als \zitat{kein Datagramm} erscheinen, stützt
sich jeder Realpfadnachweis auf das Kampagnenpaket aus Mitschnitten, Sendemanifest und Auswerterlauf;
ein Ausbleiben (etwa der Reassemblierungs-Timeout, der keine eigene Verwerfmeldung erzeugt) ist nur
über diese Negativevidenz messbar. Das Ergebnis der Einzelbewertung fasst \tabref{tab:eval-sollist}
zusammen:\footnote{Die Einzelbewertung aller 67 Zeilen liegt als \file{thesis/daten/konformitaet"=kategorien.csv}
im Repositorium der Arbeit; ein Prüfskript erzwingt die Übereinstimmung der Summen mit der Matrix.}

\begin{table}[!htb]
    \centering
    \caption{Soll-Ist-Bewertung der 67 normativen Aussagen je RFC-Bereich}
    \label{tab:eval-sollist}
    \footnotesize
    \setlength{\tabcolsep}{4pt}
    \begin{tabularx}{\textwidth}{L{1.9cm} c c c c c Y}
        \toprule
        \textbf{Bereich} & \textbf{Aussagen} & \textbf{vollst.} & \textbf{teilw.} & \textbf{n.\,erf.} &
        \textbf{n.\,anw.} & \textbf{stärkster Nachweis bzw. zentrale Grenze} \\
        \midrule
        Sec. 7 bis 9 & 8 & 8 & 0 & 0 & 0 & Wire-Prüfung mit drei Prüfern; Prüfsummenneutralität real
        (P0, Gate-Kreuztest) \\
        Sec. 10 & 17 & 17 & 0 & 0 & 0 & Parser-Property- und Fuzz-Flächen; reale Negativfälle
        (Längenfehler, UNSAFE) \\
        Sec. 11.1 bis 11.10 & 22 & 16 & 2 & 0 & 4 & FRAG-Suite lokal und real; Grenzen: NOP-Frühabbruch,
        Cachevertrag der Komfortebene \\
        Sec. 12 bis 14 & 9 & 7 & 0 & 0 & 2 & Dispositionsordnung; S-06-Matrix aus UDP-Prüfsumme und OCS
        real \\
        Sec. 15 bis 19 & 7 & 3 & 2 & 0 & 2 & API-Verträge; Fragmentsteuerung nur gebündelt \\
        Sec. 23 bis 26 & 4 & 0 & 2 & 0 & 2 & dokumentierte Abwägungen zu DoS-Grenzen und
        Referenzordnung (SHOULD) \\
        \midrule
        Summe & 67 & 51 & 6 & 0 & 10 & \\
        \bottomrule
    \end{tabularx}
\end{table}

Aus \tabref{tab:eval-sollist} geht hervor, dass 51 der 67 Aussagen vollständig, sechs teilweise und
keine als grundsätzlich unerfüllbar bewertet werden; zehn sind nicht anwendbar. Dass die Kategorie
\zitat{nicht erfüllbar} leer bleibt, ist ein Ergebnis und kein Mangel des Maßstabs: Die harten Grenzen
des Benutzerraum-Zugangs treffen den Betrieb des Sendewegs, aber keine einzelne Normaussage der Matrix.
Diese Beobachtung hat zur direkten Konsequenz, dass RFC~9868 auf der gewählten Plattform als Ganzes
implementierbar ist und die Streitfälle in den Randbedingungen liegen, nicht im Kern.

Die \textbf{vollständig} erfüllten Aussagen umfassen den gesamten Kern: Ortung und Ausrichtung der
Surplus Area, die OCS-Arithmetik samt Nullregeln, die TLV-Rahmung mit allen Längen- und
Ordnungspflichten, die acht must-support-Optionen und die FRAG-Verarbeitung einschließlich
Reassemblierung. Der Nachweis ruht auf den sechs Ebenen der Testarchitektur aus
\secref{sec:pruefarchitektur} (211
Testattributzeilen, zehn Property-Dateien mit 1024 Fällen je Eigenschaft, neun Fuzz-Ziele, 172
Lean-Theoreme in elf Modulen, die Wire-Prüfung mit drei getrennten Prüfern) und zusätzlich auf den
Realpfadläufen aus \secref{sec:beobachtbarkeit-eval}: Die Negativkampagne bestätigte alle 15
Prüfklassen ohne eine einzige Abweichung von der RFC-Erwartung, darunter das Verwerfen bei Pad- und
OCS-Fehlern, das stille Übergehen unbekannter SAFE-Optionen und die Nutzdatenkürzung bei einer
UNSAFE-Option; die Robustheitswelle ergänzte, dass EOL-Füllbytes unverarbeitet bleiben, NOP-Leitern
bis zur Schwelle sieben meldungsfrei durchlaufen und ein erweitert kodiertes Längenfeld unter 255 als
Senderverstoß alle Optionen verwirft, während ein handgebautes atomares FRAG-Einzelfragment trägt,
anhängige Fragmentsätze parallelen Verkehr vollständig isolieren und ungleiche Optionssätze je Fragment
regelkonform reassemblieren. Dieselbe Suite lief anschließend unverändert auf den Helsinki-Paaren und
transatlantisch auf drei aws-Paaren; die Bewertung stützt sich damit auf fünf reale Pfade.

Die sechs \textbf{teilweise} erfüllten Aussagen sind sämtlich SHOULD-Aussagen oder API-Zuschnitte,
keine verletzten MUSTs. Die NOP-Begrenzung erkennt und meldet lange Läufe, bricht die Verarbeitung
aber nicht ab; die Empfangs-API bündelt Pflichtoptionen je Datagramm oder Fragmentsatz, statt sie je
Fragment und Quelle einzeln erzwingbar zu machen; die DoS-Abwägungen aus Abschnitt 25 sind teils
umgesetzt, teils als begründeter Verzicht dokumentiert (kein Deckel für die Optionszahl); die
Referenzordnung gilt beim Senden kanonisch, wird beim Empfang aber nicht erzwungen. Der sechste Fall
ist der Cachevertrag der Reassemblierung: Die tiefe Schnittstelle verlangt einen Cache je
Adress-und-Port-Paar als dokumentierte Aufruferpflicht, die mitgelieferte Komfortebene bindet ihren
Empfänger jedoch nur an die Ports, sodass Datagramme verschiedener Adresspaare eine Mengengrenze
teilen (\secref{sec:betriebsgrenzen}). Dazu treten die erwarteten Betriebsgrenzen außerhalb der
Matrix: Der Sendeweg trägt bis exakt 1500 Byte Gesamtlänge und scheitert darüber am Kernel
(\texttt{EMSGSIZE}, je Richtung 33 von 33 zugestellten Längenstufen bis 1500 und null darüber, auf
allen Paaren bis hin zu den transatlantischen repliziert), eine Pfad-MTU-Ermittlung fehlt, und die
Cache-Bereinigung bleibt aufrufergesteuert. Für den Einsatz heißt das: Die Bibliothek erfüllt die
Norm, verlangt aber einen Betreiber, der Betriebsgrenzen kennt.

Die zehn \textbf{nicht anwendbaren} Aussagen bleiben sichtbar und begründet: die nicht gewählte
Empfangsprüfung der Bytes nach EOL, die nicht implementierten Optionen TIME und EXP samt der
reservierten AUTH- und UNSAFE-Kinds, die Entwurfs- und Namensregeln für neue Optionen, die
Multicast-Betrachtungen sowie zwei Sätze des API-Abschnitts ohne BCP-14-Schlüsselwort. Dass diese
Abgrenzung trägt, zeigte der Realpfad nebenbei: Eine formgültige TIME-Option lief als unbekannte
SAFE-Option still mit, genau wie es die Einstufung als nicht gewählte Funktion verlangt.

Vier \textbf{Differenzen zwischen Spezifikation, Modell, Programm und Laufzeit} verdienen eigene
Sätze. Erstens ist die Kombination aus UDP-Prüfsumme null und OCS null aus dem Benutzerraum exakt
konstruierbar und wurde auf der Leitung nachgewiesen; in der Matrix aus UDP-Prüfsumme und OCS wird
diese Zelle regelkonform verarbeitet, obwohl das Datagramm dann keinerlei Prüfsummenschutz trägt. Das
ist normgetreues Verhalten und zugleich eine sichtbare Integritätslücke der Norm. Zweitens löst
Erratum~8834 den Widerspruch zwischen den Abschnitten 10 und 14 zugunsten des Sec.-10-Verfahrens auf
(\chapref{chap:methodik}); die Laufzeit folgt dem nachweislich, denn im realen Überlängenfall wurde
ein gültiges REQ vor der defekten Option nicht gemeldet, sondern der gesamte Optionsbereich verworfen.
Drittens behandelt der Parser ein erweitert kodiertes Längenfeld unter 255 strenger, als der RFC dem
Empfänger vorschreibt; das ist als lokale Politik dokumentiert und in \tabref{tab:eval-sollist} keine
Abweichung, sondern eine ausgewiesene Verschärfung. Viertens beweisen die Lean-Theoreme Aussagen über
Modelle der Wire-Regeln, nicht über das laufende Programm; die Brücke bleibt Prüfsache der Tests
(\secref{sec:pruefarchitektur}), weshalb diese Arbeit keine formale Konformitätsaussage erhebt.

Drei \textbf{Abweichungen} traten im Prüf- und Messbetrieb auf und sind mit Ursache, Korrektur und
Restrisiko dokumentiert. Der Raw-Empfänger beendete sich auf öffentlichen Hosts still beim ersten
fremden UDP-Paket; Ursache war ein als Ende gewerteter Filterausgang, die Korrektur besteht aus dem
Weiterlesen nach gefiltertem Rauschen und einer Gesamtfrist,\footnote{Commits \texttt{f3bf430} und
\texttt{8f8c11b} im Implementierungsrepositorium.} und als Restrisiko bleibt, dass eine Messung ohne
diese Korrektur Zustellungen unterschätzt hätte; alle Kampagnen dieser Arbeit liefen mit den
korrigierten Binärdateien. Der Um-eins-Fehler der Surplus-Ortung überstand 23 Einzelfalltests und
fiel erst der Zweitprüfung im Pull-Request auf; als Korrektur wurden Property-Tests und Fuzz-Ziele je
Parsefläche verpflichtend, und das Restrisiko gleichartiger Lücken ist dadurch verkleinert, nicht
beseitigt. Der RFC-Abgleich des Abschlussschritts fand schließlich trotz grüner Prüfkette weitere
Konformitätslücken, darunter die Behandlung unbekannter UNSAFE-Optionen vor einem FRAG; die Korrektur
erfolgte gesammelt und real nachgeprüft, und als Restrisiko bleibt die Erkenntnis, dass eine grüne
Prüfkette Konformität nicht garantiert (\secref{sec:vorgehen}).

Damit kehrt die Bewertung zu der in \chapref{chap:grundlagen} offen gelassenen Frage zurück, was aus
dem Verlust einzelner Fragmente wird, den auch FRAG nicht behebt. Die Messungen beantworten sie in
vier Schritten. Ein fehlendes Fragment lässt seinen Satz liegen, ohne Nachbarn zu stören: In der
langen Serie aus 300 Zwei-Fragment-Sätzen mit gezielt zurückgehaltenem Terminalfragment kamen exakt
die geplanten 270 Sätze an, die 30 beschädigten verhungerten still, und die Quote blieb über die
Serie drittelgenau konstant (570 von 570 Fragmenten auf dem Draht, keine Drift). Der
Reassemblierungs-Timeout wirkt real, ist aber nur über Negativevidenz messbar, denn das Auslaufen
erzeugt keine eigene Verwerfmeldung. Die Eindeutigkeit der Identification ist reine Senderpflicht:
Bei absichtlich gleicher Kennung und komplementären, nicht überlappenden Fragmenten setzte der
Empfänger ein nie gesendetes Datagramm zusammen und lieferte es kommentarlos aus; das ist kein
Implementierungsfehler, sondern eine Robustheitslücke der Norm, deren einzige vorgesehene Erkennung
die optionale APC ist, und mit APC wird der Fehlschlag gemeldet, die Nutzdaten werden dennoch
zugestellt. Die Grenze des Soft-State schließlich zeigt \figref{fig:eval-cachegrenze}:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        zu/.style={draw=akzent, fill=akzenthell, semithick},
        weg/.style={draw=warnton, fill=warnhell, semithick},
        wert/.style={font=\scriptsize},
        anm/.style={font=\tiny, text=feldlinie}]
        \node[font=\scriptsize\bfseries, anchor=west] at (0,1.0) {80 anhängige Fragmentsätze je Lauf
            gegen die Mengengrenze 64};
        \draw[zu] (0,0) rectangle node[wert] {64 Sätze zugestellt (Ankunftsreihenfolge der
            Erstfragmente)} (9.6,0.55);
        \draw[weg] (9.6,0) rectangle node[wert, align=center] {16 verhungern\\still} (12.0,0.55);
        \node[anm, anchor=north west, text width=11.9cm] at (0,-0.3) {in 12 von 12 Läufen exakt auf
            Vorhersage; je Richtung 966 von 966 Fragmenten auf dem Draht; 240 von 240 unfragmentierten
            Kontrollen unbeeinflusst; ein atomares Fragment wird auch am vollen Cache zugestellt};
    \end{tikzpicture}
    \caption{Deterministische Cachegrenze der Reassemblierung im Überlastszenario}
    \label{fig:eval-cachegrenze}
\end{figure}

Aus \figref{fig:eval-cachegrenze} geht hervor, dass die SHOULD-Grenze aus Abschnitt~25.4
implementierbar und deterministisch ist und Bestandsverkehr wie atomare Zustellung schützt; was der
RFC offen lässt, sind Zahlenwert und Überlaufverhalten, und die Folge ist stiller Verlust ohne jedes
Signal an den Sender. Diese Beobachtung hat zur direkten Konsequenz, dass eine Anwendung mit
Fragmentverlust, Kennungskollisionen und Cacheüberlauf selbst rechnen muss; die Norm liefert ihr dafür
nur die APC und die Berichte.

Zwei Geltungsgrenzen rahmen dieses Ergebnis. Alle Realpfadaussagen gelten für saubere, kalibrierte
Pfade; das Verhalten unter stochastisch gestörtem Pfad wurde nach dem begründeten Verzicht aus
\secref{sec:testkonzept} nur mechanismenweise geprüft (Verlust, Timeout, Duplikat, Umordnung, Quote,
Cachegrenze), nicht als Mischung. Und die Empfängerpflichten für verfälschte Pads und Prüfsummen sind
auf Pfaden mit Prüfsummen-Gate nur über die beschriebenen pfadneutralen Konstruktionen messbar; für
den Pad-Fall ist das sogar beweisbar, weil ein regelwidriges Pad die Legacy-Faltung zwingend bricht.
In der Summe beantwortet der Abgleich FF1 so: 51 der 67 normativen Aussagen sind unter Linux, IPv4
und Benutzerraum vollständig erfüllt, sechs teilweise mit dokumentierten Gründen, keine ist
grundsätzlich unerfüllbar, und zehn liegen außerhalb des gewählten Umfangs. Wie diese Bewertung
zustande kam, und zwar unter durchgehender Beteiligung von Sprachmodellen, beschreibt der folgende
Abschnitt.

\section{Reflexion der KI-gestützten Arbeitsweise}
\label{sec:ki-reflexion}

Dieser Abschnitt liefert die in \chapref{chap:methodik} offen gebliebenen beschreibenden Kennzahlen der
Zusammenarbeit nach. Er ist rein deskriptiv: Er berichtet belegte Fälle mit ihren Ausgängen und erhebt
keine Wirksamkeitsaussage, denn eine Prüfung ohne Sprachmodelle als Vergleichsgruppe existiert nicht.
Die Rollen sind die aus \chapref{chap:methodik}: das führende Werkzeug für Entwürfe und Quelltext, der
getrennte Zweitprüfer mit dem Auftrag, Fehler und Gegenbeispiele zu suchen, und der Autor, der
entscheidet und verantwortet. Grundlage ist ein Befundregister aus zwölf Fällen, jeder mit öffentlichem
Anker im Implementierungsrepositorium oder in den versiegelten Archiven;\footnote{Register als
\file{thesis/daten/ki"=befundregister.csv} im Repositorium der Arbeit; die Pull-Request-Verläufe sind
öffentlich, Existenz, Titel, Merge-Zeitpunkte und Kommentarfolgen wurden am 25.08.2026 nachgeprüft.}
das Register ist eine geprüfte Auswahl aus 27 Pull-Requests und Messläufen, kein Vollbestand. Vier
Fallgruppen tragen das Bild:

\begin{itemize}[leftmargin=1.6em]
    \item \textbf{Übernommene Funde vor dem Merge.} Im Wire-Modell-Review wurden zwei Funde übernommen
        (eine erzwungene Längeninvariante der Prüfsummenrechnung und eine geprüfte Längen-Verengung,
        Fix im Pull-Request dokumentiert); beim Aufbau des Lean-Gates drei (ein erweitertes
        Theorem-Audit sowie zwei gegen Veränderung gepinnte Werkzeugbezüge). Der wertvollste Einzelfund
        gelang allein dem Zweitprüfer: der durch ein Byte-Palindrom maskierte Ausrichtungsfehler im
        unabhängigen Prüfprogramm der Wire-Lane (\secref{sec:pruefarchitektur}); ebenso fing allein die
        Zweitprüfung den Um-eins-Fehler der Surplus-Ortung, den 23 eigene Einzelfalltests überstanden
        hatten.
    \item \textbf{Abgelehnte Befunde.} Nicht jeder Befund wurde übernommen: Im Lean-Gate-Review lehnte
        der Autor einen Vorschlag mit dokumentierter Begründung ab (der bemängelte Bestand lag außerhalb
        des Änderungsumfangs), und im TLV-Parser-Review blieben Prüferbefunde insgesamt ohne Übernahme,
        weil sie den beabsichtigten Umfang betrafen und keinen Faktenirrtum zeigten. Die Ablehnungen
        sind in den Verläufen nachlesbar; sie gehören zum Bild derselben Arbeitsweise.
    \item \textbf{Prüfung nach dem Merge.} Eine interessante Beobachtung besteht darin, dass die
        Reihenfolge nicht immer hielt: Der Empfangs-Fix wurde vier Minuten vor der
        Zweitprüfer-Rückmeldung zusammengeführt, unmittelbar danach folgte ein Reparatur-Commit an der
        Prüfkette, und die zugehörige Gesamtfrist kam erst drei Tage später. Auch der Schlussaudit fand
        trotz durchgehend grüner Prüfkette weitere Konformitätslücken, und dort musste eine vom Modell
        vorgeschlagene Korrektur selbst noch einmal korrigiert werden (\secref{sec:vorgehen}).
    \item \textbf{Gemeinsam übersehene Fehler.} Der Zählfehler der bidirektionalen Kampagne (15 statt
        18 Kontrollformen) passierte drei Prüfende und wurde erst nachträglich als Erratum im
        versiegelten Archiv berichtigt. Ein Faktencheck dieses Textes widerlegte zudem eine eigene
        Kapitelaussage am Quelltext (der nur teilweise eingelöste Cachevertrag aus
        \secref{sec:soll-ist}), und die Nachprüfung der Grundlagen führte zu zwei ausdrücklich als
        lokale Politik dokumentierten Entscheidungen samt einer geschlossenen Testlücke. Außerhalb des
        Quelltexts ließ ein Abrufwerkzeug den entscheidenden Satz eines geprüften Abstracts aus, sodass
        die Erstbewertung falsch war und nach einem Autorenhinweis berichtigt wurde
        (\chapref{chap:methodik}).
\end{itemize}

Als beschreibende Kennzahlen des Registers ergibt das: Zehn der zwölf Fälle endeten mit Übernahme,
einer teils mit Übernahme und teils mit begründeter Ablehnung, einer vollständig mit Ablehnung; fünf
Fälle lagen vor dem Merge, sechs danach, einer außerhalb des Repositoriums. Drei Funde stammen allein
vom Zweitprüfer, die Übernahmenserie eines Falls allein vom führenden Werkzeug, und zwei Fehler wurden
zunächst von allen Beteiligten übersehen. Für die frühen Projektphasen fehlen vollständige Modell- und
Konfigurationsangaben; diese Fälle tragen den Vermerk der lückenhaften Provenienz und werden nicht
nachträglich vervollständigt (\chapref{chap:methodik}). Ebenso bleibt festgehalten, dass die
KI-Prüfungen angefordert, aber nicht technisch erzwungen waren und Sammelcommits die innere Reihenfolge
eines Schritts verschmelzen.

Das Schlussbild ist eine gemischte Bilanz, und genau sie trägt die Glaubwürdigkeit der Methode: Die
Arbeitsweise erzeugte nachweisbare Korrekturen bis hinein in Prüfprogramm und Kapiteltext, sie
produzierte Fehlbefunde, die der Autor mit Begründung ablehnte, und sie übersah Fehler, die erst eine
andere Ebene fand. Mehr als diese Beschreibung gibt der Aufbau nicht her, und mehr behauptet diese
Arbeit nicht.

\section{Diskussion und Grenzen der Aussagekraft}
\label{sec:diskussion-grenzen}

Zusammengenommen ergeben die beiden Ergebnisteile eine Pointe, die keiner allein trägt. Der
Soll-Ist-Abgleich zeigt, dass RFC~9868 im Benutzerraum implementierbar ist: 51 der 67 normativen
Aussagen vollständig, sechs teilweise, keine grundsätzlich unerfüllbar. Die Pfadmessungen zeigen
zugleich, dass diese Konformität die Zustellung nicht sichert: Ob eine Surplus Area ankommt,
entscheiden Normalisierer, Prüfsummen-Gates und Präsenz-Verwerfer entlang des konkreten Pfads, und
darum ist Erreichbarkeit eine Eigenschaft des Paars aus Quelle und Ziel, nicht des Ziels und nicht des
Endpunkts. Dieselben Messungen bestätigen umgekehrt den Entwurfskern des Standards: Die
Prüfsummenneutralität der OCS ist genau die Eigenschaft, die regelkonforme Datagramme durch die
beobachteten Gates trägt, während jedes regelwidrige Pad zur Zustellfrage wird.

Für die Betroffenen heißt das Folgendes. Wer UDP Options in einer Anwendung einsetzt, muss mit
klassenbasiertem Totalverlust und mit stiller Inhaltsvernichtung rechnen, denn ein leeres Datagramm
statt eines fehlenden erzeugt kein Signal; Ende-zu-Ende-Bestätigung, die Auswertung der
Empfangsberichte und im Zweifel eine Kapselung, die die Klasse vor dem Pfad verbirgt, sind deshalb
Teil des Betriebs und nicht Kür. Wer Netze betreibt, muss beide Richtungen getrennt prüfen, weil eine
Adressumsetzung einen dahinterliegenden Verwerfer maskieren kann, und sollte wissen, dass beobachtete
Prüfsummen-Gates gerade die regelkonformen Datagramme mit beschädigtem Pad verwerfen, bevor ein
Empfänger urteilen kann.

Diesen Aussagen stehen klare Grenzen gegenüber. Jeder Pfadbefund gilt für das beobachtete Paar, die
Richtung und das Messfenster; eine Aussage über \zitat{das Internet} trifft diese Arbeit nicht, und
als Zugangsnetz wurde nur ein Mobilfunk-Hotspot-Pfad untersucht. Ohne Messpunkt im Betreibernetz
endet jede Ursachenzuordnung an einem Intervall zwischen zwei eigenen Messpunkten; Formulierungen wie
Hetzner-Kante oder NAT-Vermittler sind sparsame Modelle, keine Gerätenachweise. Die Realpfadergebnisse
gelten für saubere, kalibrierte Pfade, und das Zusammenspiel der Störmechanismen wurde nach dem
begründeten Verzicht nur einzeln geprüft. Auf der Prüfseite beweisen die Lean-Theoreme Modelle statt
des Programms, findet Fuzzing Fehler statt Fehlerfreiheit, und bleibt die Folge der Modellausgaben
samt der Provenienz früher Projektphasen nicht reproduzierbar; eine Wirksamkeitsaussage zur
Arbeitsweise erhebt \secref{sec:ki-reflexion} bewusst nicht. Offen bleibt schließlich benannte
Nacharbeit der Bibliothek, allen voran die nur teilweise eingelöste Paarbindung des
Reassemblierungs-Caches auf der Komfortebene. Mit diesen Ergebnissen und Grenzen ist die Evaluation
vollständig; die wörtliche Beantwortung beider Forschungsfragen und die daraus folgenden Empfehlungen
übernimmt \chapref{chap:fazit} (\secref{sec:ff-beantwortung}).
